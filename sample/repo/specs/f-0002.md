# F-0002 — Count lines too

A second mode for the counter: alongside the word count, it can report how many lines a file
carries.

## Behavior

- `count --lines FILE` prints the number of lines in `FILE`.
- A file that does not end on a newline still counts its last, partial line.
- An empty file counts zero lines, the same as it counts zero words today.
