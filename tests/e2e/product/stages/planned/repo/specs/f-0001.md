# F-0001 — Count lines too

## Decisions

| choice | against | why |
| --- | --- | --- |
| a separate `count_lines` | a flag on `count` | the word count keeps its signature |

## The design

`src/lines.py` holds `count_lines(text)`: the number of lines, a last line without a newline
included.

## Acceptance tests

```
python3 -m unittest tests.test_lines
```

## Records

None.

## Stories

- S: Lines end on a newline — acceptance: tests/test_lines.py::test_last_line
