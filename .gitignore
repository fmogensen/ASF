__pycache__/
*.pyc
.pytest_cache/

# generated by any build (pipx install, pip install -e): tracking it means every build
# dirties the checkout, and harvest then refuses to fast-forward it
*.egg-info/
