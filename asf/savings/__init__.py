"""asf.savings — the savings pass: the tick proposes the next cheapest win from its own numbers.

It measures and proposes and acts on nothing: every function here reads event lists and returns
values, and the one caller that files a card goes through the ordinary filer (F-0100 §1.2).
"""
