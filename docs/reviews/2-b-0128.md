# Review — B-0128, round 2

Branch `fix/B-0128`, head `4f29539` (`task(B-0128): answer review round 1's C list — finished(),
PR numbers, a plan`). The branch's own commits since the merge-base carry review round 1's
answer: `12fbfd037` + `4f2953909` (the fix), `57669e3f9` (round 1's own review doc). The branch
is heavily stale against `origin/main` (28 ahead / 58 behind) and its history is padded with
~23 commits that are byte-for-byte copies of unrelated `origin/main` commits, mislabeled
`fix(B-0128):` by an earlier bad rebase on this branch — none of those touch this Task; the four
real commits above stay inside the plan's declared `writes:`.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The plan's Task 1 declares `writes: asf/workers/lifecycle.py asf/feeder/rows.py tests/test_lifecycle.py tests/test_feeder.py`. `git show --name-only` on the four real commits (`e00acd719`, `12fbfd037`, `57669e3f9`, `4f2953909`) touches exactly those four files plus `docs/plans/b-0128.md` and `docs/reviews/1-b-0128.md` — a plan and a review, outside the code `writes:` fence by this repo's own convention, nothing else. |
| every Step of the Task is implemented | pass | Step 1 (`finished(r)` gate, not bare `ended`): `asf/workers/lifecycle.py:417-432` — `_settling_run` filters on `finished(r)`, and `settled()` is `_settling_run(...) is not None`. Step 2 (PR numbers off the ruling): `asf/workers/lifecycle.py:439-449` — `settled_prs()` reads `report.ruling()` off the settling run's own result and returns `PR_RE.findall(...)` deduped in first-seen order. Step 3 (the row): `asf/feeder/rows.py:444-450` — `c.get('settled')` emits `FIX_CORRECT`/`WAITS_MERGE` with `#<n>, #<n>` appended when `prs` names any, `waits_on='merge'`, no `LAUNCH`. `corrections()` (`lifecycle.py:520-536`) threads both `settled` and `prs` per item. Step 4: all five tests the plan names exist and pass (below). |
| acceptance tests byte-identical to the plan's | pass | `tests/test_feeder.py:688-710` (`test_b0128_a_settled_correction_waits_on_merge_not_another_adjudicate`, `..._names_the_prs_the_ruling_waits_on`, `..._with_no_pr_named_is_the_bare_action`) and `tests/test_lifecycle.py:299,326` (`..._an_adjudicate_run_does_not_answer_the_correction`, `..._a_crashed_adjudicate_run_delivers_no_ruling_and_does_not_settle`) match the plan's `## Task 1 — Step 4` names and bodies exactly. |
| those tests were run and are green | pass | `python3 -m unittest -v` on the five named tests → five `ok`. `python3 -m unittest tests.test_lifecycle tests.test_feeder` → `Ran 192 tests in 31.772s` / `OK`. |
| the Gate commands are green | fail | `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` refused by the sandbox ("this command requires approval"), same refusal round 1 recorded; hand-checked `asf/feeder/rows.py`, `asf/workers/lifecycle.py` against every pattern in `tools/forbidden-conventions.txt` — no match. `python3 -m asf.cli check` → rc 1, 282 lines, the same pre-existing bare-decision-reference/no-closing-rule class round 1 recorded, none naming B-0128. `python3 tools/run_tests.py` → `Ran 3353 tests in 495.830s`, `FAILED (failures=1, skipped=14)`, red: `tests.test_watch.WatchTests.test_a_tick_appended_while_watching_is_printed` — deterministic, not flaky (fails in isolation too), but confirmed pre-existing staleness: a clean `origin/main` checkout passes this test cleanly, because `origin/main`'s `tests/test_watch.py` already pins `watch.run`'s clock (`now=_on_day`) so the test is not the real wall clock's date; this stale branch's copy of the file predates that fix and fails only because the real UTC date has moved past the file's hardcoded `2026-09-25`. Not this diff's file, not this diff's fault — but a named Gate command still did not come back fully green, so the row is `fail`. |
| no secret value printed, no background process, no skipped check | fail | No secret in the diff. No background process left running: `run_tests.py` was waited on to completion; a temporary `git worktree add .review-main-check origin/main`, made only to confirm `test_watch`'s failure is pre-existing on a clean base, was removed before this report (`git worktree list` no longer shows it). Skipped: `bash tools/check_conventions.sh`, same sandbox refusal as the row above — the row is `fail` regardless of the hand-check's result, same standard round 1 applied. |

verdict: approved

## C list

(none)

## I list

- `run_tests.py`'s lone red (`tests.test_watch...test_a_tick_appended_while_watching_is_printed`)
  is this branch's own staleness, not this diff's: `origin/main` already carries the fix
  (`now=_on_day` pinning `watch.run`'s clock) that this 58-behind branch lacks. It will resolve
  itself once the branch lands through a rebase onto current `main`; nothing to change here.
- The `STALEMATE → ADJUDICATE` row this Task's `if` guards (`asf/feeder/rows.py:452-454`) no
  longer passes `correction=c['text']` the way `origin/main`'s current version of the same line
  does — again pure staleness (that argument was added by a later, unrelated `main` commit this
  branch never absorbed), not something either `e00acd719`/`12fbfd037`/`4f2953909` touched. A
  three-way merge restores it cleanly (confirmed by hand: resolving the branch's actual rebase
  conflict against current `main` on this exact line auto-merges both changes with no conflict
  marker); still worth a glance once this branch is rebased for landing, since a naive `git diff`
  read on its own (rather than a real merge) makes it look like a regression.
- `docs/plans/b-0128.md`'s own `## Acceptance` checkboxes are already checked (`[x]`) by the
  writer, ahead of this review's own sign-off — harmless (the checks pass, so the marks are
  accurate), but worth naming since the card's own top-level Acceptance is still the unchecked
  `- [ ]` this review does not tick either; only the operator or a later close-out step should
  flip that one.

### Missed in round 1

(none — round 1's own diff, `e00acd719` alone, is fully accounted for by its own C1-C3, and this
round finds nothing further inside that same diff)
