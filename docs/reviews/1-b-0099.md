# Review — B-0099, round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `writes:` is declared none; the diff is `asf/tick/file_bugs.py` and `tests/test_file_bugs.py` only, the code and its test |
| every Step of the Task is implemented | fail | no plan `docs/plans/b-0099.md` and no spec exist on the branch or `origin/main`, so no Step can be checked; the code does what the title says (`asf/tick/file_bugs.py:389-392` runs `do_index(root)` and reloads items before the rule check) |
| acceptance tests byte-identical to the plan's | fail | no plan, so there is nothing to compare; the card's Acceptance is empty. Tests added: `tests/test_file_bugs.py:468` (rule with `removed:`), `:479` (card deleted) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_file_bugs` → `Ran 24 tests in 10.556s` / `OK` |
| the Gate commands are green | fail | `tools/run_tests.py` → `FAILED (errors=5, failures=35, skipped=14)`, red: test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane; `test_dry_run` + `test_gitfixture` fail identically on a clean `origin/main` (`FAILED (failures=3)`, hooksPath the operator home githooks path vs `/dev/null`), so an environment fault in this reviewer's home, not the diff; the other four modules were not re-run on main. `tools/check_generic.sh` → `check_generic: clean`. `tools/check_conventions.sh` was not run (blocked by the sandbox prompt) |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff; no background process left running. Skipped: `check_conventions.sh` not run; a full-suite run was launched by the harness in the background after a foreground timeout and waited for. No writer report was found, so it could not be read |

verdict: changes requested

## C list

- `docs/plans/b-0099.md` (missing): there is no plan or spec, so the "every Step" and "byte-identical tests" rows cannot pass. Fix: add the plan naming the Task, or fill the card's Acceptance so the two tests at `tests/test_file_bugs.py:468` and `:479` are the recorded acceptance.

## I list

- `asf/tick/file_bugs.py:391-392`: `do_index` runs on every file-bugs tick, and again at `:445` when anything was filed. Fine, but the index is written twice on a filing tick. Only rebuild when needed, or accept the cost.
- `asf/tick/file_bugs.py:392`: the reload discards `parse_errors` (`_errors`). It is harmless here, since the first load already returned on errors.
- Confirm the four other red modules (test_doctor, test_install, test_sample_product, test_e2e_lane) on a clean home; they are unrelated to this diff on inspection.
