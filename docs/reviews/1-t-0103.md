# Review — T-0103, round 1

Head: `73ff02727` (branch `worker/T-0103`, PR #49)

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff origin/main...HEAD --name-only` → `asf/feeder/rows.py`, `tests/test_feeder.py` only |
| every Step of the Task is implemented | pass | Step 1: `CAPPED_KINDS` frozenset with excluded-kinds comment (`asf/feeder/rows.py:121-125`). Step 2: `_capped` (`asf/feeder/rows.py:915-923`), byte-identical to the spec's fence (docs/specs/f-0080.md:353-363). Step 3: `candidates` applies `_capped` after `hold_unlanded`/`hold_classes`, filtered to `r.launches and r.kind in CAPPED_KINDS` (`asf/feeder/rows.py:976-980`) — the narrower application P10 of the plan explicitly chose over the spec's broader wording. Step 4: `test_a_closed_item_whose_evidence_went_quiet_stays_closed_and_unemitted` (`tests/test_feeder.py:697-707`) builds an index with the item `Closed` and no evidence and asserts `candidates` emits nothing for it |
| acceptance tests byte-identical to the plan's | pass | Plan's Task 5 acceptance fence is `python -m unittest tests.test_feeder -v` (docs/plans/f-0080.md:414-416); ran verbatim (as `python3`, same module) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_feeder -v` → `Ran 145 tests in 0.279s` / `OK` |
| the Gate commands are green | pass | `python3 -m unittest tests.test_feeder -v` → OK (145); `python3 tools/run_tests.py` → `Ran 3947 tests in 508.267s (112 module(s), 8 at a time)` / `OK (skipped=14)`; `bash tools/check_generic.sh` → `check_generic: clean` |
| no secret value printed, no background process, no skipped check | pass | all three gate commands run in the foreground, no `run_in_background`; no credentials/tokens in output; the 14 skips are pre-existing (unrelated modules — `tests/test_feeder.py` itself has 0 skips) and no check was omitted |

verdict: approved

No C findings.

I:
- `tests/test_feeder.py:697` — the docstring calls this "end to end, through ingest and the feeder", but the test builds the index by hand and never calls `asf.record.ingest`; it demonstrates `sticky`'s output is honoured by the feeder, not the ingest→feeder pipeline. This matches Task 5 Step 4's own wording ("asserted through an index ...") so it isn't a scope miss, just a docstring that oversells what the test exercises.
