# Review T-0075 — round 1

No writer's report was found on `worker/T-0075` beyond the commit `d5ca1fc22` ("wip(worker/T-0075):
the session ended with this uncommitted — committed by the factory (B-0094)"), the branch's only
commit since `origin/main`. Reviewed that diff, the plan's Task 4 (`docs/plans/f-0091.md:313-373`),
and spec §2.6 (`docs/specs/f-0091.md:220-234`) directly.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff origin/main..HEAD --stat`: `asf/metrics/metrics.py \| 29 +++++++++++++++++++++++-----` and `tests/test_metrics.py \| 35 +++++++++++++++++++++++++++++++++++` only — both listed in the Task's `writes:` |
| every Step of the Task is implemented | fail | Step 1 (`scorecard_rows` gains `landing`, leads the table, `—` form when `None`) → `asf/metrics/metrics.py:514-527`, matches the spec's row and note text verbatim. Step 3 (`cmd_rollup` passes `product=getattr(args, 'product', None)`) → `metrics.py:1449`, matches. Step 4 (tests) → `tests/test_metrics.py:377-410`. **Step 2 is not correctly implemented**: the plan requires "treat a missing registry as `landing=None` rather than an error" (`docs/plans/f-0091.md:339-341`); the code at `metrics.py:720-728` does this by catching `env.ConfigError` around `measure.ended_runs`/`measure.table`, but that call chain never raises `ConfigError` for a missing registry — verified by direct execution (`asf.improve.measure.ended_runs('nonexistent-product', …)` against a fresh `ASF_HOME` returns `[]`, no exception; `measure.table([])` then returns `{'non_landing_share': 0.0, 'usd_per_landed_item': None, 'landed_items': 0, …}` — `lifecycle._folded` returns `_EMPTY` for a missing file rather than raising, `asf/workers/lifecycle.py:294-298`). So a real "no `~/.ASF`" or "product given but no registry" rollup — exactly the case `cmd_rollup` itself anticipates at `metrics.py:1447-1448` (`_resolve_product` raising `ConfigError`, falling back to `DEFAULTS`) — renders `\| landing \| 0 % of session time landed nothing \| — per landed item (0 landed, 7 days) \|` instead of the required `\| landing \| — \| no product resolved — the session registry is machine-local \|`. |
| acceptance tests byte-identical to the plan's | pass | Task 4 fences no literal test code (only prose + the `python -m unittest tests.test_tick_steps tests.test_tick tests.test_metrics -v` command it shares with Task 3, `docs/plans/f-0091.md:357-368`); the prose's three bullets (row leads, carries both numbers, `—` form) are covered by `tests/test_metrics.py:378-386` and the fixture line at `:305`. No literal fence to diff against. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_metrics -v` (run now): `Ran 95 tests in 47.013s` / `OK` |
| the Gate commands are green | pass | `python3 -m unittest tests.test_metrics -v` → `OK` (above). `python3 -m unittest discover -s tests -t . 2>&1 \| tail -3` (run now, full suite): `Ran 3392 tests in 1897.497s` / `OK (skipped=14)`. `bash tools/check_generic.sh` (run now): `check_generic: clean` |
| no secret value printed, no background process, no skipped check | pass | Diff adds no `print`, no subprocess/thread spawn, no `unittest.skip`. Full-suite output's `gh pr list failed: … 'x/y' …` / `gh api … 404` lines are `TrunkReleases`' own fixtures exercising an intentionally-nonexistent repo name, not a leak. `ps aux` mid-run showed the discover process and its expected child `git commit -q -s -m record: inbox a-flaky-spec.md` under a `tick_test_*` temp dir (a test's own sandboxed record, not this checkout) — no process left running after completion (`[exited with code 0]`). |

verdict: changes requested

## C

- `asf/metrics/metrics.py:720-728` — the `except env.ConfigError:` guard around the landing
  computation never fires for the scenario it exists to handle. Replace the try/except with a
  check for whether the registry actually exists, e.g.:
  ```python
  landing = None
  if product is not None:
      from asf.improve import measure
      from asf.workers import pool
      if os.path.isfile(pool.sessions_path(product)):
          runs = measure.ended_runs(product, since=week[0], as_of=f'{day}T23:59:59Z')
          t = measure.table(runs)
          landing = (t['non_landing_share'], t['usd_per_landed_item'], t['landed_items'])
  ```
  so a product with no session ledger at all falls through to `landing=None` (the `—` row), rather
  than computing a zero-filled table (`0 %`, `— per landed item (0 landed, 7 days)`) that
  misrepresents "no data" as "0 % waste, 0 landed".

## I

- `tests/test_metrics.py:406-409` (`test_a_missing_registry_renders_the_dash_form_not_an_error`)
  mocks `measure.ended_runs` to raise `env.ConfigError` directly, which — per C1 — never happens
  in real code. Once C1 lands, replace the mock with a real missing ledger (a temp `ASF_HOME` with
  no `state/<product>/sessions.jsonl`) so the test exercises the actual guard rather than an
  exception nothing raises.

### Missed in round 1

(none — this is round 1)
