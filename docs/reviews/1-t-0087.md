# Review T-0087 — round 1

No writer's report was found for `worker/T-0087` — the only commit is
`de1e077 wip(worker/T-0087): the session ended with this uncommitted — committed by the
factory (B-0094)`. Reviewed the diff against `origin/main` and the plan directly.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | `git diff origin/main...HEAD --name-only` adds `full_suite_with_changes.log` (a stray 118-byte, one-line dot-progress capture with no summary line) alongside the two `writes:` files `asf/workers/lifecycle.py`, `tests/test_lifecycle.py` |
| every Step of the Task is implemented | fail | Steps 1-9 are implemented and match §2.1/§2.2/§2.3 and P6/P7 (`asf/workers/lifecycle.py:139-228,956-1042`). Step 10's `OneClassifierTest` (`tests/test_lifecycle.py:409-476`) is not: its `NOT_YET` list (`:417-428`) names ten modules but the tree has two more unexempted state-word literals, so the test the Task itself adds fails on this tree — see next row |
| acceptance tests byte-identical to the plan's | pass | every `SessionStateInvariants` bullet in the plan's Acceptance section (`docs/plans/f-0098.md:137-155`) has a matching test in `tests/test_lifecycle.py:292-434` (e.g. the B-0028 re-judge bullet → `test_a_retired_dead_pid_line_is_dead_and_a_later_result_is_re_judged`, the `state_of` no-git bullet → `test_state_of_gathers_no_git_for_a_recorded_run_or_a_live_one`) |
| those tests were run and are green | fail | `python3 -m unittest tests.test_lifecycle -v` (run now): `FAILED (failures=1)` — `AssertionError: Lists differ: ["asf/scorecard/score.py:28: 'dead' as a literal", "asf/workers/cloudpid.py:23: 'working' as a literal", "asf/workers/cloudpid.py:25: 'dead' as a literal"] != []`. The only artifact the session left, `full_suite_with_changes.log`, is 118 dots with no `OK`/`FAILED` line — no evidence it was ever run to completion |
| the Gate commands are green | fail | `python3 -m unittest tests.test_lifecycle -v` → fails (above, same command as the Task's own Gate). `bash tools/check_generic.sh` → `check_generic: clean` (pass). `python3 -m unittest discover -s tests -t . 2>&1 \| tail -3` was not completed within 900s locally; the targeted suites of the other three Tasks (`tests.test_workers tests.test_harvest tests.test_capacity tests.test_feeder tests.test_views tests.test_tick_summary tests.test_tick_steps`, 528 tests) and `tests.test_lane` (77 tests) are green, so the one known regression is `tests.test_lifecycle` itself |
| no secret value printed, no background process, no skipped check | fail | `full_suite_with_changes.log` is committed evidence of a check that was run but never confirmed to finish (no summary line) — a skipped check, and its presence in the tree is itself the scope fail above |

verdict: changes requested

## C

- `tests/test_lifecycle.py:417-428` — `OneClassifierTest.NOT_YET` is missing `asf/scorecard/score.py` (`DEAD_PREFIXES = ('dead', …)` at `asf/scorecard/score.py:28`) and `asf/workers/cloudpid.py` (`WORKING = 'working'` at `:23`, `DEAD = 'dead'` at `:25`). Either add both to `NOT_YET` with the docstring's per-Task accounting this plan's Task 4 (or a `needs writes:` this session never recorded) would need to close, or narrow `_violations`/`STATE_WORDS` so a module's own unrelated vocabulary (cloud-token states, a scorecard's failure-class prefixes) isn't flagged as a session-state derivation — but as committed, `python3 -m unittest tests.test_lifecycle -v` fails, which is Task 1's own Gate command.
- `full_suite_with_changes.log` — drop this file from the branch; it is outside `writes:`, carries no test summary, and is not evidence of anything.

## I

- (none — round 1)
