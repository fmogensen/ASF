# Review — B-0125, round 2

Head reviewed: `afbe3ea` (`fix(B-0125): round 1 — the C list of docs/reviews/1-b-0125.md`), on
`fix/B-0125`, three commits above `origin/main` (`22aae40`). No writer's report file exists on the
branch; the two `fix(B-0125)` commit messages are the report and were read as such, in order.
Round 1 (`docs/reviews/1-b-0125.md`) requested changes on six C items; this round checked each,
then ran the whole table.

The plan `docs/plans/b-0125.md` now exists (added by this head, C5), and so does
`docs/specs/b-0125.md` (C6), so the three rows round 1 had to fail for want of a plan are
checkable for the first time.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | the card declares `writes:` none; the plan's Task declares `asf/approvals.py tests/test_approvals.py docs/plans/f-0031.md docs/specs/f-0031.md` (`docs/plans/b-0125.md:17`). Seven files changed: those four, plus the job's own three artifacts — `docs/plans/b-0125.md` and `docs/specs/b-0125.md` (both demanded by round 1's C5/C6) and `docs/reviews/1-b-0125.md` (round 1's own output, committed by `bc10fad`). `asf/approvals.py` and `tests/test_approvals.py` are inside F-0031's own footprint (`docs/plans/f-0031.md:129`); `docs/specs/**` is not in the amendable set (`asf/amendable.py:41` `kinds()`), so amending it is not a governed write. Nothing outside the change's subject: no `asf/hooks.py`, no settings, no record file |
| every Step of the Task is implemented | pass | Step 1 → `HOOK_ERROR_LINE` (`asf/approvals.py:813`), printed at `:862` with `why=str(e) or type(e).__name__`, `return 2` at `:863`. Step 2 → `record_hook_error` (`:820-836`) writes the traceback via `_log_traceback` (`:838-845`) to `hook_error_log_path()` (`:816-817`) and one `hook-error` ledger line, both wrapped so neither can raise (`:833-836`). Step 3 → that line carries no `hold` key, and `_fold` skips every record without one (`:575-577` `if not hold or '/' not in hold: continue`), so `open_holds` never sees it; the three other ledger consumers all go through `open_holds` (`asf/harvest/lane.py:1327`, `asf/tick/widen_footprint.py:366`, `asf/tick/step_wave.py:173`), so the two new event kinds break no reader. Step 4 → rotation at `:840-841` against `HOOK_ERROR_LOG_MAX_BYTES = 4 << 20` (`:808`), the same cap and single generation as `harvest.keep_red_output`. Step 5 → `raise_hook_errors` (`:955-975`), wired at `:1014` inside `raise_holds`, which is the first statement of the wave's `launch` (`asf/tick/step_wave.py:417`) — unconditional, so the line reaches every tick. Step 6 → no refusal path is touched: `git diff` over `asf/approvals.py` shows no hunk in `_refusal_lines`, `_amendable_refusal_lines`, the operator-config line (`:873`), the full-suite line (`:896`) or the invalid-matrix line. Step 7 → `docs/plans/f-0031.md:162` and `docs/specs/f-0031.md:326` renamed, and D7 (`docs/specs/f-0031.md:77`) amended to the new wording. Step 8 → tests, next row. The card's investigation clause, which round 1 found unanswered, is answered with measurements in `docs/specs/b-0125.md` §1–§3 |
| acceptance tests byte-identical to the plan's | pass | the plan's five Acceptance ids all exist, with the wording the plan gives: `HookTest.test_classifier_exception_is_an_error_not_a_refusal` (`tests/test_approvals.py:412`, asserts `out == 'approvals hook error: boom; not a refusal — retry\n'`, `assertNotIn('refused', out)`, rc 2); `test_hook_error_logs_its_traceback_and_records_it` (`:423`); `test_a_message_less_exception_still_names_its_type` (`:436`); `test_the_hook_error_log_rotates_rather_than_growing_without_bound` (`:447`); `TickRaiseTest.test_repeated_hook_errors_surface_once_in_the_tick_log` (`:783`). The first is the card's own named test. **But every one of the plan's five `file:line` citations is wrong** — it names `:387 :398 :411 :422 :757`, a uniform 25–26 lines short of where those tests are; see C1. The ids themselves are exact and runnable, which is why this row is a pass |
| those tests were run and are green | pass | `env -u BACKLOG_ID_RANGE python3 -m unittest -v` over all five ids → five `ok`, `Ran 5 tests in 2.179s` / `OK`. Isolation checked: `HookTest.setUp` redirects `env.ASF_HOME` to a tmp dir, so `hook_error_log_path()` cannot touch the operator's real `~/.ASF/logs` |
| the Gate commands are green | pass | **tests**: `env -u BACKLOG_ID_RANGE python3 tools/run_tests.py` → `Ran 3203 tests in 731.857s (102 module(s), 8 at a time)` / `FAILED (failures=1, skipped=14)` / `red: test_dry_run`, and identically on a first run (`873.512s`). The single failure is the documented load flake, not this branch's: it is `tests/test_dry_run.py:75` `self.assertEqual(lines1, lines2, 'two dry runs printed different things')`, failing only on the token `gate: ? modules, 0s` vs `1s` — `f'{time.monotonic() - started:.0f}s'` at `asf/harvest/harvest.py:797`, a wall-clock measurement rounded to whole seconds and required byte-identical across two consecutive dry runs. `git diff --stat origin/main..HEAD` touches neither that test, nor `asf/harvest/`, nor `asf/tick/`. The module alone is green: `python3 -m unittest tests.test_dry_run` → `Ran 6 tests in 35.589s` / `OK`. Same flake, same line, same token recorded at `docs/reviews/3-b-0114.md:28`, where it was verified red at `origin/main` too and filed as an I. Refiled as I1. **check generic**: `bash tools/check_generic.sh` → `check_generic: clean`. **check conventions**: the literal command is refused by this session's permission layer (`This command requires approval`, for `bash`, `sh` and `./` alike), as in round 1; run through its faithful in-suite wrapper, which `subprocess.run`s the identical script over `REPO_ROOT` and asserts `returncode == 0` (`tests/test_conventions.py:171-173`, `:250-254`) → `Ran 30 tests in 86.440s` / `OK`. **record**: `python3 -m asf.cli check` → exit 1, 262 lines, every one in the separate record repo the run names on its own first line (`record: <the operator's record checkout>`, a different repository from this one) — bare `Dn` references, `writes:` intersections among Active tasks, `stories/S-000n.md:16` closing-rule warnings. That repo is outside this worktree and this diff touches no record file, so the result is branch-independent and the same 262 lines rounds 1–3 of B-0114 read |
| no secret value printed, no background process, no skipped check | pass | **No secret**: the new paths print `str(e)` (`asf/approvals.py:862`), `str(exc)[:200]` (`:830`), a job name and a timestamp. `traceback.format_exception` (`:845`) emits frames without locals, so no classified `Bash` command, no captured `patch` and no env value reaches the log; the log is `~/.ASF/logs/approvals-hook.log`, never the session transcript, and the transcript gets one line. **No background process**: every run in this review was foreground and waited on — the two full suites (873s, 732s), `tests.test_conventions` (86s), `tests.test_dry_run` (36s), the five acceptance tests, `check_generic.sh`, `asf.cli check`. **No skipped check**: the one gate command that could not be invoked literally was run as its own logic through the wrapper that executes the same script, above; nothing was waived |

verdict: changes requested

## C list

- `docs/plans/b-0125.md:55,59,61,64,67` — every `file:line` in the Acceptance points 25–26 lines
  short of the test it names, so the fence's evidence lands inside unrelated tests: `:387` is in
  `test_operator_config_write_is_always_refused`, `:398` is that test's tail, `:411` is the blank
  line above the first B-0125 test. The five ids are correct and green; only the numbers are
  wrong. Fix, in order of appearance: `tests/test_approvals.py:387` → `:412`; `:398` → `:423`;
  `:411` → `:436`; `:422` → `:447`; `:757` → `:783`. In the same file, Step 1 (`:27`) cites the
  `str(e) or type(e).__name__` idiom as `asf/tick/step_wave.py:103`; it is at `:112` (and again
  at `:402`).
- `docs/specs/b-0125.md:16-17,22-27,30,46,48,52,60,62,71` — every `asf/approvals.py` citation in the
  spec is 70 lines short (30 for the two before the insertion point), so a reader following the
  map lands in unrelated code: `:832` for `classify` is a `return 2` in the amendable branch, and
  `:833` for the allow-path return is inside the full-suite refusal. This is the one document
  whose stated purpose is that "the next intermittent refusal is diagnosed against numbers rather
  than re-investigated"; wrong coordinates defeat it. Fix, each as written → correct:
  `run_hook (asf/approvals.py:777)` → `:847`; `_enforce (:796)` → `:866`; in the §1 table
  `env.load_product | :808` → `:878`, `item_of_job | :809` → `:879`,
  `amendable.write_target | :811` → `:881`, `classify | :832` → `:902`,
  `is_granted → _fold → read | :848` → `:918`, `append | refuse, :493` → `:523`; "returns 0 at
  `:833`" → `:904`; `item_of_job` (`:596`) → `:666`; `env.load_product`
  (`asf/env.py:656-666`) → `asf/env.py:775-785`; `append` (`:493-495`) → `:523-525`; `read`
  (`:498-514`) → `:528-545`; `_intended_change` (`:669-672`) → `:739-758`, with its 8 KB ceiling
  `_PATCH_LIMIT` at `:726`. `asf/workers/lifecycle.py:202-208` is one line out — `runs` is at
  `:203`, `latest` at `:207`.

## I list

- `asf/harvest/harvest.py:797` + `tests/test_dry_run.py:75` — the load flake that reddened both
  full-suite runs of this session. A wall-clock duration rounded with `:.0f` is printed into
  output a test requires byte-identical across two consecutive runs, so any dry run whose gate
  straddles a whole second fails it; under the 8-way harness on a loaded host it straddles
  reliably. Not this branch's (the diff touches neither file, the module alone is green, and
  `docs/reviews/3-b-0114.md:28` recorded it red at `origin/main`), and not fixable here — its own
  card. Either freeze the duration in the dry run or exclude that token from the comparison.
- `asf/approvals.py:820-836` — `record_hook_error` appends to `approvals.jsonl` on the hottest
  path in the factory, and unlike the traceback log it just got a cap for (`:840`), the ledger has
  none. A systematic break writes one `hook-error` line per tool call per session forever, and
  `raise_hook_errors` re-reads the whole file every tick to count them. The announce marker pops
  the *count*, never the lines. Worth the same bound the log now has, or a compaction.
- `docs/plans/b-0125.md:21` — the Task's `writes:` omits the two files the same commit adds
  (`docs/plans/b-0125.md`, `docs/specs/b-0125.md`). A plan that does not declare itself is a
  footprint no scope check can confirm from the plan alone.
- `docs/plans/b-0125.md:41` — Step 5 says the tick line is "wired into the tick at
  `asf/tick/step_wave.py`". It is wired at `asf/approvals.py:1014`, inside `raise_holds`, which
  the wave calls; `step_wave.py` is unchanged by this diff. The effect is right, the sentence
  points at the wrong file.
- Round 1's five I items stand unaddressed, which is their right — restated so they are not lost:
  the unguarded `out(...)` in `raise_hook_errors` (`:972`) against `close_landed`'s `if out:`
  twenty lines on; the count with no time window, so two unrelated errors months apart read as one
  run of two (the plan now owns this under Accepted costs); the print-before-marker ordering (the
  plan now says which loss was picked — good); the single machine-wide `approvals-hook.log` whose
  header carries `job=` but not the product; and the over-long line and curly quote in the F-0031
  docs, both still there (`docs/specs/f-0031.md:77` gained a second `’` in "the hook’s own log").

### Missed in round 1

Visible in round 1's diff of `22a2c6a`, so recorded as I rather than held against this head:

- `asf/approvals.py:862` — the hook now tells a session to retry, with no budget anywhere. A
  systematic break (an unreadable product file, a kill on the ~1 s import the spec measures in
  §2.3) answers every retry the same way, so a session can spend its whole turn retrying a hook
  that will never pass. The tick line after the second error is the only brake, and it speaks to
  the operator, not to the session. The wording is still far better than the false refusal it
  replaces; a retry count in the line ("retry — 3rd error in this job") would let the session stop
  on its own.
- `asf/approvals.py:861` — `record_hook_error(product or environ.get('ASF_PRODUCT'), ...)` hands
  the ledger a product *name* where the tick's `raise_hook_errors` hands it a `Product`. Checked,
  not a bug: `env.state_dir` (`asf/env.py:788-792`) takes either and resolves both to the same
  directory. Worth a word, because the two halves of this feature reach one file through two
  different argument types.
