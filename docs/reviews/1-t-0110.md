# Review — T-0110, round 1

Diff reviewed: `37e16f811` (the sole commit of `origin/worker/T-0110`, one commit on
`ad7448834` = `git merge-base origin/main origin/worker/T-0110`) — the direct diff of the task's
own work, since `origin/main` has since diverged far enough from that base that a straight
`git diff origin/main origin/worker/T-0110` mixes in unrelated drift.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git show --stat 37e16f811` touches only `asf/workers/report.py`, `tests/test_report.py`, `asf/briefs/facts.py`, `tests/test_brief_facts.py`, `tests/test_workers.py`, `tests/test_lifecycle.py`, `tests/test_tick_steps.py` — exactly Task 1's `writes:` line in `docs/plans/f-0025.md` |
| every Step of the Task is implemented | pass | Steps 1–10 of Task 1 (`docs/plans/f-0025.md:95-115`) each land: `COMMON`/`_CHECK`/`_check` (`asf/workers/report.py:1-45`), `KIND_FIELDS` for the spec's twelve kinds (`:47-105`), `schema` (`:108-112`), `fence` (`:115-131`), `typed` (`:139-171`), the five readers `check`/`failure`/`ruling`/`ruling_fields`/`summary` (`:174-260`), `render` (`:283-303`), `contract` (`:306-321`), `facts.last_report` re-based on `typed(text, None)` (`asf/briefs/facts.py:134-149`), and `tests/test_report.py` rewritten with the three PD12 fixtures converted (`tests/test_workers.py:869`, `tests/test_lifecycle.py:204`, `tests/test_tick_steps.py:261-266`, all now call `report.render(...)`) |
| acceptance tests byte-identical to the plan's | pass | Every test named by Task 1's Acceptance (`docs/plans/f-0025.md:133-185`) is present under the matching class in `tests/test_report.py`: `SchemaTest` (`:24-45`), `ParseTest` (`:48-121`), `RenderTest` (`:124-141`), `ContractTest` (`:144-163`), `ReadersTest` (`:166-198`) — each with the described scenario (e.g. `test_the_json_wins_over_the_prose` at `:169-172`, `test_the_prose_reader_is_gone` at `:187-189` asserting `parse`/`unpushed`/`NO_RE` are gone). The diff adds extra subtests beyond the plan's list (`RulingFieldsTest`, `test_a_bad_type_is_refused`, etc.) but weakens none of the named ones. |
| those tests were run and are green | fail | `python3 -c "import asf.workers.report"` on the branch as it stands (rebased onto current `origin/main`, i.e. `bdd0fb767`) raises `SyntaxError: invalid decimal literal` at `asf/workers/report.py:392` — the rebase left unresolved conflict markers (`<<<<<<< HEAD` at `:383`, `=======` at `:390`, `>>>>>>>` at `:392`) inside `footprint_claim`. No test in `tests.test_report` (or anything importing `asf.workers.report`) can even collect. |
| the Gate commands are green | fail | Same import failure blocks every command in Task 1's Gate (`python3 -m unittest tests.test_report -v`, the four-module run, `discover`, both `tools/check_*.sh` — the last two also import the tree). Even past the syntax conflict, `tests/test_report.py:6,24` (`from asf.briefs.build import KINDS`; `test_every_brief_kind_has_a_schema`: `sorted(report.KIND_FIELDS) == sorted(KINDS)`) would still fail: `asf/briefs/build.py:47-48` on `origin/main` (`bdd0fb767`) lists **14** kinds (`spec-plan` and `direct` landed on trunk after this branch's base commit `ad7448834`, whose `build.KINDS` still had 12 — confirmed via `git show ad7448834:asf/briefs/build.py`), while `KIND_FIELDS` in this diff (`asf/workers/report.py:47-105`) tables only the original **12**. |
| no secret value printed, no background process, no skipped check | pass | No secret literals or credentials appear in the diff; no background process is started by the changed code; the only checks this review could not complete are the two rows above, and their evidence is the reason, not a skip |

verdict: changes requested

## C — must fix before landing

- `asf/workers/report.py:383-392` — resolve the branch's merge conflict with `origin/main`
  instead of leaving `<<<<<<<`/`=======`/`>>>>>>>` markers in `footprint_claim`. `origin/main`
  added a T-0349 early return (`status = (rep.get('status') or '')...`,
  `if status == 'done' and rep.get('pushed') and not unpushed(rep): return None, []`) after this
  branch's base, built on `parse`/`unpushed` — both deleted here (D11). The resolution has to
  retype that clause onto `_prose`/the typed report the same way the rest of `footprint_claim`
  was retyped, not just take one side.
- `asf/workers/report.py:47-105` (`KIND_FIELDS`) — add a schema entry for `spec-plan` and for
  `direct`, the two kinds `asf/briefs/build.py:47-48` gained on `origin/main` since the spec was
  written against `dfe10fe`. Without them, `tests/test_report.py:24`
  (`test_every_brief_kind_has_a_schema`) fails its own
  `sorted(report.KIND_FIELDS) == sorted(KINDS)` the moment the branch sits on current trunk —
  this is not future drift, it is already true of `origin/main` today.
- Once both are fixed, run `tests.test_report` and the rest of Task 1's Gate for real and paste
  the last lines in the report — this round could not confirm green because the branch would not
  even import.

## I — worth doing, not blocking

- `asf/briefs/facts.py:141-149` (`last_report`) — the old prose carried `pushed: yes abc1234`
  (sha inline); the typed read only ever contributes the bare `pushed: yes/no/rebased` since
  `sha` is a separate `COMMON` field not in `REPORT_FIELDS`. `tests/test_brief_facts.py:191-199`
  was updated to match, so nothing is broken, but a brief now carries slightly less information
  than before about *which* sha last landed. Adding `sha` to `REPORT_FIELDS` would restore it.
