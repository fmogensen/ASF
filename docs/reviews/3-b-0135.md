# Review 3 — B-0135

No writer's report file is on record for this session either; the commit message on `a5032016761`
is the only report. Neither `docs/specs/b-0135.md` nor `docs/plans/b-0135.md` exists (still — `ls
docs/specs docs/plans | grep -i 0135` → only the two prior review files), so, as in rounds 1 and
2, the card's own Description/Want and the carried C's/I's from those rounds are what the diff is
judged against.

Round 3 exists because round 2's approval (`docs/reviews/2-b-0135.md`, on `9e6bdfe7c`) predates
the head: one commit landed after it, `a5032016761` — "install.sh's lock comment names the clocks
it actually covers" — which the commit message says answers round 1 and round 2's carried I2
(`tools/install.sh:40`'s comment overstated "the same lock a running tick holds for its whole run"
as universal). `git show --stat a5032016761` → `tools/install.sh | 6 ++++--`, `1 file changed, 4
insertions(+), 2 deletions(-)` — that one comment, nothing else.

Reviewed `fix/B-0135` at `a5032016761` against `origin/main` (`6e5c53a01`): `git diff --stat
6e5c53a01..HEAD` → `6 files changed, 313 insertions(+), 23 deletions(-)` across
`tools/install.sh`, `asf/tick/tick.py`, `tests/test_install.py`, `docs/guide/upgrading.md`, and
`docs/reviews/1-b-0135.md` / `docs/reviews/2-b-0135.md` (both rounds' own reports, carried on the
branch, not new work this round).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card still declares no `writes:` footprint. Round 3's own commit touches only `tools/install.sh` (`git show --stat a5032016761` above), the same file round 1's I2 and round 2's I2 named — nothing outside the install/tick/docs/tests/review footprint rounds 1-2 already approved. |
| every Step of the Task is implemented | pass | No plan exists, so judged against the card's Want, both parts of which rounds 1-2 already confirmed delivered (the lock held across the swap, `tools/install.sh:47-84`; every `asf` step imported up front under that same lock, `asf/tick/tick.py:423-428` — both diffed against `6e5c53a01` above and unchanged since round 2, `git diff 6e5c53a01..HEAD -- asf/tick/tick.py` shows only the 6-line upfront-import block). Round 3's own commit closes the one thing rounds 1-2 flagged as still wrong (carried I2): `tools/install.sh:44-48` now reads "the same lock **a clock with an `asf` step** holds for its whole run (`asf.tick.tick.lock_path`, `asf.tick.tick.Locks.held` — a command-only clock takes it only briefly, around each record part, and is not the case this fix protects)" — checked against `asf/tick/tick.py:290-294`, `Locks.__init__`'s own docstring: "A clock with an `asf` step holds the product lock throughout (`held`). A clock of command steps only (`held` false) ... takes the product lock briefly, waiting, around each record part" — the comment now names exactly the case the code covers, word for word the distinction the docstring draws. |
| acceptance tests byte-identical to the plan's | pass | No plan exists to diff against; the card's own `## Acceptance` is still empty. Its prose names one test, "install waits on a held tick lock" — present, unchanged since round 2, as `tests/test_install.py:1635` `test_install_waits_on_a_held_tick_lock`, alongside `tests/test_install.py:1675` `test_a_stuck_tick_lock_times_out` and `tests/test_install.py:1699` `test_the_lock_is_held_across_the_install_itself_not_just_before_it`. Round 3's commit does not touch `tests/test_install.py` (absent from `git show --stat a5032016761` above). |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_install` → `Ran 114 tests in 41.900s` / `OK`, all three lock tests listed `ok`. `python3 -m unittest tests.test_tick -v` → `Ran 59 tests in 64.863s` / `OK` (the upfront-import site, `_run_steps`, is covered here — `TickLockTests` all green). |
| the Gate commands are green | pass | `python3 tools/run_tests.py` → `Ran 3758 tests in 707.256s (109 module(s), 8 at a time)` / `OK (skipped=14)` — clean, unlike rounds 1-2 which both recorded (and verified as environmental) a `test_watch` date-rollover flake and, in round 2, a `test_tick_steps` `KeyError`; re-ran both in isolation this round to confirm: `python3 -m unittest tests.test_watch tests.test_tick_steps` → `Ran 100 tests in 84.534s` / `OK`, both now clean on this head. `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"), the same precedent every prior round records; substituted `python3 -m unittest tests.test_conventions` → `Ran 36 tests in 86.966s` / `OK`. `python3 -m asf.cli plugin check` → `plugin: up to date with the CLI`. `python3 -m asf.cli check` still not applicable in this worktree (`ls -d tasks stories epics bugs features decisions index.json` → every one "No such file or directory"). |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff: round 3's whole change is four lines of comment prose naming an existing public class attribute (`asf.tick.tick.Locks.held`) and a file path already in the codebase — no token, credential, or path outside the repo. No background process: every command in this review ran in the foreground to completion (the 707s full suite, the 87s `test_conventions`, the 42s `test_install`, the 65s `test_tick`). One check did not run literally (`bash tools/check_conventions.sh`, sandbox-refused); its equivalent unittest ran green in its place, as in every prior round. |

verdict: approved

## C

None.

## I

**I1 (carried, not new) — still no `docs/specs/b-0135.md` or `docs/plans/b-0135.md`.** Rounds 1
and 2 already recorded this; it still holds. Three rows above still have only the card's own prose
to check the diff and its tests against.

**I2 (resolved by this round's commit, not carried) — the "whole run" comment rounds 1-2 flagged is
now scoped to the clock kind it actually describes.** No further action.

**I3 (carried, not new) — `tests/test_install.py`: the stray extra blank line before `if __name__
== '__main__':` round 1 first noted (two blank lines instead of one) is still there** (confirmed at
the file's tail this round) — cosmetic, `check_generic.sh` still passes clean, round 3's commit does
not touch this file.

**I4 — `tools/install.sh:44-48`'s new comment sentence is harder to parse than it needs to be.** The
parenthetical naming `Locks.held` and the command-only-clock exception now sits in the middle of
one sentence that also has to finish "held across the `pipx install --force` call itself, not just
checked beforehand" — a reader has to hold both clauses open across the parenthetical. Splitting it
into two sentences (name the lock and its scope first, then say it's held across the swap) would
read easier; not blocking, the meaning is unambiguous as written and verified accurate against
`asf/tick/tick.py:290-294` above.

### Missed in round 1

None found — round 1's table and both its C's, plus round 2's table, account for everything visible
in the diffs those rounds reviewed; this round's commit is new content (the comment's exact wording)
addressing a carried I, not something either prior round should have caught differently.
