# Review — B-0115, round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `writes:` is declared none; the diff against `origin/main` is `asf/scheduler.py` and `tests/test_scheduler.py` only — the fix and its test |
| every Step of the Task is implemented | fail | no plan `docs/plans/b-0115.md` and no spec `docs/specs/b-0115.md` exist on the branch or `origin/main`, so no Step can be checked. The code does what the commit title says: `asf/scheduler.py:363-373` — `RunAtLoad` moved out of the shared `plist` dict; a calendar (`clock.calendar`) job gets only `StartCalendarInterval` (`:370`), an interval job gets `RunAtLoad: True` plus `StartInterval` (`:372-373`). `RunAtLoad` is set nowhere else (`grep -rn RunAtLoad` — only `asf/scheduler.py:372` and the two test files) |
| acceptance tests byte-identical to the plan's | fail | no plan exists, and the card's own Acceptance section is empty (`- [ ]`), so there is nothing to compare against. Tests added: `tests/test_scheduler.py:283-292` — `test_daily_calendar_job_does_not_run_at_load` (asserts `RunAtLoad` absent for the `DAILY` clock, `Clock('daily', ..., None, {'Hour': 6, 'Minute': 50})`, matching the card's 06:50 example exactly) and `test_interval_job_still_runs_at_load` (asserts `RunAtLoad` true for the `RECORD` interval clock) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_scheduler` → `Ran 36 tests in 1.134s` / `OK` (includes both new tests and the untouched golden-plist test at `tests/test_scheduler.py:242`, which still expects `RunAtLoad: True` for an interval job) |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `FAILED (errors=5, failures=35, skipped=14)`, red: `test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane` — the same six modules the B-0090 round-1 review recorded as red on a clean `origin/main` (environmental, not this diff; none of the six touch `asf/scheduler.py` or `tests/test_scheduler.py`, and `test_scheduler` itself is not in the red list). `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` → not run: the sandbox refused it (needs approval), same precedent as B-0090 |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff (a boolean moved between two dict branches, a two-test addition, nothing exfiltrated); no background process left running by this review (the gate run completed before this review closed). Skipped: `check_conventions.sh` (sandbox approval, same as B-0090). No writer's report was found on the branch or in reachable state, so it could not be read |

verdict: changes requested

## C list

- `docs/plans/b-0115.md` (missing): there is no plan or spec, so the "every Step" and "byte-identical tests" rows cannot pass, and the card's Acceptance list is still empty. Fix: add the plan naming the Task ("a calendar clock does not carry `RunAtLoad`") and its Steps (move `RunAtLoad` out of the shared `plist` dict in `asf/scheduler.py:render()`; set it only on the interval branch), and give the Acceptance as the two tests now at `tests/test_scheduler.py:283-292`.

## I list

- The fix only changes the `launchd` job kind. `cron` (`asf/scheduler.py:374+`) has no `RunAtLoad`-equivalent problem — a crontab line never fires at install — so no companion fix is needed there, but the plan (once written) should say this explicitly so a future reader does not wonder why `cron` was left untouched.
- Confirm the six red modules (`test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane`) stay red on a clean `origin/main` in this environment before relying on that precedent again; not re-verified against a fresh `origin/main` checkout in this round, only cross-checked against the B-0090 round-1 review's record of the same six.
