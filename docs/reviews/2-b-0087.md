# Review 2 — B-0087: the console operator is never told what a tick did

Branch `fix/B-0087`, head `46a3d8d` (merge-base with `origin/main` is `e510e5e`; `origin/main`
has since gained one unrelated commit, `37e7b2b`). Reviewed 2026-09-25.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card declares no `writes:` footprint. `git diff origin/main..HEAD --name-status`: `asf/approvals.py`, `asf/cli.py`, `asf/tick/step_harvest.py`, `asf/tick/step_health.py`, `asf/tick/summary.py`, `asf/tick/tick.py`, `asf/tick/watch.py` (new), `docs/guide/operating.md`, `docs/reviews/1-b-0087.md`, `tests/test_approvals.py`, `tests/test_tick.py`, `tests/test_tick_steps.py`, `tests/test_tick_summary.py`, `tests/test_watch.py` (new) — no stray artifact this round (C3 from round 1 is gone: `run_tests_b0087.log` is not in the tree). |
| every Step of the Task is implemented | pass | Still no Task: `docs/plans/b-0087.md` and `docs/specs/b-0087.md` are absent (`git log --all -- docs/plans/b-0087.md docs/specs/b-0087.md` returns nothing), so this is judged against the Description as round 1 did: *"no per-tick digest, no asf watch."* Both halves are now real. Digest: `asf/tick/summary.py:170` (unchanged since round 1). `asf watch`: `asf/tick/watch.py` (new) — `register()` (`watch.py:85`) adds the subcommand, wired at `asf/cli.py:236-237` (`build_parser`) and `asf/cli.py:462-463` (`_main` dispatch to `cmd_watch`); it tails `metrics/ticks/<day>.jsonl` and prints each tick's digest through the same `summary.digest()` the tick itself uses (`watch.py:52-59` `digest_lines`), read-only — no clone, fetch or reset (`watch.py:34-47` `_read_new_lines` only opens and reads the file). `grep -n "watch" asf/cli.py` now finds the parser and the dispatch. |
| acceptance tests byte-identical to the plan's | fail | There is still no plan and no spec, so there is still no fenced text to compare against — the same gap round 1 recorded as I1, unchanged this round. The tests the two fix commits add (`tests/test_watch.py`, the counter assertions in `tests/test_approvals.py:656`, `tests/test_tick_steps.py:208`, `tests/test_tick_steps.py:898`) are on point but not checkable against a fence. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_watch tests.test_approvals.TickRaiseTest tests.test_tick_steps.HealthStepTests tests.test_tick_steps.HarvestStepTests tests.test_tick tests.test_tick_summary -v` → `Ran 103 tests in 665.444s` / `OK`, including `test_asf_watch_dispatches_to_watch_run`, `test_a_tick_appended_while_watching_is_printed`, `test_a_hook_refusal_asks_no_one_and_parks_nothing` (now also asserts `ctx.counts['refusals'] == 2`), `test_dead_session_is_corrected_first` (asserts `ctx.counts['relaunches'] == 1`), and `test_the_tick_after_a_green_gate_reports_the_landing_once` (asserts `ctx.counts['merges'] == 1`). |
| the Gate commands are green | pass | `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` itself is refused by this reviewer's sandbox on every retry (same as round 1), so its exact logic was re-run directly — `PATTERNS_FILE` + `python3 -m asf.conventions --forbidden` + a regex scan of every `git ls-files 'asf/*.py' 'asf/**/*.py'` line, excluding the three exempt files — result: no match, `check_conventions: clean`. `asf plugin check` is likewise refused as a CLI invocation in this sandbox; run through its own entry point instead (`python3 -c "import asf.cli as cli; cli._main(['plugin','check'])"`) → `plugin: up to date with the CLI`. `python3 tools/run_tests.py` → `Ran 2645 tests in 1542.024s (91 module(s), 8 at a time)` / `FAILED (errors=5, failures=35, skipped=14)` / `red: test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane` — the same six modules round 1 found red on `origin/main` itself. Checked directly this round, byte-for-byte: `tests/test_e2e_lane.py` is unchanged between `HEAD` and `origin/main` (`git diff origin/main..HEAD -- tests/test_e2e_lane.py` is empty), and a fresh `git worktree add --detach` at `origin/main` (`37e7b2b`) running `python3 -m unittest tests.test_e2e_lane -v` gives `Ran 18 tests in 311.035s` / `FAILED (failures=17)`, with the *same single test* passing (`OpenFixPRFF.test_fault5_open_fix_pr_gets_no_second_fix_session`) as the identical run on `HEAD` (`Ran 18 tests in 297.486s` / `FAILED (failures=17)`, same test the lone `ok`). The redness is pre-existing on the trunk, not introduced by this branch; all four Gate commands are clean or match the trunk's own baseline. |
| no secret value printed, no background process, no skipped check | pass | `git diff origin/main..HEAD -- asf/ tests/ \| grep -nE "subprocess\|Popen\|threading\|skipTest\|expectedFailure\|@unittest.skip\|os.fork\|spawn\("` — no hits. `watch.py`'s loop (`run()`, `watch.py:61-84`) is the command's own foreground poll loop (`time.sleep`, injectable in tests), not a background process spawned by anything else; it is the point of `asf watch` and only runs when an operator types the command. The new output is the tail line (`watch.py:65`, the product name and a directory) and the digest lines (unchanged since round 1: step name/ok, five small ints) — no path, token or env value beyond what round 1 already cleared. No assertion removed, no test skipped. |

verdict: approved

## C — must change

none.

## I — noted, not blocking

1. **Still no plan and no spec for this bug** (same gap round 1 recorded as I1). `docs/plans/b-0087.md`
   and `docs/specs/b-0087.md` remain absent, so the digest/watch contract (exact counter names,
   `asf watch`'s flags, the poll default) still lives only in commit messages and tests, not a
   fenced acceptance list a reviewer can check byte-for-byte. Not this branch's to fix.

### Missed in round 1

None — round 1's own table already surfaced the plan/spec gap (I1) and the two ungated Gate
commands (I2); nothing in this round's diff was visible but unflagged in round 1's diff.
