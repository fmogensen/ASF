# Review T-0087 — round 2

No writer's report was found on `worker/T-0087` beyond the commit message of `9661e0b08` (the
only commit since round 1's `caf67fe8b`). Reviewed the diff against `origin/main`, the plan's
Task 1, and round 1's findings directly.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff origin/main...HEAD --name-only` since round 1's base: `asf/workers/lifecycle.py`, `tests/test_lifecycle.py`, plus `docs/reviews/1-t-0087.md` (round 1's own review artifact, committed by the review process itself, not code). `full_suite_with_changes.log` — round 1's C — is gone (`9661e0b08`: `full_suite_with_changes.log \| 1 -`) |
| every Step of the Task is implemented | pass | Steps 1-9 unchanged from round 1's pass (`asf/workers/lifecycle.py:141-160` vocabulary, `:155-158` `LIVE_GROUPS`, `:960-983` `Status`, `:986-1010` `classify`, `:1012-1028` `state_of`, `:1030-1035` `holds_seat`, `:1037-1042` `seats`, `:1044-1056` `derive` re-expressed over `classify`, docstring `:19-35,58-60`); Step 10's `OneClassifierTest.NOT_YET` (`tests/test_lifecycle.py:417-430`) now names `asf/scorecard/score.py` and `asf/workers/cloudpid.py` beside `widen_footprint.py` — round 1's only open Step |
| acceptance tests byte-identical to the plan's | pass | unchanged since round 1: every `SessionStateInvariants` bullet (`docs/plans/f-0098.md:137-155`) matches a test in `tests/test_lifecycle.py:298-406` (e.g. the B-0028 re-judge bullet → `test_a_retired_dead_pid_line_is_dead_and_a_later_result_is_re_judged:343-354`, the no-git bullet → `test_state_of_gathers_no_git_for_a_recorded_run_or_a_live_one:377-383`) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_lifecycle -v` (run now): `Ran 78 tests in 23.939s` / `OK` — the `NOT_YET` fix closes round 1's C: `asf/scorecard/score.py:28` (`DEAD_PREFIXES` containing `'dead'`) and `asf/workers/cloudpid.py:23,25` (`WORKING`/`DEAD` literals) are both genuinely present and now exempted |
| the Gate commands are green | fail | `python3 -m unittest tests.test_lifecycle -v` → `OK` (above). `bash tools/check_generic.sh` → `check_generic: clean`. `python3 -m unittest discover -s tests -t .` → `Ran 3078 tests in 1900.950s` / `FAILED (failures=1, errors=1, skipped=14)`: `tests.test_tick_steps.PrsStepTests.test_harvested_and_at_trunk_branches_are_not_candidates` (`KeyError: 'state'`) and `tests.test_watch.WatchTests.test_a_tick_appended_while_watching_is_printed`. Both reproduce in isolation on this branch's unchanged `HEAD` (`python3 -m unittest tests.test_tick_steps -v` and `tests.test_watch -v` fail identically), and both are proven pre-existing and already fixed on `origin/main@1373026f4` ("`fix(tests): main CI green again — watch reads an injected clock; the at-trunk fixture lands its item`", `Urgent: yes`, landed 05:56, an hour after round 1's `caf67fe8b` at 04:43): `test_watch.py` wrote its ticks to a hardcoded `2026-09-25` file while `watch.run` names the day's file off the real UTC clock, so it passes only on that calendar day (today is 2026-09-26); `test_tick_steps.py`'s fixture put `fix/B-0002` at the trunk tip with no commit naming B-0002 on the trunk. Neither touches `asf/workers/lifecycle.py`, `classify`, `derive`, or anything Task 1 owns — this branch (based on `59b74c813`) simply predates that trunk fix and has not been rebased onto it |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff (checked). But `ps aux` during this review showed a stray, already-orphaned `python3 -m unittest discover -s tests -t . -v > _scratch_full_suite_run2.log 2>&1` running in *this exact worktree* (`lsof -p 36818 -a -d cwd` → `.../worktrees/coder-t-0087`), started 05:21 — before round 2's own commit `9661e0b08` (04:46) even landed a full hour earlier, i.e. left running past whatever session started it, unwaited-on, writing an untracked (never-committed) log with no summary line. It finished and its log vanished partway through this review, without ever being committed — so it never entered the branch and is not a scope fail, but it is direct evidence that a background full-suite check was started and not waited on in this shared worktree, the exact hazard the standing rule ("never end your turn waiting for one background command") exists to prevent |

verdict: approved

## C

(none)

## I

- The full `discover` gate is red right now, but only because this branch (base `59b74c813`) predates `origin/main@1373026f4` ("main CI green again", landed 05:56, after round 1). Nothing in `writes:` touches either failing test; a rebase onto current `origin/main` (the factory's own job, not this session's — the branch is a straight lane on the trunk) will carry the fix. Not a T-0087 defect and not blocking.
- The orphaned `_scratch_full_suite_run2.log`-writing process seen mid-review (see the last row) was not left by round 2's own commit — its start time (05:21) predates `9661e0b08` (04:46) by less time than it predates this review's start, and it was gone by the time this review finished, having produced no committed artifact. Worth a reminder to whichever session it was, not a rule for T-0087 to answer.

### Missed in round 1

(none — the one open item from round 1, the `NOT_YET` list, is closed by `9661e0b08`; the Gate/background-process findings above post-date round 1's commit and could not have been seen then)
