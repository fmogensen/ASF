# Review 2 — B-0109: full test suites run locally and exhaust the host

Branch `fix/B-0109`, head `36d7992`, three commits on `origin/main` (`37c6b3f`).
Round 1 reviewed `3a9cfc6`; the lane has since been rebased, so that commit is now `9c83c1a` and
the delta this round adds is `36d7992` — review 1's two C items and nothing else.
Reviewed 2026-09-24 on a 10-core / 16 GB host (load 2.3 at the start: the guard this item adds
would not have held anything here).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card declares no `writes:` footprint, so there is no fence to cross — the row passes on judgement, not on a check (carried: round 1 I2). `git diff --name-status origin/main..36d7992` is six files: `asf/harvest/lane.py`, `docs/config.example.yaml`, `docs/guide/product-config.md`, `tests/test_lane.py` (the guard, its two documented homes, its test), `docs/reviews/1-b-0109.md` (round 1's own verdict) and `tests/test_harvest.py` (this round's delta). `36d7992` alone touches two: one comment in `asf/harvest/lane.py:847` and the new fixture and test in `tests/test_harvest.py`. Nothing outside the guard and its tests. |
| every Step of the Task is implemented | pass | There is no Task: `docs/plans/b-0109.md` is absent, as in round 1 — judged against the card's Description and against review 1's two C items, which is what `36d7992` claims to deliver. **C1** closed: `lane.py:847`'s comment now reads `# unreadable: the default guards, not none`, which is what `host.guards_from_config({})` actually does (`asf/workers/host.py:27-39` returns the 2.0/85 defaults for an absent key), and `36d7992`'s message strikes the false sentence from `9c83c1a` in prose rather than amending a published commit. **C2** closed: `tests/test_harvest.py:2107` covers the `how == 'ci'` passthrough — see the mutation runs in the next row. Also confirmed for the Description's "nobody blamed" claim: a branch held at `HOST_PRESSURE` cannot escalate — the STALE rule is fenced behind `if s == PUSHED:` (`lane.py:431-435`), `landing_wait_s` only fires when `rec['state'] == WAITING_CI` (`lane.py:1515`) and host pressure sets plain `WAITING`, and `GATE_SLOW` `timeouts` only increments when a gate actually ran (`lane.py:1101-1103`), which a held set never does. |
| acceptance tests byte-identical to the plan's | fail | Unchanged from round 1 and not the writer's to fix from inside this branch: `docs/plans/b-0109.md` and `docs/specs/b-0109.md` are both absent and the card's Acceptance is a single empty checkbox, so there is no fenced text to compare against. The two tests are real and are the right tests — `tests/test_lane.py:435` and `tests/test_harvest.py:2107` — they are simply not checkable against a fence. See I1; this is the only failing row and it is a gap in the record, not in the code. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_harvest.ProductHarvestTests.test_host_pressure_holds_the_local_gate_and_lets_the_ci_prs_through tests.test_lane.LaneRepo.test_host_pressure_holds_the_gate_and_starts_no_suite -v` → `Ran 2 tests in 6.948s` / `OK`. Both pin real behaviour, checked by mutating `held_by_host` twice and restoring: (a) `return [f for f in ready if f.get('how') == 'ci']` → `return []` makes the new test fail `AssertionError: {'fix/B-0001': 'waiting'} != {'plan/F-0001': 'landed', 'fix/B-0001': 'waiting'}` — `Ran 1 test in 2.435s` / `FAILED (failures=1)`, so the ci passthrough round 1 found untested is now genuinely pinned; (b) an early `return ready` (the trunk's behaviour) makes it fail `AssertionError: a suite started under pressure` — `Ran 1 test in 3.075s` / `FAILED (failures=1)`, which also proves the patched `product_gate`'s error is not swallowed somewhere in the harvest. The test's asymmetry is the real one: `plan/F-0001` touches `docs/plans/f-0001.md` → class `docs` → policy `wait` → required `ci` passed → `how == 'ci'`; `fix/B-0001` → class `code` → `local-gate` → `return 'gate'` at `lane.py:1505-1506`. |
| the Gate commands are green | pass | CI's gate is `.github/workflows` steps `check_conventions`, `check_generic`, `asf plugin check`, `run_tests.py`. `python3 -m asf.cli plugin check` → `plugin: up to date with the CLI` (rc 0). `python3 tools/run_tests.py --shards 4` → `Ran 2610 tests in 578.452s (89 module(s), 4 at a time)` / `FAILED (errors=5, failures=35, skipped=14)` / `red: test_doctor, test_install, test_sample_product, test_dry_run, test_gitfixture, test_e2e_lane`. Those six run against a detached worktree at the current `origin/main` (`37c6b3f`) give the **identical** `FAILED (errors=5, failures=35, skipped=1)` and the same six names — `Ran 178 tests in 183.772s (6 module(s), 4 at a time)` — so the branch introduces no red, re-verified against the trunk the lane was rebased onto rather than round 1's older one. The two `bash` gate scripts could not be launched in this session (the harness refused `bash tools/check_conventions.sh` and `bash tools/check_generic.sh` as needing approval), so their substance was run instead: `check_generic.sh` is a wrapper that delegates to one scanner, and `python3 -m asf.redact --tree --names tools/forbidden-names.txt` → `redact: clean` (rc 0) is that scanner; `check_conventions.sh`'s loop was replicated in Python over both of its real pattern sources (`tools/forbidden-conventions.txt` plus `python3 -m asf.conventions --forbidden`) and its real file list (`git ls-files 'asf/*.py' 'asf/**/*.py'`, minus the three documented exemptions) → `patterns: 28` / `scanned 118 files -> clean`. See I4 — the operator should run the two scripts verbatim. `python3 -m asf.cli check` → rc 1, 262 lines, last line `stories/S-0004.md:16: warning: no closing rule sees this item (type story, no Task, no matrix row, parent F-0014 is New) — it can never close; see docs/specs/f-0080.md §2.1` — the same rc and the same 262 as round 1 measured, against a record this diff does not touch: pre-existing, see I6. |
| no secret value printed, no background process, no skipped check | pass | The delta adds no new output at all: the only print in the feature remains `lane.out(f'harvest: held: {why} — …')` (`lane.py:852`), and `why` comes from `host.judge` (`asf/workers/host.py:42-58`), which formats load, core count and swap percent and nothing else — no token, path or credential. The new fixture prints nothing; it appends to a `calls` list. `fake_gh_prs` does call `sh(['git', …])` for fetch/push/delete (`tests/test_harvest.py:1552-1554`) — synchronous test-fixture git against the test's own throwaway origin, the same thing the neighbouring `fake_gh` does at `:1512-1513`, waited for in-process; no `Popen`, no `&`, no thread, no `spawn`, nothing detached anywhere in the diff. The delta adds no `skipTest`, no `@unittest.skip` and no `expectedFailure`, and removes no assertion (`36d7992` is `+65 −1`, the one deletion being the replaced comment). Every command in the rows above was run in the foreground and waited for; the two mutations were reverted and `git status` is clean. |

verdict: approved

## C — must change

None. Review 1's two C items are both delivered, and both are mutation-verified above rather than
taken on the commit message's word. The one remaining `fail` row is the absent plan and spec,
which is a hole in the record and cannot be filled from inside this branch — round 1 already
ruled it I1, not a C, and nothing in the delta changed that.

## I — noted, not blocking

1. **Still no plan and no spec for an S1 bug.** `docs/plans/b-0109.md` and `docs/specs/b-0109.md`
   do not exist and the card's Acceptance is one empty checkbox. That is the whole of row 3's
   `fail`. The guard's contract lives in `held_by_host`'s docstring, two commit messages and now
   two tests — good coverage, but a later round still has no fence to check a changed test
   against, and the card cannot be closed against an empty Acceptance. Carried from round 1 I1.

2. **`tests/test_harvest.py:1524` — `fake_gh_prs`'s `required=` parameter is dead.** It is read at
   `:1535-1537`, but the fixture's only caller (`:2108`) omits it, so the branch-protection arm
   is never exercised and every run takes the `HTTP 404` path. It is copied from `fake_gh`, where
   six tests do pass it. Either drop the parameter until a test needs it, or keep it and say in
   the docstring that it mirrors `fake_gh` for the next caller.

3. **`tests/test_harvest.py:1524-1565` duplicates `fake_gh` (`:1472-1521`).** Roughly forty lines
   of near-identical `gh` dispatch. The writer's reason for a second fixture is sound — `fake_gh`
   keys its one PR off the worker's current branch and cannot hold two — but the generalisation
   runs the other way: `fake_gh` is `fake_gh_prs` with a single entry keyed off `HEAD` plus the
   `queue`/`auto` flags, so `fake_gh` could delegate to it and the `pr checks` / `pr merge` /
   `pr view` arms would exist once. Worth doing the next time either is touched, not now.

4. **The two `bash` gate scripts were not run verbatim in this session.** The harness refused to
   launch `tools/check_conventions.sh` and `tools/check_generic.sh`; both were covered by their
   real delegate or a faithful replication, and both are clean (see the Gate row). The residual
   risk is small and confined to POSIX-ERE-versus-Python regex semantics across 28 simple
   patterns, but it is not the command's own last line. See the `NEEDS OPERATOR` line in the
   session report.

### Still open from round 1 — unchanged by this delta

5. **A second full-suite path is still unguarded** (round 1 I3). `run_gate`
   (`asf/harvest/harvest.py:400`) runs `conv.test_command` in a temp clone and is reached from
   `run_product_harvest` (`:829`) and from `asf harvest --repo` (`:904`) without passing through
   `held_by_host`. The incident's second suite was the lane's, which this branch closes, so it
   does not block — but the card's title says *full test suites*, and a record product with a
   `test_command` can still start one under pressure. This is the natural follow-up card.

6. **Two asymmetries with the wave step** (round 1 I4). `step_wave.host_hold`
   (`asf/tick/step_wave.py:312`) lets a `ConfigError` propagate so the tick says so, where
   `held_by_host` swallows it to the defaults; and the wave emits a `host_pressure` metrics event
   (`:373`) where the harvest emits none, so a held landing leaves no trace in `metrics/events`.
   The second one matters more than it did before this branch: because nothing escalates a
   host-pressure `WAITING` (verified in row 2), a host that stays loaded holds every landing
   indefinitely and the only signal is one `harvest: held: …` line per tick. An event would make
   that visible in the status and doctor rows.

7. **The suite is red on this host, on the trunk too** (round 1 I5). 40 tests across
   `test_doctor`, `test_install`, `test_sample_product`, `test_dry_run`, `test_gitfixture` and
   `test_e2e_lane`, with identical `errors=5, failures=35` on `origin/main` at `37c6b3f`. Not this
   branch's, and not chased here — but the landing gate will hold every branch as `trunk-red`
   until someone does, which is its own S-level card.

8. **`asf check` is rc 1 against the operator's record** (round 1 I6). 262 lines, the same count
   round 1 measured. Record hygiene, independent of any asf source branch.

### Missed in round 1

Nothing material. Round 1 ran the whole table and its two C items were the two real defects in
that diff; everything above that predates `36d7992` is an item round 1 itself recorded, carried
forward here because it is still open, not because it was missed. The one thing this round adds to
round 1's reading of the *same* code is the escalation check in row 2 — that `HOST_PRESSURE` has
no path to STALE, to a `landing_wait` timeout or to a `gate-slow` increment — which confirms round
1's "nobody blamed" pass rather than contradicting it.
