# Review — B-0111, round 3

Branch `fix/B-0111`, head `035ae99`, one commit past round 2's `ac7ecef` review: `035ae99
wip(fix/B-0111): the session ended with this uncommitted — committed by the factory (B-0094)` —
the writer's answer to round 2's C1, captured as a factory auto-wip commit (the session ended
without committing it itself). No writer's report file exists on the branch or in this worktree
(same as rounds 1 and 2); judged against the diff and the commit's own new code comment.
`origin/main` gained one commit since this branch's base (`b39b10a`, an unrelated briefs fix); it
does not touch `asf/groom/inbox.py` or `tests/test_inbox_shape.py` and is not part of this diff —
the diff judged here is `34afef2..035ae99` (this branch's actual base, not the now-moved
`origin/main`).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card declares no `writes:` footprint (unchanged since round 1), so there is no fence to cross. `git diff 34afef2..HEAD --stat` touches only `asf/groom/inbox.py`, `tests/test_inbox_shape.py`, `docs/reviews/1-b-0111.md`, `docs/reviews/2-b-0111.md`, and a 0-byte rename `run_tests_out.txt` → `.run_tests_gate.log` (I2, carried). |
| every Step of the Task is implemented | pass | No plan exists (`docs/plans/b-0111.md` still absent — I1, carried), so judged against round 2's C1. C1 is fixed: `_UNREAD_HEADER_KEYS = ('after',)` names the key explicitly instead of matching any `word:` shape (`asf/groom/inbox.py:17-18`), and a line matching it now always advances `body_start` (`asf/groom/inbox.py:88-89`, the one-line addition `body_start = i + 1` inside the `_UNKNOWN_HEADER_RE` branch) — so `after:` is dropped from the description whether or not a further confirmed header follows it, closing the exact gap round 2 found. Live repro against round 2's own C1 fixture (`text = "# Checkout fails\nparent: E-0001\nafter: T-0001\nThe plan needs this to land first.\n"`): `c.headers == {'parent': 'E-0001'}`, `c.description == 'The plan needs this to land first.'` — matches the wanted behavior. This is option (a) round 2's C1 itself proposed (an explicit registry of unread-but-known header keys), not a new approach the review has to re-derive trust in. |
| acceptance tests byte-identical to the plan's | fail | Still no plan and no spec (`docs/plans/b-0111.md`, `docs/specs/b-0111.md` both absent — unchanged since round 1), and the card's Acceptance is still a single empty checkbox. `tests/test_inbox_shape.py:428` (`test_b_0111_round_2_c1_a_trailing_unread_header_with_nothing_after_it_is_still_dropped`) is the right test for round 2's C1, but there is no fence to check it against. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_inbox_shape -v` → `Ran 47 tests in 8.366s` / `OK`, including the new `test_b_0111_round_2_c1_a_trailing_unread_header_with_nothing_after_it_is_still_dropped` and both prior rounds' regression tests, all `ok`. Every other suite touching groom/inbox/backlog (`tests.test_groom tests.test_groom_shape tests.test_groom_every_card_decided tests.test_groom_every_tick tests.test_groom_policy tests.test_groom_adjudicate_reach tests.test_groom_for_you tests.test_backlog`) → `Ran 237 tests in 37.837s` / `OK`. |
| the Gate commands are green | pass | `bash tools/check_generic.sh` → `check_generic: clean`. `tools/check_conventions.sh` itself was again blocked by the sandbox's own approval prompt (same limitation rounds 1 and 2 hit); replicated in Python against the same two pattern sources (`tools/forbidden-conventions.txt` + `python3 -m asf.conventions --forbidden`) over `asf/groom/inbox.py` (the only `asf/**/*.py` this diff touches) — no match. `python3 -m asf.cli check` → 262 lines, same pre-existing record-repo output (bare `D<n>` references, `writes:` overlaps, un-closeable stories) rounds 1 and 2 both recorded — byte-identical line count, none of it touching this diff's files. `python3 tools/run_tests.py --shards 4` (the declared full suite), blocked in both prior rounds by host load (27–50), **completed this round**: `uptime` showed load averages `5.30 9.70 15.21`, and the run finished `Ran 2834 tests in 273.898s (92 module(s), 4 at a time)` / `OK (skipped=14)` — the 14 skips are pre-existing (`grep` finds 7 `skipUnless`/`skip` sites across the tree, none in the files this diff touches), not something this diff introduced. Gate is green in full this round, closing rounds 1 and 2's I3. |
| no secret value printed, no background process, no skipped check | pass | `git diff ac7ecef..HEAD -- asf/groom/inbox.py tests/test_inbox_shape.py` greps clean for `print(`, `subprocess`, `Popen`, thread/spawn, `skipTest`/`expectedFailure`/`@unittest.skip`, secret/token/password. No assertion removed. `check_conventions.sh` was not run as a shell process (sandbox-blocked); its check was still performed, in Python, as recorded above — nothing was silently skipped. |

verdict: approved

## C — must change

(none)

## I — noted, not blocking

(carried from rounds 1–2, unresolved, not this round's to fix from inside this branch: I1 — no
plan and no spec for an S2 bug, the reason row 3 stays `fail`; I2 — the stray zero-byte file,
renamed `run_tests_out.txt` → `.run_tests_gate.log` by this round's own wip commit but still
present and still unreferenced, worth `git rm` rather than carrying forward again)

### Missed in round 1

(none — round 1's own table and C1 already covered everything visible in the diff at that time;
round 2's C1 was a regression round 1's own fix introduced, not something round 1 could have
caught in the head it reviewed)
