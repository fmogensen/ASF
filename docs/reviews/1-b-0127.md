# Review 1 — B-0127

Reviewed `fix/B-0127` at `58e53de` against `origin/main` (`382c387` — branch is exactly one
commit ahead, `git log --oneline origin/main..HEAD` → the single fix commit). No writer's report
file exists in the worktree or the record for this session; reviewed the commit message and diff
directly. `git diff --stat origin/main...HEAD` — three files, `asf/briefs/preamble.py`,
`asf/harvest/harvest.py`, `tests/test_briefs.py`.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | The card declares no `writes:` footprint ("(none declared)"), so there is nothing to judge the diff against. The three touched files — `asf/briefs/preamble.py`, `asf/harvest/harvest.py`, `tests/test_briefs.py` — are the natural footprint for a brief-text change gated by a harvest predicate, and no unrelated module, doc or record card is touched. Recorded `fail` only because a row with nothing to compare against carries no evidence; see I1. |
| every Step of the Task is implemented | pass | Neither `docs/plans/b-0127.md` nor `docs/specs/b-0127.md` exists (`ls docs/plans docs/specs \| grep -i 0127` → empty), so there is no Task with Steps; judged instead against the card's Description clause. Clause: "when a product's landing is gated by harvest's local full-suite gate, the brief tells workers to run only the targeted tests for the files they changed, and states that the harvest gate runs the full suite before landing." Delivered: `asf/harvest/harvest.py:675-682` adds `local_gate(product)` — true iff `landing(product) == LANDING_FF` and `product.conventions.test_command` is set, and false whenever `external_ci(product)` is true; `asf/briefs/preamble.py:101-107` adds `LOCAL_GATE_RULE` ("run only the targeted tests for the files you changed, then push: harvest's gate runs the full suite on the combined head before anything lands … it just runs once per landing, not once per session"); `asf/briefs/preamble.py:117-124`'s `ci_rules` returns `[LOCAL_GATE_RULE]` when `harvest.local_gate(product)` and neither rule when `external_ci` already covers it. Verified the underlying claim: `asf/harvest/lane.py:1445-1461` (`gate_groups`) builds one combined head from every stacked branch and calls `H.product_gate` exactly once per gate, and `asf/harvest/lane.py:564` only reaches that path in `mode == 'ff'`, i.e. `landing(product) != LANDING_PR` — so the rule is added exactly where the claim it makes is true. `external_ci` (`asf/harvest/lane.py:1300-1307`) is itself gated on `landing(product) == LANDING_PR`, which is mutually exclusive with `local_gate`'s own `landing == LANDING_FF` check, so the `external_ci(product)` guard inside `local_gate` (`harvest.py:679`) can never change the outcome — redundant, not incorrect; see I2. |
| acceptance tests byte-identical to the plan's | pass | No plan exists to be byte-identical to (`docs/plans/b-0127.md` absent), and the card's own Acceptance list is empty (`- [ ]`). The card's prose names the tests directly: "a local-gate product's brief carries the rule" → `tests/test_briefs.py:390` `test_local_gate_product_gets_the_run_once_rule`, asserting the rule text, `"harvest's gate"` and `"full suite"` are all in the built preamble for a fast-forward product with `test_command` set; "the harvest gate still runs the full suite" is an existing invariant of `product_gate`/`gate_groups`, untouched by this diff and still covered by the pre-existing harvest test suite. A companion test, `tests/test_briefs.py:406` `test_no_test_command_gets_no_local_gate_rule`, checks the negative case (fast-forward, no `test_command` → no rule). Both are new, in the diff, and match the card's own wording. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_briefs -v` → `Ran 86 tests in 1.078s` / `OK`, including `test_local_gate_product_gets_the_run_once_rule … ok` and `test_no_test_command_gets_no_local_gate_rule … ok`. |
| the Gate commands are green | pass | `python3 tools/run_tests.py` → `Ran 3259 tests in 739.752s (103 module(s), 8 at a time)` / `OK (skipped=14)`. `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `python3 -m asf.redact --unpublished` → `redact: clean` (rc 0). `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"), same refusal noted in round 2 of B-0132; `tests.test_conventions` (which exercises the same script) ran green standalone — `python3 -m unittest tests.test_conventions -v` → `Ran 30 tests in 38.782s` / `OK` — and inside the full suite above. |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff: it adds one module docstring constant, one predicate function, and test fixtures built from the module's own vocabulary — no token, credential or path outside the repo; `asf.redact --unpublished` over the branch's one commit is clean. No background process: every command above ran in the foreground to completion (`tools/run_tests.py` under a long timeout, 740s actual), nothing launched with `&` or `run_in_background`, and `git status --porcelain` is empty apart from this review file. The 14 skips are the suite's standing count, unchanged by this diff. One check did not run literally — `bash tools/check_conventions.sh`, covered by the green unittest that runs it; see the NEEDS OPERATOR line in the report. |

verdict: approved

## C

none.

## I

**I1 — neither `docs/specs/b-0127.md` nor `docs/plans/b-0127.md` exists, and the card declares no
`writes:`.** One row of this table had nothing to judge against and is recorded `fail` for that
reason alone, not for anything in the diff — the same gap I7 of `docs/reviews/1-b-0132.md` records
for that item. An S2 whose fix lands in one commit still wants a `writes:` line, if only so the
scope row can be answered.

**I2 — `asf/harvest/harvest.py:679`: the `external_ci(product)` guard inside `local_gate` is dead
weight.** `external_ci` (`asf/harvest/lane.py:1300-1307`) is only ever true when
`landing(product) == LANDING_PR`; `local_gate`'s own second line already requires
`landing(product) == LANDING_FF`. The two conditions can never both hold, so the guard never
changes `local_gate`'s return value for any product — dropping it would read the same for every
input and cost one fewer import-time call. Not incorrect, and the docstring's claim ("never true
for a product `external_ci` already covers") is true, just for a reason the code doesn't need to
state twice.
