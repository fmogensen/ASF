# Review — B-0099, round 2

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `writes:` is declared none on the card; the plan's Task 1 names `asf/tick/file_bugs.py` and `tests/test_file_bugs.py`, and the code diff touches only those (plus `docs/plans/b-0099.md`, `docs/reviews/1-b-0099.md`) |
| every Step of the Task is implemented | pass | Step 1 → `asf/tick/file_bugs.py:389-392` (`do_index(root)` after the parse-error gate, then `load_items` reload); Step 2 → `tests/test_file_bugs.py:468` and `:479` |
| acceptance tests byte-identical to the plan's | pass | the plan gives test names and behaviour, not bodies; both names match exactly (`test_a_rule_retired_since_the_last_index_files_no_bug` at `tests/test_file_bugs.py:468`, `test_a_rule_card_deleted_since_the_last_index_files_no_bug` at `:479`) and do what the plan says |
| those tests were run and are green | pass | `python3 -m unittest tests.test_file_bugs` → `Ran 24 tests in 8.257s` / `OK` |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `FAILED (errors=5, failures=35, skipped=14)` / `red: test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane`; the identical line on a clean `origin/main` worktree, so environmental, not the diff. `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` → not run: the sandbox refused it (needs approval) |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff; no background process; skipped: `tools/check_conventions.sh` (sandbox approval) |

verdict: approved

The two fail rows are the environment (the suite is red identically on `origin/main`) and a gate the sandbox would not let this reviewer run. Neither is caused by the diff, so neither blocks.

## C list

none

## I list

- `asf/tick/file_bugs.py:391-392`: the index is written once more per tick (and again after a filing); the plan accepts this cost.
- `asf/tick/file_bugs.py:392`: the reload drops `parse_errors`; the first load already gated on them.

### Missed in round 1

- The plan's absence was the only blocker in round 1, and it is now supplied; nothing else new.
