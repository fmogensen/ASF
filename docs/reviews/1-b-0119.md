# Review — B-0119, round 1

Branch `fix/B-0119`, head `d6cc40f` (two commits ahead of `origin/main`: `63b6d82` the fix, `d6cc40f` an empty stray file committed by the factory's own wip-commit step).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card declares no `writes:` footprint, so there is no fence to cross. `git diff origin/main...HEAD --name-status` is exactly `asf/tick/steps.py` (the fix), `tests/test_tick.py` (its test) and `.b0119_gate.log` (a 0-byte file added by `d6cc40f`'s wip auto-commit — see I2). |
| every Step of the Task is implemented | fail | No plan `docs/plans/b-0119.md` and no spec `docs/specs/b-0119.md` exist on the branch or `origin/main` (`find docs -iname '*0119*'` and `git log --all -- 'docs/plans/*0119*' 'docs/specs/*0119*'` are both empty), so no Step can be checked. Judged against the card's Description instead: `asf/tick/steps.py:109-159` (`run_command`) replaced the single `proc.communicate(timeout=timeout)` call with a `select`/`readline` loop that calls `emit(prefix + line.rstrip('\n'))` as each line arrives, so a command step's tick log now gets a line the moment it is written rather than only after the process exits — this is exactly the bug's "the tick writes a line when each step starts and ends" for a command step's own output. The card also asks for a starts/ends line with owner and pid and a doctor SCHEDULER row showing the current step; neither appears anywhere in this diff. See C1. |
| acceptance tests byte-identical to the plan's | fail | There is no plan and the card's own Acceptance is a single empty checkbox, so there is no fenced text to compare against. The test the commit names, `tests/test_tick.py:558` (`test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end`), is real and matches the commit message's `Test:` line. See C1. |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_tick.LegacyStepTests.test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end` → `ok`, `Ran 1 test in 4.393s` / `OK`. Full module: `python3 -m unittest tests.test_tick` → `Ran 57 tests in 186.156s` / `OK` — nothing in the file this diff touches is red. |
| the Gate commands are green | fail | `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` was refused by the sandbox ("this command requires approval"), same as the B-0110 round-1 review recorded; ran its logic directly in Python (same patterns file, same `asf.conventions --forbidden` generation, same three exemptions) and got `CLEAN`, but the named Gate command itself did not run — counted under the next row. `python3 -m asf.cli check` → rc 1, the same 262-line bare-decision-reference/no-closing-rule output the B-0109 review recorded as pre-existing (I6 there); this diff touches no record card. `python3 tools/run_tests.py` → `FAILED (errors=5, failures=35, skipped=14)`, red: test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane — the identical six modules the B-0087, B-0090, B-0099, B-0109 and B-0110 round-1 reviews all recorded as red, none of which import `asf.tick.steps` or exercise `run_command`; the one module this diff touches, `tests.test_tick`, is fully green. See I1. |
| no secret value printed, no background process, no skipped check | fail | No secret in the diff (only step names, prefixes and a timeout float). No background process left running by this review — `tools/run_tests.py` ran to completion in the foreground; a temporary `git worktree add /tmp/b0119-main-check origin/main` made to attempt an `origin/main` comparison run was removed before this report (`git worktree list` no longer shows it), and the two other `python -m unittest` processes visible in `ps` belong to unrelated worktrees (`lsof -p` shows their `cwd` under the operator's own checkout and a scratchpad, neither of them this checkout). Skipped: `bash tools/check_conventions.sh` was refused by the sandbox, same as the row above — a named Gate command that did not run counts this row `fail` regardless of the equivalent Python check's result. |

verdict: changes requested

## C list

- `docs/plans/b-0119.md` (missing): there is no plan or spec, so "every Step" and "byte-identical tests" cannot pass. Add the plan naming the Task — stream a command step's output line-by-line as it runs (done, `asf/tick/steps.py:109`) — and give the Acceptance as the test already at `tests/test_tick.py:558`.
- The card's Description also asks for "a line when each step starts and ends (step, owner, pid, time), flushed at once" and "the doctor's SCHEDULER row can then show the current step" — this diff only makes a *command* step's own stdout stream live; it adds no start/end line (owner, pid, timestamp) around a step, and touches nothing in the doctor. If the plan (once written) scopes the Task down to the command-output half only, say so explicitly and file the start/end line and the SCHEDULER row as a separate follow-up card; otherwise this diff is an incomplete delivery of the Task as described.

## I list

- `.b0119_gate.log`: a 0-byte file added by the wip auto-commit `d6cc40f`, same pattern the B-0110 round-2 review flagged as a non-blocking stray worktree log. Worth a `.gitignore` entry so the factory's own commit step stops picking it up.
- The six red gate modules (test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane) match the exact set recorded red across five prior rounds (B-0087, B-0090, B-0099, B-0109, B-0110); this round could not re-verify them against a clean `origin/main` (the comparison worktree was set aside once `tools/check_conventions.sh`'s sandbox refusal made a second full `run_tests.py` pass impractical inside this review's time budget) — confirm before relying on the precedent again.
- `asf/tick/steps.py:132-155`: the `select`/`readline` loop calls `time.monotonic()` once per iteration when `select` times out early on a spurious wakeup; harmless at today's scale but worth a comment if a future change tightens the poll interval.
