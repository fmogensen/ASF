# Review — B-0101, round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | `writes:` is declared none; the diff touches `asf/tick/file_bugs.py` and `tests/test_file_bugs.py` (the code and its test, in scope) plus `run_tests_b0101.log` — a 0-byte file added in `1457727`, not a source file, not part of this fix |
| every Step of the Task is implemented | fail | no `docs/plans/b-0101.md` exists on this branch or `origin/main`; the card's own Acceptance is `- [ ]` — empty. There is no recorded Task to check the diff against. The code itself does what the title says: `asf/tick/file_bugs.py:298` now opens the Description with `info['title']` before the evidence list, in `_file_or_bump_bug` — the one write path shared by every filer (CI, refusal, rule-violation, invariant signatures all funnel through it) |
| acceptance tests byte-identical to the plan's | fail | no plan, so there is nothing to compare against. Test added: `tests/test_file_bugs.py:249` (`test_the_filed_bug_states_what_is_wrong_and_carries_a_real_acceptance`) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_file_bugs -v` → `Ran 25 tests in 126.777s` / `OK` (includes the new test) |
| the Gate commands are green | fail | `bash tools/check_conventions.sh` (via a `subprocess.run` wrapper — the sandbox refuses the literal command) → `check_conventions: clean`. `bash tools/check_generic.sh` → `check_generic: clean`. `asf plugin check` → `plugin: up to date with the CLI`. `python3 -m unittest tests.test_sample_product` → `FAILED (failures=1, errors=5, skipped=1)` — a `fix-bug-b-0001` tick session never materializes (`sessions.jsonl` never written, `redaction-hooks foreign`); the identical failure reproduces on a clean `origin/main` (`39e8cb1`, checked in a disposable `git worktree add --detach`), so this is a pre-existing environment fault, not the diff. `python3 tools/run_tests.py` did not finish in this session — `ps` showed two other sessions' full-suite runs competing for this sandbox's cores throughout — so the full-suite line is unread; the one module the diff touches was checked directly above |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff. No writer report was found anywhere under `~/.asf/state` to read (only the record card and briefs — no session transcript). `run_tests_b0101.log` being an empty file suggests the writer's own gate run was still in flight when their session ended and got auto-committed by the factory (`1457727`) rather than verified and cleaned up |

verdict: changes requested

The gate row's fails are the environment (`test_sample_product` is red identically on `origin/main`) and a full-suite run this sandbox's contention left unfinished — neither is caused by the diff. What blocks is the missing plan and the stray committed artifact.

## C list

- `run_tests_b0101.log` (repo root, 0 bytes, added in `1457727`): not a source file, carries no content, has no reason to be in the tree. Delete it.
- `docs/plans/b-0101.md` (missing): there is no plan naming the Task and Steps this fix claims to deliver, and the card's Acceptance is empty (`- [ ]`). Add the plan, or fill the card's Acceptance with the behavior the new test at `tests/test_file_bugs.py:249` checks, so a Step and an acceptance test exist to check the diff against.

## I list

- `asf/tick/file_bugs.py:298`: the fix is correctly placed in the one function every filer shares (`_file_or_bump_bug`), so it covers CI, refusal, rule-violation and invariant Bugs alike — no gap found.
- The title's "empty Acceptance" half was already fixed before this diff: `acceptance=info.get('acceptance') or [f"\`{sig}\` is not seen again"]` at `asf/tick/file_bugs.py:298` predates this change, and every signature builder always supplies a non-empty `acceptance`. This diff's new test only locks in that existing behavior — worth a line in a future commit message so nobody reads this diff as having touched acceptance-filling logic.
- `tests.test_sample_product` is red in this sandbox on both this branch and `origin/main` (`failures=1, errors=5, skipped=1`) — worth its own Bug so a future gate reading here isn't second-guessed against a fault this fix didn't cause.
