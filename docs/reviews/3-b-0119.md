# Review — B-0119, round 3

Branch `fix/B-0119`, head `87e9b78` (one commit ahead of round 2's `b3479f5`, which is one commit
ahead of round 2's reviewed head `fbe29d7`: `b3479f5` is round 2's own review file landing on the
branch, `87e9b78` is a wip auto-commit that adds one 0-byte file, `.b0119_test_output.log`, and
touches nothing else). No code changed since round 2 — `git diff fbe29d7...HEAD -- asf/tick/steps.py
tests/test_tick.py docs/plans/b-0119.md` is empty. This round exists only because the head moved
(B-0094's own commit), not because of any new work; it re-verifies round 2's approval against the
one new file and against a fresh run of every gate.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | Plan Task 1 declares `writes: asf/tick/steps.py tests/test_tick.py` (`docs/plans/b-0119.md:17`). `git diff origin/main...HEAD --name-status` is `asf/tick/steps.py` (M), `tests/test_tick.py` (M), `docs/plans/b-0119.md` (A), `docs/reviews/1-b-0119.md` (A), `docs/reviews/2-b-0119.md` (A) — all already checked in as of round 2 — plus two 0-byte strays, `.b0119_gate.log` (A, round 1's I2/round 2's I item) and `.b0119_test_output.log` (A, new this round). No production file outside the declared footprint is touched; the two strays are empty auto-commit artifacts, not code — see I list. |
| every Step of the Task is implemented | pass | Unchanged since round 2. Step 1 (`docs/plans/b-0119.md:19-21`): `asf/tick/steps.py:129-142`, the `select`/`readline` loop replacing `proc.communicate`. Step 2 (`:22-24`): `asf/tick/steps.py:143-151`, `os.killpg` + drain + `timeout after {timeout}s — killed` + `return 124`. Step 3 (`:25-28`): `tests/test_tick.py:558-568`, `test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end`. |
| acceptance tests byte-identical to the plan's | pass | Same text as round 2 confirmed byte-identical (`docs/plans/b-0119.md:32-37` against `tests/test_tick.py`); no test file changed since (`git diff fbe29d7...HEAD -- tests/test_tick.py` empty). |
| those tests were run and are green | pass | `python3 -m unittest tests.test_tick.LegacyStepTests.test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end tests.test_tick.LegacyStepTests.test_timeout_kills_a_sleep tests.test_tick.LegacyStepTests.test_timeout_kills_the_whole_process_group tests.test_tick.LegacyStepTests.test_failing_command_step_exits_1_and_later_steps_still_run -v` → all four `ok`, `Ran 4 tests in 5.349s` / `OK`. Full module: `python3 -m unittest tests.test_tick` → `Ran 57 tests in 63.799s` / `OK`. |
| the Gate commands are green | fail | `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` refused by this session's sandbox, same as rounds 1-2. `python3 -m asf.cli check` → rc 1, same pre-existing bare-decision-reference/no-closing-rule record lint every round since B-0087 has recorded (touches no record card). `python3 tools/run_tests.py` → `Ran 2890 tests in 606.780s (93 module(s), 8 at a time)` / `FAILED (failures=1, skipped=14)`, `red: test_conventions` — `tests.test_conventions.CheckConventionsScriptTests.test_the_whole_package_is_clean`, reproduced in isolation (`Ran 1 test in 18.503s` / `FAILED`), asserting `check_conventions.sh`'s own return code is 0 and failing on `asf/upgrade.py:4` and `asf/upgrade.py:141` (both match the `tools/[A-Za-z0-9_-]+\.(py\|sh\|json)` pattern in `tools/forbidden-conventions.txt` — literal calls to `tools/install.sh` in a docstring and an operator message). `asf/upgrade.py` is untouched by this diff (`git diff origin/main...HEAD -- asf/upgrade.py` is empty) and is byte-identical to `origin/main`'s copy (`git show origin/main:asf/upgrade.py` carries the same two lines) — this is a real, reproducible, pre-existing failure on trunk itself, not introduced here. See `### Missed in round 1`. |
| no secret value printed, no background process, no skipped check | fail | No secret in the diff or in anything this review printed (step names, a `[command:<step>] ` prefix, a timeout float, a test's own assertion text — no token or credential). `python3 tools/run_tests.py` ran past this session's 600s foreground limit and was moved to background by the harness; it was watched to completion with `Monitor` before this report continued, not left running unattended, and no other background process was started or left behind (`git worktree list` shows only this checkout). Skipped: `bash tools/check_conventions.sh`, same sandbox refusal as rounds 1-2 — a named Gate command that did not run counts this row `fail` regardless of the equivalent check's result (unchanged treatment from rounds 1-2; this round additionally reproduced the same finding via the repo's own `test_conventions.py`, which did run). |

verdict: approved

Round 2's actual objection (none — it approved) stands: no code changed since that approval, the
three Steps and the Acceptance tests are unchanged and still green, and the two Gate rows that
fail here are both pre-existing and outside this diff's footprint (`asf/upgrade.py`, unrelated
record-lint content, and the sandbox's own refusal to run one script directly) — the same
treatment rounds 1-2 gave the equivalent pre-existing failures they found. Nothing in this diff
needs a change.

### Missed in round 1

- `tests.test_conventions.CheckConventionsScriptTests.test_the_whole_package_is_clean` is red
  right now, and reproducibly so, on `asf/upgrade.py` — a file already on the branch (via
  `4ef2578`, an ancestor of this branch's own `54de582`) at round 1's head `d6cc40f`, and
  identical to `origin/main` throughout. Round 1's full-suite run reported six red modules
  (`test_doctor`, `test_install`, `test_sample_product`, `test_gitfixture`, `test_dry_run`,
  `test_e2e_lane`) and did not list `test_conventions`; round 2's reported two full green runs of
  the whole suite. Since the code has not changed since round 2 and this failure reproduces
  deterministically in isolation right now, one of those two prior full-suite reports missed it —
  it was there to find in round 1's own diff. It is pre-existing and outside this Task's `writes:`
  (`asf/tick/steps.py`, `tests/test_tick.py`), so it is not a blocking finding here, but it is a
  real, currently-red line on this branch and on `origin/main` and is worth its own card so the
  Gate actually goes green rather than being re-asserted clean by habit.

## I list

- `.b0119_gate.log` and `.b0119_test_output.log`: two 0-byte strays from the factory's wip
  auto-commit step, same pattern as round 1's I2 / round 2's I item (`.b0119_test_output.log` was
  untracked at round 2's time and has since been committed by `87e9b78`). Both still worth a
  `.gitignore` entry so the auto-commit step stops picking them up.
- Two untracked scratch files sit in this worktree from an earlier session
  (`.b0119_modules.txt`, a test-module list; `.b0119_run_rest.sh`, a wrapper around
  `tools/run_tests.py`) — neither is part of any commit or this diff, left as found.
- `docs/plans/b-0119.md`'s "Accepted costs" section still defers the start/end line (step, owner,
  pid, time) and the doctor SCHEDULER row to "a follow-up card (filed against this item)" without
  naming its id — same open item round 2 flagged; confirm the card exists before this item closes.
