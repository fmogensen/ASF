# Review 5 — B-0135

No writer's report file is on record for this session either. Reviewed `fix/B-0135` at
`88f5c1a1f` against `origin/main` (`bae095283`, which now carries T-0075's two landed commits —
`git merge-base HEAD origin/main` → `bae095283`, confirming the branch sits cleanly on top of it).

Round 5 exists because round 4 found `.review-scratch/` (four files, 6420 lines from a different
card, B-0136, swept in by a wip auto-commit) inside the branch. The one commit since round 4,
`88f5c1a1f` ("remove `.review-scratch/`, a different card's scratch files swept in by the wip
auto-commit"), is exactly that fix: `git show --stat 88f5c1a1f` → the same four files, 6420
deletions, nothing else touched.

`git diff --stat origin/main..HEAD` → `tools/install.sh`, `asf/tick/tick.py`,
`tests/test_install.py`, `docs/guide/upgrading.md`, and `docs/reviews/{1,2,3,4}-b-0135.md` — 8
files, 454 insertions, 23 deletions. No `.review-scratch/*` anywhere (`find . -iname
'.review-scratch*' -not -path './.git/*'` → empty). Two commits visible in `git log
origin/main..HEAD` that are not B-0135's own (`e40b244db`, `3f6556566`, upgrade.py/test_metrics.py
work) are not actually in this branch's diff against `origin/main` — `git merge-base --is-ancestor
e40b244db origin/main` and the same for `3f6556566` both say yes: the branch was rebased onto a
`main` that already carries them, so they contribute nothing to this diff.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card still declares no `writes:` footprint. `git diff --stat origin/main..HEAD` is now exactly `tools/install.sh`, `asf/tick/tick.py`, `tests/test_install.py`, `docs/guide/upgrading.md` and this card's own `docs/reviews/*-b-0135.md` — the six files rounds 1-3 already approved, plus round 4's own review. C1 from round 4 (the four stray `.review-scratch/*.py` files) is gone: `88f5c1a1f` deletes exactly those four paths and nothing else. |
| every Step of the Task is implemented | pass | No plan exists (`ls docs/plans docs/specs \| grep -i 0135` → empty, still), so judged against the card's Want, unchanged since round 2: the lock held across the swap (`tools/install.sh:44-88`, the `flock`/`pipx install --force`/`flock -u` sequence, identical to rounds 2-4) and every `asf` step imported up front under that lock (`asf/tick/tick.py:423-428`, `for step, owner, _ in rows: if owner == 'asf': _asf_step(step)`, identical to rounds 2-4). |
| acceptance tests byte-identical to the plan's | pass | No plan to diff against; the card's `## Acceptance` is still empty. Its prose names one test, "install waits on a held tick lock" — present, unchanged since round 3, at `tests/test_install.py:1739` `test_install_waits_on_a_held_tick_lock`, plus `test_a_stuck_tick_lock_times_out` (`:1779`) and `test_the_lock_is_held_across_the_install_itself_not_just_before_it` (`:1803`), both round 1's own findings turned into tests. |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_install` → `Ran 122 tests in 20.153s` / `OK`, all three lock tests `ok` (confirmed individually: `test_install_waits_on_a_held_tick_lock ... ok`, `test_a_stuck_tick_lock_times_out ... ok`, `test_the_lock_is_held_across_the_install_itself_not_just_before_it ... ok`). `python3 -m unittest -v tests.test_tick` → `Ran 59 tests in 30.651s` / `OK`, `TickLockTests` all green. |
| the Gate commands are green | pass | `python3 tools/run_tests.py` → `Ran 3774 tests in 493.123s (109 module(s), 8 at a time)` / `OK (skipped=14)`. `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"), same as every prior round; substituted `python3 -m unittest tests.test_conventions -v` → `Ran 36 tests in 51.510s` / `OK`. `python3 -m asf.cli plugin check` → `plugin: up to date with the CLI`. `python3 -m asf.cli check` still not applicable in this worktree (`ls -d tasks stories epics bugs features decisions index.json` → every one "No such file or directory"). |
| no secret value printed, no background process, no skipped check | pass | `grep -riE "api[_-]?key\|password\|token\|secret"` over this fix's own files (`tools/install.sh asf/tick/tick.py docs/guide/upgrading.md tests/test_install.py`) turns up only pre-existing test fixtures unrelated to this fix (`_MARKER = 'THE-SECRET-MARKER'`, a stand-in AWS-shaped literal in an unrelated pre-push-hook test that the test itself asserts never gets printed) — nothing this fix's own commits introduced. No background process: every command in this review ran in the foreground to completion (the 493s full suite, the 52s `test_conventions`, the 20s `test_install`, the 31s `test_tick`). One check did not run literally (`bash tools/check_conventions.sh`, sandbox-refused); its equivalent unittest ran green in its place, as in every prior round. |

verdict: approved

## C

None.

## I

**I1 (carried, not new) — still no `docs/specs/b-0135.md` or `docs/plans/b-0135.md`.** Rounds 1-4
already recorded this; it still holds.

**I3 (carried, not new) — `tests/test_install.py`: the stray extra blank line before `if __name__
== '__main__':`.** Rounds 1-4 already noted this, still present (two blank lines instead of one
at the file's end), still cosmetic — `check_generic.sh` passes clean.

**I4 (carried, not new) — `tools/install.sh:45-51`'s comment sentence is harder to parse than it
needs to be.** Rounds 3-4 already recorded this; unchanged since.

### Missed in round 1

None found — the defect this round's own diff resolves (`.review-scratch/` removal) was itself
round 4's own finding, not something round 1 could have caught; round 4's finding predates this
round's commit by construction.
