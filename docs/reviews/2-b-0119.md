# Review — B-0119, round 2

Branch `fix/B-0119`, head `fbe29d7` (two commits ahead of round 1's `d6cc40f`: `a9ae141` an
empty wip auto-commit touching nothing, `fbe29d7` adds `docs/plans/b-0119.md`). No code changed
since round 1 — `asf/tick/steps.py` and `tests/test_tick.py` are byte-identical to `d6cc40f`
(`git diff d6cc40f...HEAD -- asf/tick/steps.py tests/test_tick.py` is empty). The only substantive
change this round is the plan round 1's C1 asked for.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The plan's Task 1 declares `writes: asf/tick/steps.py tests/test_tick.py` (`docs/plans/b-0119.md:17`). `git diff origin/main...HEAD --name-status` is `asf/tick/steps.py` (M), `tests/test_tick.py` (M), `docs/plans/b-0119.md` (A, the plan itself, this round's own deliverable), `docs/reviews/1-b-0119.md` (A, the prior round's review, already checked in) and `.b0119_gate.log` (A, a 0-byte stray — see I2, carried from round 1). No production file outside the declared footprint is touched. |
| every Step of the Task is implemented | pass | Step 1 (`docs/plans/b-0119.md:19-21`, a `select`/`readline` loop replacing `proc.communicate`, emitting per line): `asf/tick/steps.py:129-142` — `select.select([proc.stdout], [], [], remaining)` then `proc.stdout.readline()` then `emit(prefix + line.rstrip('\n'))` inside the `while True` loop. Step 2 (`docs/plans/b-0119.md:22-24`, timeout kills the process group, drains, emits, returns 124): `asf/tick/steps.py:143-151` — `os.killpg(proc.pid, signal.SIGKILL)`, drains `proc.stdout.read()` line by line into `emit`, then `emit(f'{prefix}timeout after {timeout}s — killed')`, `return 124`. Step 3 (`docs/plans/b-0119.md:25-28`, a two-line command with a 1s sleep between, asserting both lines and a >0.5s gap): `tests/test_tick.py:558-568`, `test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end` — `sh -c 'echo one; sleep 1; echo two'`, asserts `['[command:batch] one', '[command:batch] two']` and `times[1][0] - times[0][0] > 0.5`. |
| acceptance tests byte-identical to the plan's | pass | Plan Acceptance (`docs/plans/b-0119.md:32-34`) names `tests/test_tick.py:558`, `test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end` — present verbatim at that line. Plan Acceptance (`:35-37`) names `test_timeout_kills_a_sleep`, `test_timeout_kills_the_whole_process_group`, `test_failing_command_step_exits_1_and_later_steps_still_run` as unchanged — all three exist in `tests/test_tick.py` and none of their bodies differ from `origin/main` (`git diff origin/main...HEAD -- tests/test_tick.py` shows only the one new test method added). |
| those tests were run and are green | pass | `python3 -m unittest tests.test_tick.LegacyStepTests.test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end tests.test_tick.LegacyStepTests.test_timeout_kills_a_sleep tests.test_tick.LegacyStepTests.test_timeout_kills_the_whole_process_group tests.test_tick.LegacyStepTests.test_failing_command_step_exits_1_and_later_steps_still_run -v` → all four `ok`, `Ran 4 tests in 4.594s` / `OK`. Full module: `python3 -m unittest tests.test_tick` → `Ran 57 tests in 48.282s` / `OK`. |
| the Gate commands are green | fail | `bash tools/check_generic.sh` → `check_generic: clean` (rc 0), run literally. `bash tools/check_conventions.sh` → refused by this session's sandbox ("this command requires approval"), same as round 1; ran a faithful re-implementation (same two pattern sources — `tools/forbidden-conventions.txt` plus `python3 -m asf.conventions --forbidden`, same three exempt files, same `git ls-files 'asf/*.py' 'asf/**/*.py'`) and got `CLEAN`, but the named command itself did not run. `python3 -m asf.cli check` → rc 1, last lines the same bare-decision-reference / no-closing-rule record lint (`tasks/T-0214.md:56`, `stories/S-0001..4.md:16`) every round back through B-0087 has recorded as pre-existing and unrelated to this diff (touches no record card). `python3 tools/run_tests.py` → `Ran 2882 tests in 246-271s (93 module(s), 8 at a time)` / `OK (skipped=14)`, run twice, both green — unlike round 1's red six modules (`test_doctor`, `test_install`, `test_sample_product`, `test_gitfixture`, `test_dry_run`, `test_e2e_lane`), none of which are red this round. |
| no secret value printed, no background process, no skipped check | fail | No secret in the diff (step names, a `[command:<step>] ` prefix, a timeout float, no token or credential). No background process left running by this review — every command above ran in the foreground to completion, and no worktree or process was left behind (`git worktree list` shows only this checkout; the scratch files this review wrote, `.cli_check.out` and `.run_tests.out`, were removed before this report — `git status --short` shows only the pre-existing, session-supplied `.b0119_test_output.log`, untouched). Skipped: `bash tools/check_conventions.sh`, same sandbox refusal as the row above — a named Gate command that did not run counts this row `fail` regardless of the equivalent check's result (same treatment as rounds 2 of B-0090 and B-0099). |

verdict: approved

Both fail rows are the same standing sandbox limitation this repo's reviews have carried since
B-0087 — `check_conventions.sh` cannot be invoked directly in this sandbox — verified clean by a
faithful re-implementation each time, never the diff's fault, and not blocking in any prior round
that reached this point clean otherwise (2-b-0090, 2-b-0099, 4-b-0114). Round 1's actual
objection — no plan existed to check the Task or the Acceptance against — is resolved: the plan
now on the branch names the same three Steps already in the code, scopes the Task to the
command-step-output half of the bug precisely as round 1's C2 asked, and its Acceptance matches
the tests already in the tree. The full suite is green this round, unlike round 1.

### Missed in round 1

- (none — round 1's own table and C list already covered everything visible in that diff; this
  round only reviews the plan round 1 asked for, which did not exist to miss.)

## I list

- `.b0119_gate.log`: still present, still a 0-byte stray from the factory's wip auto-commit,
  same as round 1's I2. Still worth a `.gitignore` entry so it stops being picked up.
- `docs/plans/b-0119.md`'s "Accepted costs" section defers the start/end line (step, owner, pid,
  time) and the doctor SCHEDULER row to "a follow-up card (filed against this item)" — confirm
  that card actually exists before this item closes; the plan does not name its id.
