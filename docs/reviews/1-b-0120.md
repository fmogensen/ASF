# Review — B-0120, round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `writes:` is declared none; `git diff origin/main...fix/B-0120 --stat` touches only `asf/invariants.py` and `tests/test_record_stage.py` — the fix and its test |
| every Step of the Task is implemented | fail | no plan `docs/plans/b-0120.md` and no spec `docs/specs/b-0120.md` exist on the branch or `origin/main` (`git log --all -- docs/plans/b-0120.md docs/specs/b-0120.md` is empty), so no Step can be checked. What the code does: `asf/invariants.py:263-284` `check_i10` previously skipped its refusal only when the Feature was already landed *and* had unclosed Tasks before this writer ran (`_landed(before) and _unclosed_tasks(_before_record(ctx), fid)`); the fix drops the second clause so any Feature already landed before this writer ran is out of scope for I10, full stop (`:280-281`) — matching the bug: F-0095 landed with zero open Tasks, then T-0085 was minted under it and a later, unrelated write to `features/F-0095.md` was refused because `_unclosed_tasks` was empty *before* the writer ran |
| acceptance tests byte-identical to the plan's | fail | no plan exists to compare against. The card's own Acceptance is operational ("no writer is refused on I10 at `features/F-0095.md` for 24h") and not something a unit test can byte-match. Test added instead: `tests/test_record_stage.py:299-313`, `test_i10_a_task_opened_under_an_already_landed_feature_never_refuses_its_write` — builds F-0001 already `Resolved`/`landed`, then in one writer mints T-0001 under it (open) and appends an unrelated byte to F-0001's own card; asserts `findings == []` and F-0001's state stays `Resolved` |
| those tests were run and are green | pass | `python3 -m unittest tests.test_record_stage -v` → `Ran 20 tests` / `OK` (includes the new test and the pre-existing `test_i10_a_feature_resolved_over_an_open_task_is_refused`, which still refuses the original bug — a Feature landing *this* writer with an open Task). Also ran `tests.test_invariants` and `tests.test_doc_lane_landing` (the other two modules exercising I10): `Ran 38 tests` / `OK` |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `FAILED (failures=1, skipped=14)`, red: `test_conventions` (`asf/upgrade.py:4` and `:141`, a file this diff never touches — reproducible on `origin/main` unchanged since the diff doesn't touch it either) — environmental, not this diff. A second run also flagged `test_dry_run` on a `1s` vs `0s` gate-duration timing string, which disappeared on rerun — flaky, unrelated to `asf/invariants.py` or `tests/test_record_stage.py`. `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` → not run: the sandbox refused it (needs approval), same as the B-0115 round-1 precedent |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff (a dropped boolean-and clause, a 15-line test); no background process left running by this review. Skipped: `check_conventions.sh` (sandbox approval, same as B-0115). No writer's report was found on the branch or in reachable state, so it could not be read |

verdict: changes requested

## C list

- `docs/plans/b-0120.md` (missing): there is no plan or spec, so "every Step" and "byte-identical tests" cannot pass on the table above. Fix: add the plan naming the Task ("I10 does not re-litigate a Feature already landed"), its one Step (drop the `_unclosed_tasks(_before_record(ctx), fid)` clause in `check_i10`, `asf/invariants.py:280`), and give the Acceptance as the test now at `tests/test_record_stage.py:299-313`.

## I list

- The card's Acceptance ("no writer is refused on I10 at `features/F-0095.md` for 24h") is a soak criterion no review round can verify directly — it can only be confirmed by watching the factory not re-refuse that card once this lands. The unit test at `tests/test_record_stage.py:299-313` reproduces the exact refusal shape from the bug report and is the closest verifiable proxy.
- Once landed, `check_i10` never re-examines a Feature's Tasks again, by design (the docstring at `asf/invariants.py:267-269` says so). If a Feature's `state`/`stage` is ever hand-edited back to landed after being un-landed with Tasks still open, I10 would not catch it on the next write — worth a line in the (still-to-be-written) plan's Accepted-costs section, since it is a deliberate trade rather than an oversight.
