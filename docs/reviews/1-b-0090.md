# Review — B-0090, round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `writes:` is declared none; the diff is `asf/plugin_build.py`, `plugin/README.md`, `plugin/hooks/hooks.json`, `tests/test_plugin.py` only — the generator, its output, the doc note, and the test |
| every Step of the Task is implemented | fail | no plan `docs/plans/b-0090.md` and no spec `docs/specs/b-0090.md` exist on the branch or `origin/main`, so no Step can be checked; the code does what the commit title says: `asf/plugin_build.py:85-98` adds `CONSOLE_RULES` and `render_hooks_json()`, `:134` wires it into `expected_files()` so `plugin/hooks/hooks.json` is generated with a `SessionStart` hook |
| acceptance tests byte-identical to the plan's | fail | no plan, so there is nothing to compare. Test added: `tests/test_plugin.py:98-111` (`test_b0090_plugin_ships_a_hook_for_the_console_rules`) checks the file exists, a `SessionStart` command mentions "delegate", and `plugin_build.diff()` is empty |
| those tests were run and are green | pass | `python3 -m unittest tests.test_plugin` → `Ran 9 tests in 2.105s` / `OK` |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `FAILED (errors=5, failures=35, skipped=14)`, red: test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane — the same six modules the B-0099 round-1/round-2 reviews recorded as red on a clean `origin/main` (environmental, not this diff; none of the six touch `plugin_build.py`, `plugin/hooks/`, or `test_plugin.py`). `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` → not run: the sandbox refused it (needs approval), same as both B-0099 rounds |
| no secret value printed, no background process, no skipped check | fail | no secret in the diff (the hook string is a static sentence, no apostrophes, single-quoted, nothing exfiltrated); no background process left running by this review. Skipped: `check_conventions.sh` (sandbox approval). No writer's report was found on the branch or in reachable state, so it could not be read |

verdict: changes requested

## C list

- `docs/plans/b-0090.md` (missing): there is no plan or spec, so the "every Step" and "byte-identical tests" rows cannot pass. Fix: add the plan naming the Task and its Steps (generate `plugin/hooks/hooks.json` from `asf plugin build`; wire a `SessionStart` hook carrying the console rules), and give the Acceptance as the test now at `tests/test_plugin.py:98`.

## I list

- `asf/plugin_build.py:85-89`: `CONSOLE_RULES` paraphrases `docs/CONSTITUTION.md` §5 and R-0113 by hand; if either wording changes, this string silently drifts from its source. Consider a comment pointing back with the exact source lines, or generate it from them.
- `plugin/hooks/hooks.json:9`: the hook only prints the rules once at `SessionStart`; nothing re-asserts them mid-session if the transcript is compacted. Out of scope for this fix, but worth naming in a follow-up if rule adherence is later found to fade over a long session.
- Confirm the six red modules (test_doctor, test_install, test_sample_product, test_gitfixture, test_dry_run, test_e2e_lane) stay red on a clean `origin/main` in this environment before relying on that precedent again; not re-verified in this round, just cross-checked against the B-0099 reviews.
