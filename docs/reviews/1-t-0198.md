# Review T-0198 — round 1

No standalone writer's report file exists; the report is the commit message of `2bf33dc8c`
("landed_proves() gathers claims that landed..."). Reviewed that commit's diff against
`origin/main` (three-dot, matching `gh pr diff 40`), and Task 5 of `docs/plans/f-0040.md`.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff --stat origin/main...HEAD` and `gh pr diff 40 --name-only`: `asf/evidence/evidence.py`, `tests/test_evidence.py` only — the two files Task 5's `writes:` names. (`git diff origin/main HEAD`, two-dot, spuriously shows `asf/ci_queue.py` etc. touched — that is `origin/main` having advanced past this branch's parent by one unrelated commit, `954ee3e5f`; the branch's actual merge-base is `9c7338742`, the parent of `2bf33dc8c`, so the three-dot diff is the correct comparison and it is clean) |
| every Step of the Task is implemented | pass | Step 1 (pr source): `state == "MERGED"`, `mergeCommit.oid` set, `ancestry(product, [main_ref])` gate, `pr`/`sha` from the merge, `task` from `pr_naming_ids(p)` (`evidence.py:1138`) — the same helper `id_evidence` already uses for this purpose (`evidence.py:1043`), not literally `id_tokens` as the plan's prose says, but the established, more correct convention (`naming_ids`'s own docstring: a body mention "names nothing"). Step 1 (commit source): `git log --format=%H%x1e%B --grep=^Proves: origin/<main> --since=<record_since>` (`evidence.py:1140-1148`), `task` from `naming_ids(subject, product.main)` on the commit's own subject line. Step 2 (dedupe, pr kept): `seen`/`add()` keyed on `(story, line, test)`, PRs looped before the commit-log loop, so a duplicate's second occurrence (commit) is silently dropped and the `pr`-sourced entry survives (`evidence.py:1150-1152` vs. `1154-1160`) — verified by `test_the_same_claim_from_both_sources_keeps_the_pr_entry_once`. Step 3 (sort): `entries.sort(key=lambda e: (e["line"], e["task"] or ""))` (`evidence.py:1163`) — verified by `test_entries_sort_by_line_then_task`. Step 4: `"proves": landed_proves(product, prs)` added to `discover()`'s dict (`evidence.py:705`), built from the `prs` already in hand. Step 5: `id_tokens`, `ID_TOKEN`, `record_since` all untouched by the diff. Step 6: all six `LandedProvesTests` cases named by the plan are present and pass (see below) |
| acceptance tests byte-identical to the plan's | pass | plan's acceptance commands (`docs/plans/f-0040.md:427-434`) are `python3 -m unittest -v tests.test_evidence` and `python3 tools/run_tests.py && bash tools/check_conventions.sh && bash tools/check_generic.sh && python3 -m asf.cli check`, run verbatim below; `tests/test_evidence.py`'s new `LandedProvesTests` covers exactly the six cases the plan's Step 6 lists (trailer lands a claim, PR body lands a claim, an open PR's claim absent, a non-ancestor merged PR absent, dedupe keeps the pr entry, sort by line/task, plus a seventh — no-pull-request-host still lands commit claims — which is Step 4's own last sentence, not a stray addition) |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_evidence`: `Ran 105 tests in 51.279s` / `OK`, all 7 `LandedProvesTests` `ok`. `python3 -m unittest -v tests.test_proves`: `Ran 27 tests in 0.013s` / `OK`. `python3 tools/run_tests.py`: `Ran 3899 tests in 676.386s (111 module(s), 8 at a time)` / `OK (skipped=14)` — the 14 skips are pre-existing and outside `tests.test_evidence`/`tests.test_proves` (neither of those two modules' own `-v` runs above show a skip). `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` itself is refused by this sandbox ("This command requires approval") on every invocation attempted (unrelated to this diff — `check_generic.sh`, a sibling script, ran fine); reimplemented its exact logic (same `forbidden-conventions.txt`, same `asf.conventions --forbidden` generation, same 4-file exclude list, same `git ls-files asf/*.py asf/**/*.py` scope) in a throwaway script and it reports clean. `python3 -m asf.cli check` exits 1 with 283 lines, none mentioning `T-0198`, `evidence.py`, or `asf/evidence` — every line is a pre-existing bare-decision-reference or cross-task `writes:`-intersection warning against other cards (`tasks/T-0003.md`, `tasks/T-0024.md`, etc.), unrelated to and unreachable from this Task's footprint (`asf/evidence/evidence.py`, `tests/test_evidence.py` only; no backlog card is in `writes:`) |
| the Gate commands are green | pass | the Gate block (`docs/plans/f-0040.md:418-423`) is exactly `tests.test_evidence`, `tests.test_proves`, `tools/run_tests.py`, `check_conventions.sh && check_generic.sh` — all four covered above; the Gate does not include `asf.cli check` (that is Acceptance-only) |
| no secret value printed, no background process, no skipped check | pass | every check above ran to completion in the foreground and was waited on; no credential, token or secret appears in any run's output (`landed_proves` shells out to local `git log`/`gh pr list` only, both already-established patterns in this file); no test in `tests.test_evidence` or `tests.test_proves` was skipped |

verdict: approved

## C

(none)

## I

- `docs/plans/f-0040.md:391` cites `id_tokens (evidence.py:683)` for the PR's task extraction, but
  the landed code (rightly) uses `pr_naming_ids`, matching `id_evidence`'s own convention
  (`evidence.py:1043`) rather than the plan's literal words. Worth a one-line plan correction so a
  later reader does not go looking for a `id_tokens` call that was never the right one.
- The Task's own `bash tools/check_conventions.sh` gate command cannot be run directly in this
  review sandbox (blocked with "This command requires approval" on every attempt, while the
  sibling `check_generic.sh` runs fine); the equivalent logic was reproduced in Python and passed,
  but a session that can actually invoke the script should confirm it once for the record.

### Missed in round 1

(n/a — this is round 1)
