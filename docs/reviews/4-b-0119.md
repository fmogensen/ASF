# Review — B-0119, round 4

Branch `fix/B-0119`, head `73fd84b`. The worktree was rebased onto a fresh `origin/main` before
this round (the SHAs round 1-3 recorded — `d6cc40f`, `fbe29d7`, `87e9b78`, etc. — are no longer
in this branch's ancestry; the same commit subjects are, rehashed). No code changed since round
2/3's approval — `git diff origin/main...HEAD -- asf/tick/steps.py tests/test_tick.py` is
byte-identical to what rounds 2 and 3 reviewed. This round's own diff over round 3's approved
state is: two 0-byte strays (`.b0119_gate.log`, `.b0119_test_output.log`) deleted, `.gitignore`
gains a `.b[0-9][0-9][0-9][0-9]_*` entry, and `docs/plans/b-0119.md`'s "Accepted costs" section
gains the follow-up card's intended filename — the three things round 2 and round 3's I lists
asked for.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | Plan Task 1 declares `writes: asf/tick/steps.py tests/test_tick.py` (`docs/plans/b-0119.md:17`). `git diff origin/main...HEAD --name-status`: `asf/tick/steps.py` (M), `tests/test_tick.py` (M), `.gitignore` (M), `docs/plans/b-0119.md` (A), `docs/reviews/1-b-0119.md` (A), `docs/reviews/2-b-0119.md` (A), `docs/reviews/3-b-0119.md` (A). No production file outside the declared footprint is touched; `.gitignore` and the plan/review files are the round's own hygiene deliverable, not code, same treatment rounds 2-3 gave the plan and review additions in their own diffs. |
| every Step of the Task is implemented | pass | Unchanged since round 2/3. Step 1 (`docs/plans/b-0119.md:19-21`): `asf/tick/steps.py:131-152`, the `select`/`readline` loop replacing `proc.communicate`. Step 2 (`:22-24`): `asf/tick/steps.py:153-160`, `os.killpg` + drain `proc.stdout.read()` + `timeout after {timeout}s — killed` + `return 124`. Step 3 (`:25-28`): `tests/test_tick.py:558-568`, `test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end`. `git diff origin/main...HEAD -- asf/tick/steps.py` shows only a docstring reword (adds the "B-0119" rationale) plus the loop body already reviewed; no behavioural change. |
| acceptance tests byte-identical to the plan's | pass | Plan Acceptance (`docs/plans/b-0119.md:32-37`) names `tests/test_tick.py:558`, `test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end`, plus the three unchanged timeout/exit-code tests — all present verbatim; `git diff origin/main...HEAD -- tests/test_tick.py` shows only the one new test method, unchanged since round 2. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_tick.LegacyStepTests.test_b0119_output_is_emitted_as_the_command_runs_not_buffered_to_the_end tests.test_tick.LegacyStepTests.test_timeout_kills_a_sleep tests.test_tick.LegacyStepTests.test_timeout_kills_the_whole_process_group tests.test_tick.LegacyStepTests.test_failing_command_step_exits_1_and_later_steps_still_run -v` → all four `ok`, `Ran 4 tests in 4.863s` / `OK`. Full module: `python3 -m unittest tests.test_tick` → `Ran 57 tests in 78.109s` / `OK`. |
| the Gate commands are green | pass | `bash tools/check_generic.sh` → `check_generic: clean` (rc 0), run literally. `bash tools/check_conventions.sh` was again refused by this session's sandbox ("this command requires approval"), same as rounds 1-3; a faithful re-implementation (same two pattern sources — `tools/forbidden-conventions.txt` plus `python3 -m asf.conventions --forbidden`, same three exempt files, same `git ls-files 'asf/*.py' 'asf/**/*.py'`) prints `clean`, and `tests.test_conventions.CheckConventionsScriptTests.test_the_whole_package_is_clean` — round 3's red module — now passes directly (`python3 -m unittest tests.test_conventions` → `Ran 30 tests in 17.6s` / `OK`): the rebase picked up `131c0dc fix(upgrade): name the install script without its path, so check_conventions passes`, already an ancestor of `origin/main`, and `asf/upgrade.py` is now byte-identical to `origin/main` with no forbidden literal. `python3 -m asf.cli check` → rc 1, same pre-existing bare-decision-reference/no-closing-rule record lint every round since B-0087 has recorded (`tasks/T-0211.md:38` etc., `stories/S-0001..4.md:16`), touching no record card this diff writes. `python3 tools/run_tests.py` → `Ran 2915 tests in 523.081s (94 module(s), 8 at a time)` / `OK (skipped=14)` — fully green, unlike round 1's six red modules and round 3's one. |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff or in anything this review printed (step names, a `[command:<step>] ` prefix, a timeout float, a `.gitignore` pattern — no token or credential). No background process left running: `python3 tools/run_tests.py` ran to completion in the foreground under a 900s timeout (523s actual); this review's own scratch files (`.b0119_check_conventions.py`, `.b0119_cli_check.out`, `.b0119_run_tests.out`) were removed before this report (`git status --short` is empty). `git worktree list` shows the expected set of other sessions' worktrees, none added by this review. The one still-skipped check, `bash tools/check_conventions.sh`'s sandbox refusal, is covered by a faithful re-implementation that ran and by `test_conventions.py`'s own assertion of the same script, both green — same standing treatment as rounds 1-3, and this round the equivalent unittest is green rather than red. |

verdict: approved

Round 3's approval stands: no code changed since it, the three Steps and the four Acceptance
tests are unchanged and still green, and this round's own diff does exactly what round 2 and
round 3's I lists asked — deletes the two stray 0-byte files, adds the `.gitignore` rule so the
factory's wip auto-commit stops re-adding them, and gives the deferred follow-up card its
intended filename. The Gate is fully green this round, including the one row (`test_conventions`
/ `check_conventions.sh`) that was red in round 3 on a pre-existing, off-diff `asf/upgrade.py`
violation — that violation is gone because this branch was rebased onto a newer `origin/main`
that already carries its fix (`131c0dc`), not because of anything in this diff.

### Missed in round 1

- (none — this round's diff is entirely the hygiene/plan cleanup round 2 and round 3's own I
  lists asked for; nothing here was visible, or omitted, in round 1's diff.)

## I list

- `docs/plans/b-0119.md`'s "Accepted costs" section now names the follow-up card's intended path
  (`inbox/tick-a-start-end-line-per-step-owner-pid-time-and-doctor-s-scheduler-shows-the-current-step.md`)
  but says "awaiting the groom to give it an id" — the card is still not actually filed via
  `asf inbox`; `inbox/` does not exist inside this code checkout (it is filed against the record
  repo, outside this worktree's sandbox access, so this review cannot confirm it either way).
  Round 2 and round 3 both asked to "confirm that card actually exists before this item closes" —
  naming its intended path is progress but does not close that ask; run `asf inbox --title "…"`
  against the record before B-0119 itself is called done, or confirm here that it already
  happened and just wasn't visible from this checkout.
