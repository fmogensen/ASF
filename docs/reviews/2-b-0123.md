# Review — B-0123, round 2

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | `writes:` is still unrestricted, but `git diff origin/main origin/fix/B-0123 --stat` is no longer the 3-file fix round 1 saw: `origin/fix/B-0123` is 39 commits ahead of / 61 behind `origin/main` (`git rev-list --left-right --count origin/main...origin/fix/B-0123`) and its own commits include `8fc9ed0` "fix(B-0123): fix(upgrade): a deferred upgrade marks itself pending…", `b255a9f` "fix(B-0123): fix(upgrade): the upgrade's owner keeps running its steps…" and `9dd08b5` "fix(B-0123): feat(ci-queue): one CI start queue per product…" — none of these are the daily-catch-up card; they duplicate features `origin/main` already carries independently (`asf/upgrade.py` at `origin/main` already has `read_pending`/`write_pending`/`PENDING_TTL_S`, `asf/upgrade.py:46-96`). A direct consequence lands in the diff itself: see C3 below |
| every Step of the Task is implemented | fail | unchanged from round 1: no `docs/plans/b-0123.md` or `docs/specs/b-0123.md` exists on the branch or `origin/main` (`git log --all --oneline -- docs/plans/b-0123.md docs/specs/b-0123.md` is still empty) — still no Step list to check against. The code itself is unchanged since round 1: `asf/tick/tick.py:413-419` calls `steps.daily_catch_up_due`/`daily_catch_up_message` (`asf/tick/steps.py:206-227`), same as before |
| acceptance tests byte-identical to the plan's | fail | still no plan to compare against; the two tests round 1 named are unchanged at `tests/test_tick.py:605-648` (`DailyCatchUpTests`, same two test names, same bodies) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_tick.DailyCatchUpTests -v` → `Ran 2 tests in 4.202s` / `OK` |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `Ran 3259 tests in 592.604s (103 module(s), 8 at a time)` / `OK (skipped=14)` — fully green this round, no flake. `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` → not run: the sandbox refused it (needs approval), same gap as round 1 and the B-0115/B-0120 precedent — no evidence it is green, so this row cannot pass on the "no row without evidence" rule |
| no secret value printed, no background process, no skipped check | fail | no secret in what was inspected; no background process left running by this review. Skipped: `check_conventions.sh` (sandbox approval), repeat of round 1. No writer's report was found on the branch or in reachable state (searched for `*report*` and the `ASF-Session: asf/correct-b-0123@…` tag on `8fc9ed0`'s commit — no session artifact reachable from this worktree) |

verdict: changes requested

## C list

- `origin/fix/B-0123` (branch history, not a single line): commits `8fc9ed0`, `b255a9f`, `9dd08b5` carry the `fix(B-0123):` subject prefix (via `ASF-Session: asf/correct-b-0123@20260925T211114Z` on `8fc9ed0`) but their content — an `upgrade-pending` marker mechanism and a per-product CI start queue — has nothing to do with "a failed daily run is never retried until the next day", and duplicates work `origin/main` already has on its own (`asf/upgrade.py:46-96` at `origin/main` already defines `pending_path`/`read_pending`/`write_pending`/`clear_pending`/`PENDING_TTL_S`). Attempting to rebase this branch onto current `origin/main` conflicts on exactly these three commits (`asf/tick/tick.py`, `asf/upgrade.py`, `tests/test_drift.py`) because the same feature already exists upstream under a different history. Fix: cut these commits off the branch — rebase keeping only the real B-0123 commits (the daily-catch-up fix and the round-1 review file) onto current `origin/main` — before the next round.
- `asf/tick/tick.py:114`: as a direct symptom of the above, `git diff origin/main origin/fix/B-0123 -- asf/tick/tick.py` shows `bug_args = _ns(default_bug_epic=default_bug_epic, product=product.name, file_bug_level=…)` on `origin/main` losing `product=product.name` on `origin/fix/B-0123`. `asf/tick/file_bugs.py:429`, `_product(args)`, reads `getattr(args, 'product', None)` — with the kwarg gone, `cmd_file_bugs` resolves `env.load_product(None)` instead of the tick's own product, so the file-bugs step (and everything downstream of `_product`/`_conventions`, e.g. `default_bug_epic` fallback and bug-filing conventions) would run against the wrong product wherever this branch's tree is what lands. This is not part of the daily-catch-up fix and must not ship; it disappears once the branch above is rebased to drop the unrelated commits.
- `asf/tick/steps.py:226-227` (round 1's C, still open): `daily_catch_up_message` still prints `"daily: catching up — {hour:02d}:{minute:02d} run has not succeeded today"` — the card's Description still asks for the failure reason (`"daily: catching up — 06:00 run failed: <reason>"`), and no code in this diff threads a reason from the failed run into the later tick that catches it up. Unaddressed since round 1.
- `docs/plans/b-0123.md` / `docs/specs/b-0123.md` (round 1's C, still open): still missing, so "every Step" and "byte-identical tests" still cannot pass on the table above.

## I list

- Round 1's two I items (`asf/tick/steps.py:200-204`'s silent `None` fallback for a non-`at:`-shaped daily clock; the actual idempotency mechanism being the stamp write, not a call-site guard) are unaddressed but still non-blocking.
- Once the branch is rebased to only the B-0123-scoped commits, re-run the full round-2 table against that clean diff rather than assuming the fail rows above clear on rebase alone — the conflict resolution itself (three files) is exactly the kind of manual merge that deserves its own look.
