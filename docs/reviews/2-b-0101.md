# Review — B-0101, round 2

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `docs/plans/b-0101.md` Task 1 declares `writes: asf/tick/file_bugs.py tests/test_file_bugs.py`; `git diff origin/main..HEAD --stat` touches exactly those two files plus `docs/plans/b-0101.md` (the plan itself) and `docs/reviews/1-b-0101.md` (round 1's own review, a process record, not source). `run_tests_b0101.log`, the stray 0-byte artifact round 1's C list named, is gone (`git show 7c0ac73 -- run_tests_b0101.log` shows it deleted; `ls run_tests_b0101.log` → no such file; `git status --porcelain` clean) |
| every Step of the Task is implemented | pass | Step 1 ("build the Description body as `info['title']` followed by a blank line and the `Evidence:` list"): `asf/tick/file_bugs.py:295` → `body = info['title'] + '\n\nEvidence:\n' + '\n'.join(f"- {l}" for l in info['evidence'])`. Step 2 (the named test): `tests/test_file_bugs.py:249` defines `test_the_filed_bug_states_what_is_wrong_and_carries_a_real_acceptance` |
| acceptance tests byte-identical to the plan's | pass | the plan's Acceptance cites `tests/test_file_bugs.py::test_the_filed_bug_states_what_is_wrong_and_carries_a_real_acceptance` by exact name; that test exists at `tests/test_file_bugs.py:249` under `class FileBugsIntegrationTests`, unchanged since round 1 |
| those tests were run and are green | pass | `python3 -m unittest tests.test_file_bugs -v` → `Ran 25 tests in 35.386s` / `OK` (includes `test_the_filed_bug_states_what_is_wrong_and_carries_a_real_acceptance ... ok`) |
| the Gate commands are green | fail | `python3 -c "subprocess.run(['bash','tools/check_conventions.sh'])"` → `check_conventions: clean` (RC 0). Same wrapper, `tools/check_generic.sh` → `check_generic: clean` (RC 0). `asf plugin check` → `plugin: up to date with the CLI` (RC 0). `python3 -m unittest tests.test_sample_product -v` → `FAILED (failures=1, errors=5, skipped=1)`, `redaction-hooks RED ... foreign` — the identical fault round 1 already reproduced on a clean `origin/main` and recorded as pre-existing, not caused by this diff. `python3 tools/run_tests.py` timed out after 1500s unread; `ps aux \| grep run_tests.py` showed 9 concurrent processes from other sessions' full-suite runs sharing this sandbox throughout — the same contention round 1 hit, not this diff |
| no secret value printed, no background process, no skipped check | pass | `git diff origin/main..HEAD \| grep -iE "password\|secret\|token\|api[_-]?key\|BEGIN"` matches only the word "secret" inside the copied text of `docs/reviews/1-b-0101.md` itself, not an actual secret value. No process from this review session left running (`ps` after the timed-out full-suite check shows nothing of mine live). No writer-session transcript for `asf/correct-b-0101@20260924T235512Z` exists under the factory's state dir (only the brief) — the same known gap round 1 hit, not new — but this round's diff carries no stray or truncated artifact (the round 1 C-list log is gone, and the plan is complete), so there is no diff evidence of a skipped check |

verdict: approved

Both C items from round 1 are closed: the plan now exists and names the Task the diff
implements, and the stray `run_tests_b0101.log` is deleted. The gate row's fail is unchanged
from round 1's own finding — `test_sample_product`'s pre-existing `redaction-hooks` fault and
this sandbox's full-suite contention — neither caused by this diff, as round 1 already
established by reproducing the same failure on a clean `origin/main`.

## C list

None.

## I list

- Carried from round 1, still true: `tests.test_sample_product` is red in this sandbox
  (`failures=1, errors=5, skipped=1`, `redaction-hooks RED`) on this branch too — still worth its
  own Bug so a future gate reading here isn't second-guessed against a fault neither B-0101 diff
  caused.
