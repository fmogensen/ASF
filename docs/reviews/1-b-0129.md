# Review 1 — B-0129

Reviewed `fix/B-0129` at `0dd2ee2` against `origin/main`. `git merge-base HEAD origin/main` →
`fe8ccd5`, and `git log --oneline origin/main..HEAD` → the single commit `0dd2ee2`, so the branch
is exactly one commit ahead of its base whatever `origin/main` has done since (four more commits —
`6a015f7`, `d9d9916`, `f92c40d`, `aa8ab3a` — landed on trunk while this review ran; none of them
touch `asf/harvest/lane.py`'s `trunk_red` or `tests/test_harvest.py`, so they do not change what
this diff is judged against). No writer's report file exists in the worktree or the record for
this session; reviewed the commit message and diff directly, as round 1 of `B-0127` did for the
same gap. `git diff --stat origin/main...HEAD` — two files, `asf/harvest/lane.py`,
`tests/test_harvest.py`.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | The card declares no `writes:` footprint ("(none declared)"), so there is nothing to judge the diff against. The two touched files — `asf/harvest/lane.py` (the `trunk_red` method itself) and `tests/test_harvest.py` (its test) — are exactly the pair a bug fix in that one method should touch, and nothing else in the diff. Recorded `fail` only because a row with nothing to compare against carries no evidence; see I1. |
| every Step of the Task is implemented | pass | Neither `docs/plans/b-0129.md` nor `docs/specs/b-0129.md` exists (`ls docs/plans docs/specs \| grep -i 0129` → empty), so there is no Task with Steps; judged instead against the card's Description clause this diff addresses: "If the job is red or flaky on the trunk (red on ≥1 of the last N, or red then green on one sha), mark the check `trunk-red`." Before this diff, `trunk_red` (`asf/harvest/lane.py:2424`) picked `last = max(done, key=completed_at)` and judged only that one run's conclusion — a failed run rerun to green at the same commit reads as the *last* completed run and the whole sha reads clean. The diff (`lane.py:2456`) replaces that with `any(r.get('conclusion') in TRUNK_RED_CONCLUSIONS for r in done)` — a sha with a rerun is red if any completed run at it is, which is exactly "red then green on one sha" from the card's own wording. The rest of the card's Description (bounded re-run/wait instead of sending back, filing one Bug per trunk-red job, the exact `held <branch>: … re-gating, not the branch's fault` line) is delivered by code already on `origin/main` before this branch's merge-base — the `wait(lane, f, f'trunk-red: {...}')` calls at `lane.py:2380` and `:2398`, and the WAITING-not-corrected path exercised by the pre-existing `test_a_pr_red_on_a_check_the_trunk_is_red_on_too_waits_and_is_not_sent_back` (`tests/test_harvest.py:2353`) — none of it is this branch's diff, so it is out of scope for this round rather than missing from it. |
| acceptance tests byte-identical to the plan's | pass | No plan exists to be byte-identical to (`docs/plans/b-0129.md` absent), and the card's own Acceptance list is empty (`- [ ]`). The card's prose names two test scenarios directly. "a job green on the trunk still sends the branch back" is already covered by the pre-existing `test_a_pr_red_on_a_check_the_trunk_is_green_on_is_still_sent_back` (`tests/test_harvest.py:2368`), untouched by this diff. "a PR red only on a job that is red on the trunk is re-gated and triggers no correction row" is the pre-existing `test_a_pr_red_on_a_check_the_trunk_is_red_on_too_waits_and_is_not_sent_back` (`tests/test_harvest.py:2353`), also untouched. The one new test in the diff, `test_trunk_red_flags_a_rerun_that_flipped_from_red_to_green` (`tests/test_harvest.py:2422-2438`), matches the card's own added clause word for word — a completed `failure` run followed by a completed `success` run for `gate` at the same sha, asserting `trunk_red(['gate']) == {'gate': sha}` — and reuses the same `push_trunk`/`fake gh` fixture pattern as the neighboring `test_trunk_red_judges_the_latest_completed_run`. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_harvest -v` → `Ran 150 tests in 488.468s` / `OK`, including `test_trunk_red_flags_a_rerun_that_flipped_from_red_to_green … ok` and the two pre-existing trunk-red tests named above, unaffected by this diff. |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `Ran 3335 tests in 235-368s (105 module(s), 8 at a time)` / `FAILED (failures=2, skipped=14)`, red: `test_roles` (`test_every_kind_is_bound_and_no_more`, BINDINGS missing `spec-plan`/`direct`) and `test_tick_steps` (`test_an_open_pr_is_adopted_and_foreign_or_live_branches_are_left`, an unexpected `pr create` call). Neither touches a file this diff changes (`asf/roles/roles.py`, `asf/tick/*` are untouched by `0dd2ee2`). Isolated in a scratch worktree of the current `origin/main` tip (`aa8ab3a`, four commits past this branch's `fe8ccd5` merge-base): both tests pass there (`python3 -m unittest tests.test_roles.BindingTests.test_every_kind_is_bound_and_no_more` → `OK`; `python3 -m unittest tests.test_tick_steps.PrsStepTests.test_an_open_pr_is_adopted_and_foreign_or_live_branches_are_left` → `OK`) — `6a015f7` (already on trunk, not on this branch) adds exactly the `spec-plan`/`direct` bindings `test_roles` was missing. So the red is this branch sitting on a trunk snapshot from before those fixes landed, not a defect this diff introduces; recorded `fail` because the command's own last line is red, per the letter of the check. `bash tools/check_generic.sh` → `check_generic: clean`. `python3 -m asf.redact --unpublished` → `redact: clean`. `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"), same refusal noted in round 1 of `B-0127`; `tests.test_conventions` (which exercises the same script) ran green standalone — `python3 -m unittest tests.test_conventions -v` → `Ran 30 tests in 25.370s` / `OK`. |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff: it changes one docstring and one line of loop logic, plus a test fixture built from the module's own vocabulary — no token, credential or path outside the repo; `asf.redact --unpublished` over the branch's one commit is clean. No background process: every command above ran in the foreground to completion, nothing launched with `&` or `run_in_background`; the two scratch `git worktree add`/`remove` pairs used to isolate the gate failures from the diff ran and were removed in the same turn, and `git status --porcelain` is empty apart from this review file. The 14 skips are the suite's standing count, unchanged by this diff. One check did not run literally — `bash tools/check_conventions.sh`, covered by the green unittest that runs it; see the NEEDS OPERATOR line in the report. |

verdict: approved

## C

none.

## I

**I1 — neither `docs/specs/b-0129.md` nor `docs/plans/b-0129.md` exists, and the card declares no
`writes:`.** One row of this table had nothing to judge against and is recorded `fail` for that
reason alone, not for anything in the diff — the same gap I1 of `docs/reviews/1-b-0127.md` records
for that item.

**I2 — the Gate row is red on this worktree only because its trunk snapshot (`fe8ccd5`) predates
`6a015f7`, which is already on `origin/main` and fixes the exact `test_roles` failure seen here.**
Not a defect in this diff (verified by running the two failing tests against the current
`origin/main` tip in a scratch worktree, where both pass); flagging so whichever session next
touches this branch does not re-diagnose the same drift. The factory's own rebase-before-landing
step should carry this branch past `6a015f7` without anyone doing anything.

**I3 — `lane.py:2456`: `any(r.get('conclusion') in TRUNK_RED_CONCLUSIONS for r in done)` still
leaves `red[name] = sha` pointing at the *sha*, not at which of the (possibly several) runs was
the red one.** Fine for `trunk_red`'s own callers, which only ever join the dict's keys into a
`trunk-red: <names>` message, but if a later round wants to cite the run (as the card's "citing
the runs" line for Bug-filing asks), the run id is not retained here and would need re-fetching.
