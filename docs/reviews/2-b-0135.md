# Review 2 — B-0135

No writer's report file is on record for this session either; the commit message on `9e6bdfe7c`
is the only report. Neither `docs/specs/b-0135.md` nor `docs/plans/b-0135.md` exists (still —
`ls docs/specs docs/plans | grep -i 0135` → only `docs/reviews/1-b-0135.md`), so, as in round 1,
the card's own Description/Want and round 1's two C's are what the diff is judged against.

Reviewed `fix/B-0135` at `9e6bdfe7c` against `origin/main` (`3eebf372d`): `git diff --stat
origin/main..HEAD` → `5 files changed, 260 insertions(+), 19 deletions(-)` across
`tools/install.sh`, `asf/tick/tick.py`, `tests/test_install.py`, `docs/guide/upgrading.md`, and
`docs/reviews/1-b-0135.md` (round 1's own report, carried in the branch, not new work). Round 2's
own commit (`9e6bdfe7c`) alone: `asf/tick/tick.py` +6, `docs/guide/upgrading.md` +2/-1,
`tests/test_install.py` +44, `tools/install.sh` +38/-14 (`git show --stat 9e6bdfe7c`).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | The card still declares no `writes:` footprint. All five touched files are the natural footprint of an install/tick fix and its docs/tests/review record — same files round 1 touched, plus `asf/tick/tick.py` which round 1's own C2 named as the file the fix was missing from. |
| every Step of the Task is implemented | pass | No plan exists, so judged against the card's Want and round 1's two C's, both now addressed. **C1** (lock checked-then-released, not held): `tools/install.sh:47-75` opens `tick.lock`, loops on `fcntl.flock(f, LOCK_EX \| LOCK_NB)` until acquired or the bounded deadline trips, then runs `pipx install --force` under that same open, locked file, releasing it only in a `finally: fcntl.flock(f, LOCK_UN)` (`tools/install.sh:69-75`) — the lock is held *through* the install, not released before it. **C2** (steps imported lazily mid-tick): `asf/tick/tick.py:413-418`, in `_run_steps`, now does `for step, owner, _ in rows: if owner == 'asf': _asf_step(step)` before the step-running loop begins (`tick.py:419` on), resolving (importing) every `asf`-owned step's module up front. This runs after `acquire_lock` in `cmd_tick` (`tick.py:358-359`) has already taken `tick.lock`, so it happens under the same lock `install.sh` now waits on — a swap can no longer land between two of one tick's own step imports, and can't land mid-swap-vs-tick either, since both sides hold the one lock for the relevant window. |
| acceptance tests byte-identical to the plan's | pass | No plan exists to diff against; the card's own `## Acceptance` is still empty. Its prose names one test, "install waits on a held tick lock" — present as `tests/test_install.py:1260` `test_install_waits_on_a_held_tick_lock`. Two more were added beyond the named one: `tests/test_install.py:1300` `test_a_stuck_tick_lock_times_out` (carried from round 1) and the new `tests/test_install.py:1324` `test_the_lock_is_held_across_the_install_itself_not_just_before_it`, which is the direct regression test for C1 — it starts the install, waits until `pipx` (a stub that sleeps 1s) has been invoked, then asserts `fcntl.flock(probe, LOCK_EX \| LOCK_NB)` on `tick.lock` raises `OSError` while the install is still running, i.e. the lock is provably held *during* the swap, not just before it. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_install -v` → `Ran 87 tests in 40.170s` / `OK`, including all three lock tests (`test_install_waits_on_a_held_tick_lock`, `test_a_stuck_tick_lock_times_out`, `test_the_lock_is_held_across_the_install_itself_not_just_before_it`), each listed `ok`. |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `Ran 3370 tests in 835.008s (105 module(s), 8 at a time)` / `FAILED (errors=1, failures=1, skipped=14)`, red: `test_tick_steps`, `test_watch`. Neither is this diff's: `git diff origin/main..HEAD --stat -- tests/test_watch.py tests/test_tick_steps.py asf/tick/watch.py` → empty, nothing in the diff touches either file or its subject. Checked both in isolation on `origin/main` itself (a scratch worktree at `3eebf372d`, `PrsStepTests.test_harvested_and_at_trunk_branches_are_not_candidates`) → same `KeyError: 'state'` at `tests/test_tick_steps.py:925` — pre-existing, not introduced here. `test_watch` is round 1's already-recorded date-rollover flake (`tests/test_watch.py:12` `DAY = '2026-09-25'` against today's real `2026-09-26`), reproduced in isolation (`python3 -m unittest tests.test_watch` → same `FAILED (failures=1)`, `test_a_tick_appended_while_watching_is_printed`). `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` was again refused by this session's sandbox ("this command requires approval"); substituted `python3 -m unittest tests.test_conventions` → `Ran 30 tests in 135.149s` / `OK`, the same substitution every prior round records. `python3 -m asf.cli check` still not applicable in this worktree (`ls -d tasks stories epics bugs features decisions index.json` → every one "No such file or directory"). Row is `fail` per the letter of the check — two red modules — but both are verified environmental, not this diff's. |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff: a lock path, two env-var knobs (`ASF_INSTALL_LOCK_WAIT_S`, `ASF_INSTALL_LOCK_POLL_S`), a `for` loop over already-public step names, doc prose, and tests — no token, credential, or path outside the repo. No background process: every command here ran in the foreground to completion (the 835s full suite, the 135s `test_conventions`, the 40s `test_install`). One check did not run literally (`bash tools/check_conventions.sh`, sandbox-refused); its equivalent unittest ran green in its place, as in every prior round. |

verdict: approved

## C

None.

## I

**I1 (carried, not new) — still no `docs/specs/b-0135.md` or `docs/plans/b-0135.md`.** Round 1
already recorded this; it still holds. Two rows above still have only the card's own prose to
check the diff against.

**I2 (carried, not new) — `tools/install.sh:40`'s comment still says the lock is "the same lock a
running tick holds for its whole run," which is only true for a clock with an `asf` step.** Round
1 flagged the same line for the same reason; the wording is unchanged. `Locks.__init__`
(`asf/tick/tick.py:295-297`) still lets a command-only clock run with `held=False`, so "whole run"
overstates the general case, though it is now accurate for every clock this fix actually protects
(any clock with an `asf` step takes and holds `tick.lock`, and now pre-imports every such step
under it).

### Missed in round 1

None found — round 1's table and both C's account for everything visible in the diff it reviewed;
this round's fix addresses both without exposing anything round 1 should have caught.
