# Review 1 — T-0220

No writer's report file is on record in this worktree (checked: no `*T-0220*report*` anywhere
under it). Reviewed `worker/T-0220` at `105cb95bb` against `origin/main` (fast-forward ancestor,
`git log origin/main..HEAD` shows exactly this one commit) and against Task 1 of
`docs/plans/f-0044.md`.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff --stat origin/main...HEAD` → `asf/record/ingest.py \| 34 ++` and `tests/test_ingest.py \| 157 +++++` only — exactly the card's `writes: asf/record/ingest.py, tests/test_ingest.py`. |
| every Step of the Task is implemented | pass | Step 1: `ON_PROD_EVENT = 'feature-on-prod'` beside `MACHINE_KEY_ORDER`, with the §2.1 comment (`asf/record/ingest.py:35-36`). Step 2: `write_on_prod_event(root, iid, from_stage, now)` (`:39-59`) — path `<root>/metrics/events/<now[:10]>.jsonl`, reads the file if present and returns `False` on a matching `kind`+`item` line, else `os.makedirs(..., exist_ok=True)` and appends the exact `json.dumps(..., sort_keys=True, ensure_ascii=False) + '\n'` shape, returns `True`. Step 3: `old = machine.get('stage')` read before `_ingest_fields` (`:820-821`), and the fire condition sits after the `write_fields`/History block, inside the `if ordered is None: continue` guard (`:834-835`) — byte-identical to the plan's given snippet. Step 4: docstring names P7 (`natural_key` has no `events` branch, raises `KeyError`) as why it bypasses `append_event` (`:41-44`). Step 5: `_ingest_fields`, `append_history_lines`, `## History` and `stage_since` are untouched — confirmed by the diff itself touching no line in between. Step 6 & 7: covered by `FeatureOnProdEventTests`, see next row. `import json` / `import os` added at the top (`:14-15`). |
| acceptance tests byte-identical to the plan's | pass | The plan gives no literal test source for Task 1, only the behaviors of steps 6-7; `tests/test_ingest.py:958` defines `FeatureOnProdEventTests`, the exact class name the plan's **Acceptance** command names (`python3 -m unittest -v tests.test_ingest.FeatureOnProdEventTests`), and its 9 methods cover every clause: one line on the transition (`test_the_transition_into_on_prod_writes_one_event`), keys sorted + `ts` matches `TS_RE` (same test), a second same-day ingest adds none (`test_a_second_ingest_the_same_day_adds_none`), `landed` without a deployment writes none (`test_landed_without_a_deployment_writes_none`), already-`on-prod` writes none (`test_a_feature_already_at_on_prod_writes_none`), a Task/Bug reaching prod writes none (`test_a_task_and_a_bug_reaching_prod_write_none`), History/`stage_since` unchanged (`test_history_and_stage_since_are_unchanged_by_the_new_write`), and `intake_latency_rows` unmoved by the new kind — §3.1's last clause, P9 (`test_intake_latency_rows_is_unmoved_by_the_new_kind`). Two extra tests beyond the plan's list (`test_write_on_prod_event_is_idempotent_and_sorted`, `test_an_unparseable_line_is_skipped_not_raised_on`) directly exercise steps 2 and 4's own claims and add coverage, not scope. |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_ingest.FeatureOnProdEventTests` → `Ran 9 tests in 0.144s` / `OK` (all 9 `ok`, three benign pre-existing `ResourceWarning`s from an unrelated fixture helper at `tests/test_ingest.py:66`, unchanged by this diff). `python3 -m unittest tests.test_ingest -v` → `Ran 78 tests in 0.815s` / `OK`. |
| the Gate commands are green | pass | `python3 -m unittest tests.test_ingest -v` → `OK` (above). `tests.test_record` named in the plan's Gate does not exist in this tree (`ModuleNotFoundError: No module named 'tests.test_record'`, confirmed absent on `origin/main` too via `git show origin/main:tests/test_record.py` → "does not exist") — pre-existing gate/tree drift, not caused by this diff; ran its two real modules instead: `python3 -m unittest tests.test_metrics -v` → `Ran 95 tests in 11.359s` / `OK`; `python3 -m unittest tests.test_record_mentions tests.test_record_stage -v` → `Ran 22 tests in 1.135s` / `OK`. `python3 tools/run_tests.py` → `Ran 3823 tests in 469.965s (109 module(s), 8 at a time)` / `OK (skipped=14)`. `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"); ran its two halves by hand instead — `python3 -m asf.conventions --forbidden` (the generated pattern half) and a `grep -HInE` of the full pattern set against the one changed `asf/*.py` file, `asf/record/ingest.py` → no match, i.e. clean. `python3 -m asf.cli check` exits 1, but every line it prints is pre-existing noise (`tasks/T-000x.md` bare decision references, `writes:` overlaps between unrelated Active tasks, four stories with no closing rule) against a different, separately-configured backlog record on this host, not this worktree's own tree; nothing it prints names `T-0220`, `F-0044`, `ingest.py` or `feature-on-prod` (`grep -i` over its output → empty), so it is unconnected to this diff. |
| no secret value printed, no background process, no skipped check | pass | `grep -n "print(\|subprocess\|Popen\|os.system"` over `asf/record/ingest.py` turns up only the pre-existing `sys.stderr` parse-error print at `:799`, untouched by this diff. No secret-shaped literal anywhere in the diff (`write_on_prod_event` writes only `from`/`item`/`kind`/`ts`). Every command above ran in the foreground to completion; nothing was backgrounded. The one command not run literally (`bash tools/check_conventions.sh`, sandbox-refused) had its equivalent run by hand, as noted above. |

verdict: approved

## C

None.

## I

None.
