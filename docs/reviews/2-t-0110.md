# Review — T-0110, round 2

Diff reviewed: `980621515` (`origin/worker/T-0110`'s current head) against `origin/main` at
`bae095283` — the branch's merge-base with `origin/main` is `bae095283` itself, so the branch now
rebases cleanly onto current trunk (round 1's first C) and a straight `git diff origin/main HEAD`
is the task's own work, nothing else.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff --stat bae095283..HEAD` touches `asf/workers/report.py`, `tests/test_report.py`, `asf/briefs/facts.py`, `tests/test_brief_facts.py`, `tests/test_workers.py`, `tests/test_lifecycle.py`, `tests/test_tick_steps.py`, `asf/workers/runtime.py`, `tests/test_widen.py` — Task 1's `writes:` line plus the two files round 1's C list itself required (`runtime.py`, `test_widen.py`); `docs/reviews/1-t-0110.md` is round 1's own review file |
| every Step of the Task is implemented | fail | Steps 1–10 land as round 1 found, and both of round 1's C findings are fixed: no conflict markers remain in `footprint_claim` (`asf/workers/report.py:383-431`, confirmed by `grep -n '<<<<<<<\|=======\|>>>>>>>' asf/workers/report.py` — no match), and `KIND_FIELDS` now tables all fourteen kinds (`sorted(report.KIND_FIELDS) == sorted(build.KINDS)` is `True`, both lists `['adjudicate', 'close', 'coder', 'correct', 'direct', 'fix-bug', 'fixer', 'groom', 'plan', 'rebase', 'reshape', 'review', 'spec', 'spec-plan']`). But D11's own rule — "`parse`, `unpushed` and `NO_RE` are deleted, not deprecated…so the three readers outside the module that stood on them are retyped in the same diff" — is still not done in full: `grep -rn "report\(_mod\)\?\.\(parse\|unpushed\|NO_RE\)\b" asf/ tests/` (excluding `report.py` itself) finds two more live callers this branch never touched: `asf/workers/health.py:229-230` (`push_retry`: `report_mod.parse(...)`, `report_mod.NO_RE.match(...)`) and `asf/workers/lifecycle.py:598` (`overruling`: `report_mod.parse(text or '')`). `python3 -c "from asf.workers import report; report.parse"` raises `AttributeError` — both call sites are dead the moment this branch lands |
| acceptance tests byte-identical to the plan's | pass | `tests/test_report.py` is unchanged since round 1 (this round's commits touch only `report.py`, `runtime.py`, `test_lifecycle.py`, `test_widen.py`); round 1 already matched every class/case named by Task 1's Acceptance (`docs/plans/f-0025.md:133-185`) to `SchemaTest`/`ParseTest`/`RenderTest`/`ContractTest`/`ReadersTest`, unweakened |
| those tests were run and are green | fail | `python3 -m unittest tests.test_report -v`: 37 tests, `OK`. `python3 -m unittest tests.test_brief_facts tests.test_workers tests.test_lifecycle tests.test_tick_steps -v`: `Ran 307 tests in 55.579s` / `FAILED (errors=4)` — all four errors are `AttributeError: module 'asf.workers.report' has no attribute 'parse'` raised from `asf/workers/health.py:229` inside `push_retry`, hit by `tests.test_workers.TestHealth.test_b0075_a_self_reported_unpushed_result_is_held_for_correction_too`, `.test_transitions`, `TestSpawn.test_f0087_a_correction_takes_over_the_ended_jobs_worktree_with_its_uncommitted_work` and `.test_f0087_an_empty_reaped_branch_never_blocks_the_next_launch_on_it` |
| the Gate commands are green | fail | Same as above: the four-module command in Task 1's Gate is red with a reproducible `AttributeError`, so the Gate is not green regardless of the remaining commands. `bash tools/check_generic.sh`: `check_generic: clean`. `bash tools/check_conventions.sh`: `check_conventions: clean`. `python3 -m unittest discover -s tests -t .` still fails for the same reason (`health.push_retry` is on the path any health test exercises) |
| no secret value printed, no background process, no skipped check | pass | No secret literals in the diff; the changed code starts no background process; every Gate command was run — the failures above are reproduced runs, not skipped ones |

verdict: changes requested

## C — must fix before landing

- `asf/workers/health.py:229-230` (`push_retry`) — retype off `report_mod.parse(...).get('pushed')`
  and `report_mod.NO_RE.match(said)`, both gone. This is a live path: `push_retry` is called from
  `health()` for every unpushed/failed result, so any run through `tests.test_workers.TestHealth`
  or `TestSpawn` now raises `AttributeError` instead of classifying the failure — confirmed by
  the four errors above. Read `said` off `report.typed(text, None)`'s `pushed` field the way
  `asf/workers/runtime.py:477` was just retyped (`report.fence(text) is not None` /
  `report.failure(text)`), and replace `NO_RE.match(said)` with the typed check it stood in for
  (a `pushed` value that is not `'no'`).
- `asf/workers/lifecycle.py:598` (`overruling`) — `rep = report_mod.parse(text or '')`, then reads
  `rep.get('status')`, `rep.get('pushed')`, `rep.get('commits')` off the prose dict `parse`
  returned. `parse` no longer exists, so any call reaches an `AttributeError` (there is no test
  covering `overruling` today — `grep -rn overruling tests/` is empty — so the suite does not
  catch this one the way it caught `health.py`'s, but `asf/harvest/lane.py:1160` calls it in
  production on every lane pass that has an open finding). Retype the same four reads onto
  `report.typed(text, None)` (`status`, `pushed`, `commits`) plus the existing
  `report_mod.ruling`/`report_mod.ruling_fields`/`report_mod._claim` calls a few lines below,
  which are already typed-object calls and need no change.

## I — worth doing, not blocking

- `asf/briefs/facts.py:141-149` (`last_report`) — unchanged since round 1's I: `sha` is still not
  in `REPORT_FIELDS`, so a brief no longer carries which sha last landed.

### Missed in round 1

(none — both C findings above are readers of the deleted `parse`/`NO_RE` that round 1's own
review method could not have reached: round 1 diffed only the branch's single commit, which never
touches `health.py` or `lifecycle.py`, and round 1's test run never got past the merge-conflict
`SyntaxError` to execute anything that would have hit `push_retry`)
