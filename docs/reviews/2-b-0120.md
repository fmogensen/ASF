# Review — B-0120, round 2

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | Task 1's own `writes:` names `asf/invariants.py` `tests/test_record_stage.py`; `git diff origin/main...HEAD --stat` touches exactly those two plus `docs/plans/b-0120.md` and `docs/reviews/1-b-0120.md` — the plan and round-1 review, self-authored process artifacts outside any Task's footprint by convention, not code |
| every Step of the Task is implemented | pass | Now that `docs/plans/b-0120.md` exists: Step 1 ("drop the `_unclosed_tasks(_before_record(ctx), fid)` clause in `check_i10`, `asf/invariants.py:280`") → `asf/invariants.py:279-281` reads `if before and _landed(before): continue` — the clause is gone, exactly as specified. Step 2 (add `test_i10_a_task_opened_under_an_already_landed_feature_never_refuses_its_write` to `tests/test_record_stage.py`) → present verbatim at `tests/test_record_stage.py:299-313` |
| acceptance tests byte-identical to the plan's | pass | Plan's Acceptance cites `tests/test_record_stage.py:299-313`, `test_i10_a_task_opened_under_an_already_landed_feature_never_refuses_its_write`; `sed -n '299,313p' tests/test_record_stage.py` matches that function byte for byte, including the B-0120 docstring and both assertions (`findings == []`, `state == 'Resolved'`) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_record_stage -v` → `Ran 20 tests in 4.438s` / `OK` (includes the new test and the still-passing negative case, `test_i10_a_feature_resolved_over_an_open_task_is_refused`, which still refuses a Feature landing *this* writer performs while it has an open Task) |
| the Gate commands are green | pass | `python3 tools/run_tests.py` → `Ran 2916 tests in 883.449s (94 module(s), 8 at a time)` / `OK (skipped=14)` — round 1's one red row (`test_conventions` on an off-diff `asf/upgrade.py` literal) is gone; unrelated to this diff, it cleared on its own between rounds. `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` → sandbox refused it ("this command requires approval"), same as round 1 and the standing B-0090/B-0110/B-0115/B-0119 precedent; ran the equivalent unittest module instead — `python3 -m unittest tests.test_conventions` → `Ran 30 tests in 84.259s` / `OK`, covering the same script's pattern-and-exemption logic and green |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff (a 45-line plan doc, a docstring extension, a dropped boolean-and clause, a 15-line test — nothing exfiltrated). No background process left running: every command above ran to completion in the foreground; `git status --short` is empty at close. Skipped: `check_conventions.sh`'s sandbox refusal, covered by the faithful re-implementation in the row above (same standing treatment as the B-0119 round-4 precedent). No writer's report reachable — this session's sandbox exposes only this worktree (`find` outside it is refused), and none exists inside it either; the plan commit's own message (`0a0f477`) is the only account of that session's work — same standing limitation every prior round in this record has hit |

verdict: approved

## C list

none

## I list

- Round 1's I list carries forward unresolved: the card's Acceptance ("no writer is refused on I10 at `features/F-0095.md` for 24h") is a soak criterion no review round can verify directly, only watched for in the factory once this lands.
- The plan's own "Accepted costs" section now documents round 1's second I item (`check_i10` never re-examines a landed Feature's Tasks again is a deliberate trade, not an oversight) — closed by this round's plan, no further action needed.

### Missed in round 1

none — the code diff (`asf/invariants.py`, `tests/test_record_stage.py`) is unchanged since round 1, which already reviewed it in full; this round's only new material is the plan doc.
