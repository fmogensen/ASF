# Review T-0075 — round 2

Read the writer's commit `5e514f095` ("task(T-0075): render_daily's missing-registry guard checks
the ledger file, not a ConfigError that never fires"), its full diff against `origin/main`
(`asf/metrics/metrics.py` and `tests/test_metrics.py`, on top of round 1's `9f2ad1adf`), and
Task 4 of the plan (`docs/plans/f-0091.md:313-373`).

Round 1 (`docs/reviews/1-t-0075.md`) found one C: the `except env.ConfigError:` guard around the
landing computation never fires, because `measure.ended_runs`/`measure.table` return a
zero-filled table for a missing registry rather than raising, so a rollup with no session ledger
rendered `0 %` / `0 landed` instead of the required `—` row. This round verifies the fix.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff origin/main...HEAD --stat`: `asf/metrics/metrics.py \| 28 +++++++++++++++++++++++-----` and `tests/test_metrics.py \| 44 ++++++++++++++++++++++++++++++++++++++++++++` only (plus this review file) — both listed in the Task's `writes:` |
| every Step of the Task is implemented | pass | Step 1 (`scorecard_rows` gains `landing`, leads the table, `—` form) → `asf/metrics/metrics.py:514-527`. Step 2 (`render_daily` gains `product`, computes `landing` over the scorecard's own `week`, treats a missing registry as `landing=None`) → `metrics.py:716-728`: the dead `except env.ConfigError:` is gone, replaced with `if os.path.isfile(pool.sessions_path(product)):` — a product with no `state/<product>/sessions.jsonl` now takes the `landing=None` branch directly, no exception involved. Verified live: `env.state_dir` creates the directory (`os.makedirs(..., exist_ok=True)`) but never the file, so `sessions_path` not existing is exactly "no registry", and nothing in `measure.ended_runs`/`measure.table` is reached to raise anything. Step 3 (`cmd_rollup` passes `product=getattr(args, 'product', None)`) → `metrics.py:1451`, unchanged from round 1, still correct. Step 4 (tests) → `tests/test_metrics.py` `LandingRowTests`, both the present-ledger and missing-ledger cases now build a real temp `ASF_HOME`/`state/<product>/` tree instead of mocking an exception — closes round 1's I as well as its C. |
| acceptance tests byte-identical to the plan's | pass | Task 4 fences no literal test code (only prose + the shared `python -m unittest tests.test_tick_steps tests.test_tick tests.test_metrics -v` command, `docs/plans/f-0091.md:357-368`); the prose's three bullets (row leads, carries both numbers, `—` form) are covered by `tests/test_metrics.py:378-386` and the fixture line at `:305`. No literal fence to diff against — same finding as round 1, nothing changed here. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_metrics -v` (run now): `Ran 95 tests in 10.915s` / `OK` |
| the Gate commands are green | pass | `python3 -m unittest tests.test_metrics -v` → `OK` (above). `python3 -m unittest discover -s tests -t . 2>&1 \| tail -5` (run now, full suite): `Ran 3429 tests in 1088.750s` / `OK (skipped=14)`. `bash tools/check_generic.sh` (run now): `check_generic: clean` |
| no secret value printed, no background process, no skipped check | pass | `git diff origin/main...HEAD -- asf/metrics/metrics.py tests/test_metrics.py` adds no new `print`, no `subprocess`/`Popen`/`Thread`, no `unittest.skip` (the one `print(` line the grep matched is unchanged context in `cmd_rollup`, not added by this diff). Full-suite run finished on its own (`[exited with code 0]`), leaving only its own sandboxed `tick_test_*` temp-dir commit line in the tail, no leaked value and no process still running after completion. |

verdict: approved

## C

(none)

## I

(none — round 1's I, replacing the mocked `env.ConfigError` with a real missing ledger, was
folded into this round's fix rather than left outstanding)

### Missed in round 1

(none)
