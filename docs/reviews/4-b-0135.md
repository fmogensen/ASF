# Review 4 — B-0135

No writer's report file is on record for this session; the only commit since round 3's approval
(`docs/reviews/2-b-0135.md`'s counterpart at that point, `7e9ead6cd` "review(B-0135): round 3 —
approved") is `80992a4b7`, whose subject is itself the explanation: "wip(fix/B-0135): the session
ended with this uncommitted — committed by the factory (B-0094)". Neither `docs/specs/b-0135.md`
nor `docs/plans/b-0135.md` exists (still — `ls docs/specs docs/plans | grep -i 0135` → empty), so,
as in rounds 1-3, the card's own Description/Want and the carried record from those rounds are what
the diff is judged against.

Round 4 exists because round 3's approval predates the head: `git diff --stat c910cfaa6..HEAD`
(`c910cfaa6` = round 3's reviewed commit, "install.sh's lock comment names the clocks it actually
covers") → one new commit, `80992a4b7`, touching only `.review-scratch/base_test_install.py`,
`.review-scratch/fix_test_install.py`, `.review-scratch/main_test_install.py`,
`.review-scratch/parent_test_install.py` — `4 files changed, 6420 insertions(+)`. No file this fix
actually owns (`tools/install.sh`, `asf/tick/tick.py`, `tests/test_install.py`,
`docs/guide/upgrading.md`) changed at all since round 3 (`git diff c910cfaa6..HEAD -- tools/
install.sh asf/tick/tick.py tests/test_install.py docs/guide/upgrading.md` → empty).

Reviewed `fix/B-0135` at `80992a4b7` against `origin/main` (`994d3fdf6`): `git diff --stat
origin/main..HEAD` → `11 files changed, 6796 insertions(+), 23 deletions(-)`, the same six
fix/test/doc/review files rounds 1-3 already covered, plus the four new `.review-scratch/*.py`
files above.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | The card still declares no `writes:` footprint, but the four new `.review-scratch/*.py` files (`80992a4b7`, 6420 lines) are not this fix's footprint by any reading: `diff .review-scratch/fix_test_install.py tests/test_install.py` shows content about a `NOT_LOADED` clock-status case and a `scheduler status` retry harness explicitly commented `the B-0136 incident` — a different card's test material, not B-0135's. `git ls-tree -r origin/main --name-only \| grep -i review-scratch` and `git log --all --oneline -- .review-scratch` both show this directory exists nowhere else in the repo's history; it was introduced only by this one auto-commit. See C1. |
| every Step of the Task is implemented | pass | Unchanged since round 2/3: no plan exists, so judged against the card's Want, both parts of which remain delivered and untouched by this round's commit — the lock held across the swap (`tools/install.sh:47-88`, identical to round 3) and every `asf` step imported up front under that lock (`asf/tick/tick.py:423-428`, identical to round 3). `git diff c910cfaa6..HEAD -- tools/install.sh asf/tick/tick.py` → empty confirms neither moved this round. |
| acceptance tests byte-identical to the plan's | pass | No plan exists to diff against; the card's `## Acceptance` is still empty. Its prose names one test, "install waits on a held tick lock" — present, unchanged since round 3, at `tests/test_install.py:1635` `test_install_waits_on_a_held_tick_lock`, alongside `test_a_stuck_tick_lock_times_out` (`:1675`) and `test_the_lock_is_held_across_the_install_itself_not_just_before_it` (`:1699`). `git diff c910cfaa6..HEAD -- tests/test_install.py` → empty. |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_install` → `Ran 114 tests in 42.243s` / `OK`, all three lock tests listed `ok`. `python3 -m unittest tests.test_tick -v` → `Ran 59 tests in 61.781s` / `OK`, `TickLockTests` all green (the upfront-import site, `_run_steps`). |
| the Gate commands are green | pass | `python3 tools/run_tests.py` → `Ran 3760 tests in 511.549s (109 module(s), 8 at a time)` / `OK (skipped=14)` — clean, no flakes reproduced this round. `bash tools/check_generic.sh` → `check_generic: clean` (rc 0), run literally (even with the four stray `.review-scratch` files present, nothing in them trips a forbidden token). `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"), the same precedent every prior round records; substituted `python3 -m unittest tests.test_conventions` → `Ran 36 tests in 71.745s` / `OK`. `python3 -m asf.cli plugin check` → `plugin: up to date with the CLI`. `python3 -m asf.cli check` still not applicable in this worktree (`ls -d tasks stories epics bugs features decisions index.json` → every one "No such file or directory"). Green on the letter of the check — but see C1: green tests do not excuse the four files that should never have landed. |
| no secret value printed, no background process, no skipped check | pass | Scanned the four new files for anything secret-shaped (`grep -riE "api[_-]?key\|password\|token\|secret"` over `.review-scratch/*.py`) — the only hits are the same stand-in literal rounds 1-3 already cleared, `_MARKER = 'THE-SECRET-MARKER'`, never a real credential. No background process: every command in this review ran in the foreground to completion (the 512s full suite, the 72s `test_conventions`, the 42s `test_install`, the 62s `test_tick`). One check did not run literally (`bash tools/check_conventions.sh`, sandbox-refused); its equivalent unittest ran green in its place, as in every prior round. |

verdict: changes requested

## C

**C1 — `80992a4b7` lands four files, 6420 lines, that belong to no card this branch delivers, and
must come off `fix/B-0135` before this can ship.**

`.review-scratch/base_test_install.py`, `fix_test_install.py`, `main_test_install.py`, and
`parent_test_install.py` are four near-complete snapshots of a `tests/test_install.py`-shaped file
(`base`≈1573, `fix`≈1650, `main`≈1624, `parent`≈1573 lines) that read as a diffing tool's own
working copies — a base/fix/main/merge-parent comparison set — not test code this fix owns.
`diff .review-scratch/fix_test_install.py tests/test_install.py` shows they describe a *different*
card's material: a `NOT_LOADED` clock-status fixture and a `scheduler status` retry harness with the
comment "exactly the B-0136 incident" — B-0136, not B-0135. `git log --all --oneline --
.review-scratch` shows the directory exists nowhere in the repo's history except this one commit;
`git ls-tree -r origin/main --name-only | grep -i review-scratch` confirms `main` never had it
either. The commit message says why it happened: "the session ended with this uncommitted —
committed by the factory (B-0094)" — the factory's `commit_leftovers` wip auto-commit runs `git add
-A` and swept an untracked scratch directory into the branch, exactly the failure mode the repo's
own `.gitignore` already documents and has been bitten by twice before ("a session's own scratch
files in a card's worktree ... swept into a branch's history more than once (B-0110, B-0119)") — but
the ignore pattern it added only covers `.b[0-9][0-9][0-9][0-9]_*`, which `.review-scratch` does not
match (verified: `fnmatch.fnmatch('.review-scratch', '.b[0-9][0-9][0-9][0-9]_*')` → `False`), so it
was not excluded this time. The fix: a commit on `fix/B-0135` that removes
`.review-scratch/` entirely (`git rm -r .review-scratch`) — the six files rounds 1-3 already
approved are otherwise untouched and need no further change.

## I

**I1 (carried, not new) — still no `docs/specs/b-0135.md` or `docs/plans/b-0135.md`.** Rounds 1-3
already recorded this; it still holds.

**I2 (resolved, not carried) — round 3's "whole run" comment fix.** No further action.

**I3 (carried, not new) — `tests/test_install.py`: the stray extra blank line before `if __name__
== '__main__':`** rounds 1-3 already noted, still present, still cosmetic — `check_generic.sh`
passes clean and this round's commit does not touch the file.

**I4 (carried, not new) — `tools/install.sh:44-48`'s comment sentence is harder to parse than it
needs to be.** Round 3 already recorded this; the commit since then does not touch the file.

### Missed in round 1

None found — the defect this round catches (`80992a4b7`) postdates every prior round's diff by
construction; nothing in rounds 1-3's own reviewed commits could have shown it.
