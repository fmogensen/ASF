# Review — T-0137, round 2

Head reviewed: `97d10589f` (`task(T-0137): asf readme --refresh resolves the repo before requiring
product config`), on top of `35d50529e` (round 1) and `f8c6a6b44` (the original Task 3 commit).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff origin/main..HEAD --stat` touches `asf/cli.py`, `asf/conventions.py`, `asf/views/readme.py`, `docs/products.example.yaml`, `tests/test_cli.py`, `tests/test_conventions.py`, `tests/test_readme.py` — the Task's `writes:` list, plus `docs/reviews/1-t-0137.md`, which is the round-1 review artifact, not task code. The round-2 commit `97d10589f` itself touches only `asf/views/readme.py` and `tests/test_readme.py`, both in scope |
| every Step of the Task is implemented | pass | Round 1's only open item, Step 6, is now met: `asf/views/readme.py:401-417` resolves `repo_dir`/`conv` through `env.load_product` with a `try/except env.ConfigError` fallback to `_git_root(root)`/`conventions_mod.Conventions()` — the same pattern the default/`--check` branch two lines below already used — reads the page, and returns `readme: no spans — nothing to render`, exit 0, via `spans(text)` *before* `resolve_record`/the product config is ever required. Reproduced live: `readme.cmd_readme(argparse.Namespace(product=None, refresh=True, check=False, json=False), root=<repo with a spanless README and no product config>)` now prints `readme: no spans — nothing to render` and returns `0` instead of raising `ConfigError`. Steps 1-5 and 7 are unchanged from round 1's pass |
| acceptance tests byte-identical to the plan's | pass | `docs/plans/f-0030.md` Task 3 Acceptance: `python3 -m unittest -v tests.test_readme.CommandTests`, then `python3 tools/run_tests.py` / `bash tools/check_generic.sh` — run verbatim below |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_readme.CommandTests` → `Ran 6 tests in 1.798s` / `OK`; `python3 -m unittest -v tests.test_readme tests.test_conventions tests.test_cli` → `Ran 87 tests in 194.181s` / `OK`; `python3 tools/run_tests.py` → `Ran 3780 tests in 1024.255s (109 module(s), 8 at a time)` / `OK (skipped=14)` |
| the Gate commands are green | fail | `bash tools/check_generic.sh` → `check_generic: clean`. `bash tools/check_conventions.sh` did not run: every invocation form tried in this worktree (`bash tools/check_conventions.sh`, `sh tools/check_conventions.sh`, `./tools/check_conventions.sh`, and as the third leg of the `&&` chain the plan's Gate line names) is refused by this sandbox with `This command requires approval`, and no approval channel is available in this non-interactive session — not a code failure, an environment one (round 1's session could and did run it, getting `check_conventions: clean`; nothing in this round's diff adds a new path-shaped literal for it to catch). `python3 -m asf.cli check` → exit 1, last line `stories/S-0004.md:16: warning: no closing rule sees this item ...` — the same pre-existing backlog-record drift round 1 already found, touching none of this Task's files |
| no secret value printed, no background process, no skipped check | pass | `python3 tools/run_tests.py 2>&1 \| tail -6` ran past the foreground timeout and was auto-moved to background by the harness; it was watched to completion (not abandoned) and its final `OK (skipped=14)` line captured above before continuing. No credential, token or path outside the repo/record was printed. The one command that did not run is reported above and under `NEEDS OPERATOR`, not silently skipped |

verdict: approved

### Missed in round 1

- `asf/views/readme.py:401-421` (`cmd_readme`, `--refresh` branch) — the branch never checks
  `as_json`; `asf readme --refresh --json` on a page with a span prints the plain `readme: <path>
  rewritten/unchanged` lines, not the `{"ok": ..., "complaints": [...], ...}` shape §2.3/Step 5
  describes for `--json`. This is unchanged by round 2's fix and was already present, untested
  either way, in round 1's diff (`tests/test_readme.py`'s `test_spanless_readme_is_exit_0_on_every_form`
  tests `--refresh` and `--json` as separate forms, never combined). Worth confirming whether
  `--refresh --json` is meant to emit the JSON shape too, or whether `--refresh`'s output is
  intentionally always plain text and `--json` only modifies the default/`--check` path.

## C

none

## I

- `bash tools/check_conventions.sh` could not be executed in this review sandbox (see the Gate
  row). Round 1 ran it clean and this round's diff adds no new path-shaped literal, so there is
  no reason to expect regression, but the literal command was not re-verified this round.

NEEDS OPERATOR: run `bash tools/check_conventions.sh` (or grant this session's sandbox approval
to run it) on `plan/T-0137` head `97d10589f` and confirm it still prints `check_conventions:
clean` — this review could not execute that one Gate command itself.
