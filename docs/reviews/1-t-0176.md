# Review — T-0176, round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff --stat 7a5881dc0 363ce734c` touches only `asf/harvest/harvest.py` and `tests/test_harvest.py` — exactly Task 5's `writes:`. |
| every Step of the Task is implemented | pass | Step 1: `commits_name_items` (harvest.py:680), `commits_name_item` rewritten over it (harvest.py:688), `members_named` (harvest.py:694). Step 2: `lane_refusal(repo, trunk, branch, item, members=())` (harvest.py:851) — merge refusal unchanged, naming check is `commits_name_items(repo, trunk, branch, ids)` with the two texts (`commits do not name {names}` where `names` includes `"or one of …"` when `members`). Step 3: `land_ff` gains `note=''` (harvest.py:1070), `land_combined`/`land_set` gain `notes=None` (harvest.py:1213, 1232) and print `{(notes or {}).get(branch, "")}`. Step 4: `run_product_harvest` (harvest.py:1325) reads `members = delivery_members(items, item)` (:1386), passes to `lane_refusal` (:1391), builds `delivery_note` before the branch is deleted, passes `item_footprint(items, item)` as `item_writes`. Step 5: `DeliveryLaneTest` in tests/test_harvest.py. Step 6: see next row. |
| acceptance tests byte-identical to the plan's | pass | The plan (docs/plans/f-0102.md:497-503) gives prose, not literal source (no spec at docs/specs/t-0176.md exists to diff against byte-for-byte). All 4 named acceptance tests are present in `DeliveryLaneTest` and match the described behaviour: `test_commits_naming_different_members_are_accepted`, `test_a_commit_naming_no_member_is_refused` (asserts `'commits do not name F-0097 or one of F-0097, B-0034'`), `test_a_missing_member_is_reported_not_refused`, `test_merge_commit_still_refused`. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_harvest.DeliveryLaneTest -v` on the worker's own tree (363ce734c, checked out standalone — see the C list for why "standalone" matters): `Ran 8 tests in 112.123s` / `OK`. `python3 -m unittest tests.test_harvest -v` on the same tree: `Ran 85 tests in 89.163s` / `OK` (run with this review session's own `GIT_CONFIG_KEY_*` hook-injection variables unset — with them set, 5 unrelated fixtures fail because a product `commit-msg`-style hook from *this review session's own environment* prepends `task(T-0176):` to every commit subject the test fixtures make in their throwaway repos; confirmed as session contamination, not a code defect, by reproducing and then clearing it). |
| the Gate commands are green | fail | `python3 -m unittest tests.test_harvest -v`: green (above). `python3 tools/run_tests.py`: green once the session's hook-injection env vars are unset — `Ran 1858 tests in 191.038s (65 module(s), 8 at a time)` / `OK (skipped=14)` (the script's own hermetic env normally strips this, but see the C list: with the vars set it reports `FAILED (errors=6, failures=13, skipped=14)` across `test_doctor`, `test_install`, `test_gitfixture`, `test_sample_product` — none of them touched by this diff, all about `core.hooksPath`/hook-install machinery, i.e. review-session noise). `bash tools/check_generic.sh`: `check_generic: clean`. `bash tools/check_conventions.sh`: **could not be run** — this review session's own tool permissions blocked it (`bash tools/check_conventions.sh` and even the plain `python3 -m asf.conventions --forbidden` it shells out to were refused as "requires approval" with no operator present to approve). Manually grepped the diff against the static half of the check (`tools/forbidden-conventions.txt`): no hits. The generated half (`python3 -m asf.conventions --forbidden`) was never actually run against this diff. One of the four named Gate commands has no completed run — this row is `fail` on the letter of "a result with no evidence counts as fail," though the risk this specific diff trips it looks low. |
| no secret value printed, no background process, no skipped check | pass | Diff has no printed secrets, no background/subprocess spawn, no disabled/`SkipTest` test. Commit `363ce734c` carries `Signed-off-by:`. |

verdict: changes requested

## C (blocking)

- **The branch cannot land on current `origin/main` — the module it patches has been extracted and rewritten out from under it.** `worker/T-0176` is 435 commits behind `origin/main` (`git rev-list --left-right --count origin/main...363ce734c` → `435 1`) and carries exactly one commit, `363ce734c`, cut from `7a5881dc0`. Between that base and `origin/main` (`46e5913ff`), the commits from `e48caa821` ("fix(lane,feeder): the PR lane is one state machine…") onward moved every function this commit touches — `lane_refusal`, `land_ff`, `land_combined`, `land_set`, `commits_name_item`, `item_of`, `hold_with_correction`, `touched_files`, `merge_commits` — out of `asf/harvest/harvest.py` and into a class-based state machine in `asf/harvest/lane.py` (`Lane`, `lane_pass`, `gate_pass`); `asf/harvest/harvest.py`'s `run_product_harvest` on main is now a thin wrapper that delegates to it (harvest.py:887 on `46e5913ff`). Rebasing `363ce734c` onto `origin/main` produces real, non-mechanical conflicts (confirmed: attempted the rebase, hit conflicts at harvest.py:705, :921, :1428 whose HEAD side is empty because the surrounding functions no longer exist there — aborted rather than resolve them by guesswork, since that would mean inventing how this feature maps onto `Lane` rather than reviewing what was submitted). Landing this as written is not possible; it needs a genuine re-implementation against `asf/harvest/lane.py`'s current `lane_refusal(repo, trunk, branch, item, conv=None)` (lane.py:629 on `46e5913ff`) and the `Lane` class's per-branch loop (around lane.py:1148, `f['refusal'] = lane_refusal(repo, trunk, b, item, conv)`), not a rebase of this commit. Note also that main's `lane_refusal` has itself grown a third refusal kind since this branch was cut (`customer_content.refusal`, lane.py:641) that this commit's version knows nothing about — a naive port that copies this commit's `lane_refusal` body wholesale would silently drop that check.
  - Fix: re-cut this Task against current `origin/main`: port `commits_name_items`/`members_named`/`delivery_members`/`item_footprint`/`delivery_note` (these have no analogue on main and can likely move over close to as-is) into `asf/harvest/lane.py`, thread the `members=()` argument and the two refusal texts through the *current* `lane_refusal` (preserving its `conv`/`customer_content` refusal), and thread `note` through wherever `Lane` now prints `landed {branch} → …` (lane.py:1630, :1636, :2539, :2636 on `46e5913ff` — more than one call site now, where the plan's Task 5 assumed exactly two). The plan (docs/plans/f-0102.md, Task 5) itself needs updating first — its file/line pointers (`:669`, `:800`, `:988`, `:1124`, `:1141`, `:1233`, `:1297`) describe `asf/harvest/harvest.py` as it stood at `7a5881dc0`, not current `main`.

## I (non-blocking)

- The plan's Task 5 `writes:` line (`asf/harvest/harvest.py, tests/test_harvest.py`) is itself stale for the same reason as the C finding above — once re-cut, it should probably read `asf/harvest/lane.py, tests/test_lane.py` (or whichever module now owns this logic) instead. Worth fixing in the plan alongside the re-cut so the next round isn't reviewed against pointers that don't exist either.
- `delivery_note`'s `note = f': delivered {", ".join(named) or "nothing"}'` handles the all-missing case ("delivered nothing") — reasonable, but it's not one of the plan's four named acceptance tests, so it's untested at that exact branch (every member missing). Low risk given `members_named`'s own coverage, but worth a fixture once this lands on the current architecture.
