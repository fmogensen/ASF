# Review — T-0159, round 2

Branch `plan/T-0159`, head `329922881` — one commit past round 1's `bb1204a24`:
`329922881 fix(T-0159): check_savings names a non-dict conventions.savings block instead of
crashing`, which answers round 1's sole C1. Still delivers plan `docs/plans/f-0100.md`'s Task 3.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff origin/main...HEAD --stat` touches `asf/conventions.py`, `asf/doctor.py`, `docs/products.example.yaml`, `tests/test_conventions.py`, `tests/test_doctor.py` — Task 3's own `writes:` — plus `docs/plans/f-0100.md` (named in T-0159's top-level "Writes" list) and `docs/reviews/1-t-0159.md` (the round 1 review artifact itself, landed by the round 1 session, not a footprint violation). |
| every Step of the Task is implemented | pass | Steps 1, 2, 3, 5, 6 unchanged from round 1's pass. Step 4's gap is closed: `check_savings` (`asf/doctor.py:501-528`) now guards the container's own shape before iterating — `if savings is not None and not isinstance(savings, dict): findings.append((False, f'conventions.savings must be a map, not {savings!r}')); savings = None` — then falls back to `{}`, emitting a red finding naming the bad value instead of crashing. Reproduced live: `doctor.check_savings(env.Product('a', {'conventions': {'savings': 'yes'}}))` → `[(False, "conventions.savings must be a map, not 'yes'")]`, no exception. |
| acceptance tests byte-identical to the plan's | pass | Same classes as round 1 (`SavingsDefaultsTests` `tests/test_conventions.py:292-317`, `TestCheckSavings` `tests/test_doctor.py`), plus the new regression `test_a_non_dict_savings_block_is_named_not_crashed_on` (`tests/test_doctor.py:614-617`) added alongside them — an addition, not a change to the plan's named acceptance, so the named tests remain byte-identical to Step 6's description. |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_conventions tests.test_doctor tests.test_env` → `Ran 139 tests in 84.701s` / `OK` (139 not round 1's 138: the one new regression test). `python3 tools/run_tests.py` → `Ran 3648 tests in 743.936s (107 module(s), 8 at a time)` / `OK (skipped=14)`. |
| the Gate commands are green | fail | `bash tools/check_generic.sh` → `check_generic: clean` (rc 0). `bash tools/check_conventions.sh` refused by the sandbox again ("this command requires approval"), same class of refusal round 1 recorded; hand-verified: `python3 -m asf.conventions --forbidden` → the same 11 patterns round 1 saw, and grepping the two changed files for every pattern finds hits only inside `asf/conventions.py` (the module the script exempts by name) — `asf/doctor.py` has none. `python3 -m asf.cli check` → rc 1; grepping its output for `savings`/`doctor.py`/`products.example` turns up only pre-existing `writes: 'docs/products.example.yaml'/'tests/test_savings.py' intersects Active task ...` lines against other Active tasks' declared footprints — a backlog-record condition, not new to this diff (T-0056/T-0073/T-0119 vs T-0159 on `products.example.yaml`, and T-0159 vs T-0160 on `test_savings.py`, all pre-existing). Named command did not run clean, so the row is `fail`, but nothing in it traces to this Task's code — same disposition round 1 gave the same command. |
| no secret value printed, no background process, no skipped check | fail | No secret in the diff or in any command's output — only thresholds, docstrings, item/decision ids, and file paths. No background process: every command above was waited on to completion. Skipped: `bash tools/check_conventions.sh` was refused by the sandbox — a named Gate command that did not run counts this row `fail` regardless of the hand-check above (same finding round 1 made on the same command; not a new defect in this diff). |

verdict: approved

## C list

(none)

## I list

- `docs/plans/f-0100.md:207`: round 1's I-list note stands — the corrected `stage_limits` anchor
  reads `:447 on this base` where the merge-base actually has it at `:439`. Untouched by this
  round's commit, still cosmetic (the plan disclaims line numbers as context, never the target).

### Missed in round 1

(none — round 1's own I-list item above is carried forward, not new)
