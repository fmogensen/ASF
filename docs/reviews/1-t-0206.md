# Review — T-0206 round 1

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | commit `ce35751a8` (T-0206's own commit, diffed against its merge-base `7650e703c`) touches only `asf/size.py` and `tests/test_size.py`. `git diff origin/main..HEAD --stat` also lists `asf/ci_queue.py`, `asf/proves.py`, `asf/record/ingest.py`, `tests/test_ci_queue.py`, `tests/test_ingest.py`, `tests/test_proves.py`, `docs/reviews/1-t-0194.md`, `docs/reviews/1-t-0220.md` — this is trunk drift, not scope creep: `origin/main` (0c910d299) has three commits merged after `worker/T-0206` branched (T-0220 #33, T-0194 #32, a `ci_queue` relief commit), none of them touched by T-0206's own commit. |
| every Step of the Task is implemented | pass | Step 1 → `asf/size.py:19-22` (`SMALL, MEDIUM, LARGE`, `CLASSES`, `KINDS`, tested by `test_classes`/`test_kinds`). Step 2 → `asf/size.py:25-35` (`SizeConfig` frozen dataclass, the five named fields, all defaulted). Step 3 → `asf/size.py:55-66` (`countable`: `None`/empty → blocker, `:` prefix → blocker, wildcard char → blocker, else `len(writes)`). Step 4 → `asf/size.py:38-52` (`kind_of`, uses `asf.feeder.footprint.globs_overlap` per line 17/50, falls back to `'code'`). Step 5 → `asf/size.py:69-96` (`classify`'s four-step order: floor computed first but applied last so a blocker still wins `LARGE` over the floor, per plan step 5's "blocked sixteen-file footprint still be `LARGE`" — verified by `test_never_small_entry_does_not_lower_a_large_count`). Step 6 → `why` sentences at lines 60,63,65,78,88,90,92, each a substring assertion in the tests, never a whole-string match. Step 7 → `tests/test_size.py` covers every case the plan's step 7 lists (three→small, sixteen→large, ten→medium, both boundaries, wildcard-among-literals, cross-repo, empty, never-small floor, docs-kind and code-kind at fifteen, `kind_of` docs/code/no-match/empty, `CLASSES`/`KINDS`). |
| acceptance tests byte-identical to the plan's | pass | Plan's Task 1 **Acceptance**, `docs/plans/f-0041.md:126-128`: `` python3 -m unittest -v tests.test_size ``. Spec §3.1, `docs/specs/f-0041.md:420-422`: the same command. `tests/test_size.py` exists and is collected under that module path. |
| those tests were run and are green | pass | `python3 -m unittest -v tests.test_size` → `Ran 23 tests in 0.001s` / `OK`. |
| the Gate commands are green | pass | `python3 -m unittest -v tests.test_feeder tests.test_conventions` → `Ran 173 tests in 24.663s` / `OK`. `python3 tools/run_tests.py` → `Ran 3846 tests in 603.012s (110 module(s), 8 at a time)` / `OK (skipped=14)`. `bash tools/check_generic.sh` → `check_generic: clean`. `check_conventions.sh` could not be invoked directly in this sandbox (script execution with parameter expansion is blocked here), so its check was reproduced by hand: `python3 -m asf.conventions --forbidden` plus `tools/forbidden-conventions.txt`'s patterns, grepped against `asf/size.py` (the only `asf/**/*.py` this Task writes) — zero matches, i.e. clean. `python3 -m asf.cli check` exits 1, but every line it prints is a pre-existing lint finding against the separate backlog record repo (bare `D<n>` references and footprint intersections across dozens of unrelated task cards, e.g. `tasks/T-0003.md`, `tasks/T-0024.md`, `tasks/T-0056.md`) — none of it names `T-0206`, `asf/size.py` or `tests/test_size.py`, and `asf/cli.py` (the only file this command's own code lives in) is untouched by T-0206's commit (`git diff 7650e703c..HEAD -- asf/cli.py` is empty). This is baseline record-repo noise this Task's footprint cannot have caused. |
| no secret value printed, no background process, no skipped check | pass | No credentials or tokens appeared in any command's output above. Every check ran in the foreground and was waited on; nothing was launched with `run_in_background`. |

verdict: approved

No C list.

I list:
- `asf/size.py:31-35` — `SizeConfig`'s numeric defaults (`small_max_files=3`, `small_max_files_by_kind={'docs': 15, 'copy': 15}`, `medium_max_files=15`) duplicate the `DEFAULT_SMALL_MAX_FILES`/`DEFAULT_SMALL_MAX_FILES_BY_KIND`/`DEFAULT_MEDIUM_MAX_FILES` constants Task 2 (`docs/plans/f-0041.md:160-163`) is about to add to `asf/conventions.py`. Not a defect in this Task — the plan explicitly asks for "every field defaulted, so the module is usable with no product at all" and names no source of truth for the numbers — but worth a one-line cross-check when Task 2 lands, so the two literals never drift apart.
