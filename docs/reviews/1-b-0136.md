# Review 1 — B-0136

Reviewed `fix/B-0136` at `8e120bfc8` against `origin/main` at `2b5fa1e70`. `git diff --stat
origin/main...HEAD` — seven files: `asf/doctor.py`, `asf/views/status.py`, `docs/reviews/1-b-0136.md`,
`tests/test_doctor.py`, `tests/test_install.py`, `tests/test_views.py`, `tools/install.sh`. Neither
`docs/plans/b-0136.md` nor `docs/specs/b-0136.md` exists, so there is no Task with Steps or a
plan's acceptance tests to compare against — judged against the card's own Description and Tests
clause. The branch's own history carries an earlier `docs/reviews/1-b-0136.md` (commit `3c17507eb`,
"round 1 — approved") written against a since-superseded head, plus a run of commits whose
subjects repeat verbatim (`the console gets its own reviewed allow list` ×3,
`a declared clock not loaded is named, not omitted` ×5) — the branch's last commit,
`8e120bfc8`, explains why: "the branch's own history hit the same conflict against
`origin/fix/B-0136` three times; each failed correction replayed content already present instead
of dropping it." That commit's own diff and message are judged as a correctness claim below (row
2), not taken on trust — it did not fully hold.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | The card declares no `writes:` footprint ("(none declared)"), so there is nothing to judge the diff against — recorded `fail` only because a row with nothing to compare against carries no evidence; see I1. The seven touched files are exactly the pair of production modules the card's Description names (`tools/install.sh`'s scheduler step; `asf status`'s Cron row and `asf doctor`'s scheduler section) plus their own tests, plus this review file — nothing else in the diff, and no `asf/console_perms.py` (that feature is already on `origin/main` from B-0131; confirmed `test_step_6_offers_the_console_permissions_and_writes_nothing` and `class ConsolePermissionsTest` both already exist in `origin/main:tests/test_install.py`). |
| every Step of the Task is implemented | pass | No plan exists, so judged against the card's own Description. "tools/install.sh verifies after its scheduler step that every clock the product declares is loaded, retries the bootstrap once, and fails the install loudly naming the missing label if it still isn't": `tools/install.sh:67-91` adds `scheduler_install_verified()`, which runs `asf scheduler install`, reads back `asf scheduler status --product "$PRODUCT"`, retries the bootstrap once on a non-zero status, re-reads status, and on a still-non-zero result prints `install: NEEDS OPERATOR: clock(s) still not loaded after retrying the bootstrap: <label>` to stderr before `step()` records the step `FAILED`. "The tick of any other product (or `asf status`) flags 'clock \<label\> not loaded' as RED in the Cron row, instead of omitting it": `asf/doctor.py:662-679`'s `scheduler_rows` computes `missing` (declared clocks absent from `loaded_labels`) and appends one `(RED, label, 'declared in products/{p}.yaml but not loaded')` row per label; `asf/views/status.py:294-312`'s `cron_cell` computes the same `missing` set and appends `'clock {label} not loaded'` into the Cron cell's text (this view has no color layer at all — text is the row's only signal, consistent with every other flagged row in it). |
| acceptance tests byte-identical to the plan's | pass | No plan exists; the card's Acceptance list is empty. The card's Description names the scenario directly: "a declared clock with a plist but no loaded job is reported not-loaded by status and doctor; install retries the bootstrap." All four tests the commits name exist: `test_a_declared_clock_with_no_loaded_job_is_red_naming_the_label` (`tests/test_doctor.py:849`), `test_cron_flags_a_declared_clock_that_is_not_loaded` (`tests/test_views.py:228`), `test_an_unloaded_clock_is_retried_once_and_then_succeeds` and `test_an_unloaded_clock_still_missing_after_retry_fails_loudly_naming_it` (`tests/test_install.py:1590`, `:1602`) — see C1 below: each of these last two, plus `test_step_6_offers_the_console_permissions_and_writes_nothing`, is defined a **second, byte-identical time** later in the same file (`tests/test_install.py:1622`, `:1634`, `:1648`), a leftover of the rebase-conflict replay `8e120bfc8` describes and claims to have resolved down to "one copy of each" — it has not: two copies of each survive, not one. `python3 -m unittest tests.test_install.InstallScriptTest -v` runs 6 tests, not 9, confirming Python silently drops the earlier definition of each pair — the class still misreports its own test count, the exact defect `8e120bfc8`'s message names. |
| those tests were run and are green | pass | `python3 -m unittest tests.test_doctor.TestSchedulerSection tests.test_views.StatusViewTests tests.test_install.InstallScriptTest -v` → `Ran 29 tests` / `OK`, including all four named tests. |
| the Gate commands are green | pass | `python3 tools/run_tests.py` → `Ran 3714 tests in 555.650s (109 module(s), 8 at a time)` / `OK (skipped=14)`. `bash tools/check_generic.sh` → `check_generic: clean`. `python3 -m asf.redact --unpublished` → `redact: clean`. `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"); `python3 -m unittest tests.test_conventions -v` (exercises the same script) → `Ran 36 tests in 88.537s` / `OK`. |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff: scheduler read-back logic, a doctor row, a status cell, an install-script retry, and their tests, all built from the module's own existing vocabulary — no token, credential, or path outside the repo; `asf.redact --unpublished` is clean. `python3 tools/run_tests.py` (555s) and `tests.test_conventions` (88s) were both run to completion in the foreground and waited on; no background process was left running. One check did not run literally — `bash tools/check_conventions.sh`, refused by the sandbox — covered instead by the unittest that exercises the same script, same gap as prior rounds on this card's neighbors. |

verdict: changes requested

## C

**C1 — `tests/test_install.py` still defines three test methods twice, byte-identical, a
leftover of the rebase-conflict replay the branch's own last commit (`8e120bfc8`,
"a duplicated rebase leaves one test method three times over") claims to have fixed down to one
copy each.** `test_an_unloaded_clock_is_retried_once_and_then_succeeds` (lines 1590 and 1622),
`test_an_unloaded_clock_still_missing_after_retry_fails_loudly_naming_it` (lines 1602 and 1634),
and `test_step_6_offers_the_console_permissions_and_writes_nothing` (lines 1616 and 1648) are each
defined a second time a few lines below the first, with identical bodies. Python silently drops
the earlier definition of each pair (the class runs 6 methods where the source defines 9), so the
suite still passes — but the file still lies about its own test count, which is the precise
defect `8e120bfc8` set out to close and did not. Fix: delete the second definition of each of the
three methods (`tests/test_install.py:1622-1653`), leaving one copy of each, as the commit message
already claims is the state of the file.

## I

**I1 — the card declares no `writes:` footprint, so the scope row has nothing to judge the diff
against.** Recorded `fail` for that reason alone; the seven files touched are exactly what the
card's Description calls for, plus this review file, and nothing more.

**I2 — `asf/doctor.py`'s and `asf/views/status.py`'s `missing` computation is duplicated
almost verbatim between the two modules** (`scheduler.label_for(product.name, c.name, cfg) for c
in declared if ... not in loaded_labels`). Fine as delivered — the two call sites differ in what
they do with `missing` (a doctor row vs. a joined cell fragment) — but a third caller would be the
signal to factor it into one `asf/scheduler.py` helper (e.g. `missing_clocks(product, loaded,
cfg)`) rather than a third copy.

**I3 — the branch's history is difficult to audit as delivered**: alongside the genuine B-0136
fix commit, it carries a run of commits whose subjects repeat verbatim across a rebase-conflict
loop (`the console gets its own reviewed allow list` ×3, tagged `ASF-Session: asf/fix-bug-b-0131`
— a different item's session id, from before B-0131 landed on `origin/main`; `a declared clock not
loaded is named, not omitted` ×5) that net out to no change in the final tree (beyond C1) but make
`git log` on this branch actively misleading about what happened and when. Nothing in this
diff's tree needs to change beyond C1, but a future rebase of this branch should collapse this
run rather than replay it again.
