# Review 1 — B-0136

Reviewed `fix/B-0136` at `456db94` against `origin/main`. `git merge-base HEAD origin/main` →
`55c0250`; `origin/main` has since gained one more commit, `5651905d9` (`fix(lane): a branch
rewrite touches only the branch's own commits…`), which touches only `asf/harvest/lane.py` and its
test — nothing this branch's diff touches, so it does not change what this diff is judged against.
`git log origin/main..HEAD --oneline` → the single commit `456db94`. No writer's report file
exists in the worktree or the record for this session; reviewed the commit message and diff
directly. Neither `docs/plans/b-0136.md` nor `docs/specs/b-0136.md` exists (`ls docs/plans
docs/specs | grep -i b-0136` → empty), so there is no Task with Steps or a plan's acceptance
tests to compare against — judged instead against the card's own Description and Tests clause, as
round 1 of `B-0129` did for the same gap. `git diff --stat origin/main...HEAD` — six files:
`asf/doctor.py`, `asf/views/status.py`, `tools/install.sh`, `tests/test_doctor.py`,
`tests/test_install.py`, `tests/test_views.py`.

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | fail | The card declares no `writes:` footprint ("(none declared)"), so there is nothing to judge the diff against — recorded `fail` only because a row with nothing to compare against carries no evidence; see I1. The six touched files are exactly the pair of production modules the card's Description names (`tools/install.sh`'s scheduler step; `asf status`'s Cron row and `asf doctor`'s scheduler section) plus their own tests — nothing else in the diff. |
| every Step of the Task is implemented | pass | No plan exists, so judged against the card's own Description. "tools/install.sh verifies after its scheduler step that every clock the product declares is loaded, retries the bootstrap once, and fails the install loudly naming the missing label if it still isn't": `tools/install.sh:65-88` adds `scheduler_install_verified()`, which runs `asf scheduler install`, then reads back `asf scheduler status --product "$PRODUCT"` (pre-existing in `asf/scheduler.py:836-849`, unchanged by this diff — returns exit 1 and prints `"<label>  not loaded"` for any declared clock `loaded_jobs()` doesn't cover); on a non-zero status it retries the bootstrap once (`tools/install.sh:76`), re-reads status, and if still non-zero prints `install: NEEDS OPERATOR: clock(s) still not loaded after retrying the bootstrap: <label>` (`tools/install.sh:79-84`) before `step()` records the step as `FAILED`. "The tick of any other product (or `asf status`) flags 'clock \<label\> not loaded' as RED in the Cron row, instead of omitting it": `asf/doctor.py:637-646`'s `scheduler_rows` now computes `missing` (declared clocks absent from `loaded_labels`) and appends one `(RED, label, 'declared in products/{p}.yaml but not loaded')` row per label — `RED` is the doctor module's own severity constant, matching the card's word for the doctor row; `asf/views/status.py:299-309`'s `cron_cell` computes the same `missing` set and appends `'clock {label} not loaded'` into the Cron cell's joined text — `asf status`'s render is plain markdown text with no color layer anywhere in this module (confirmed: no `RED`/`GREEN`/ANSI marker exists in `asf/views/status.py` before or after this diff), so the card's "RED" is satisfied literally for doctor and by the cell's own text for status, consistent with how every other row in this view already signals trouble (e.g. `not_configured(...)`). |
| acceptance tests byte-identical to the plan's | pass | No plan exists to be byte-identical to, and the card's own Acceptance list is empty (`- [ ]`). The card's Description names the scenario directly: "a declared clock with a plist but no loaded job is reported not-loaded by status and doctor; install retries the bootstrap." The commit message names four tests and all four exist verbatim: `test_a_declared_clock_with_no_loaded_job_is_red_naming_the_label` (`tests/test_doctor.py:736`), `test_cron_flags_a_declared_clock_that_is_not_loaded` (`tests/test_views.py:228`), `test_an_unloaded_clock_is_retried_once_and_then_succeeds` and `test_an_unloaded_clock_still_missing_after_retry_fails_loudly_naming_it` (`tests/test_install.py:1268`, `:1280`). |
| those tests were run and are green | pass | `python3 -m unittest tests.test_doctor.TestSchedulerSection tests.test_views.StatusViewTests tests.test_install.InstallScriptTest tests.test_scheduler -v` → `Ran 217 tests` / `OK`, including all four named tests passing. |
| the Gate commands are green | fail | `python3 tools/run_tests.py` → `Ran 3360 tests in 1002.669s (105 module(s), 8 at a time)` / `FAILED (failures=2, skipped=14)`, red: `test_watch` (`test_a_tick_appended_while_watching_is_printed`) and `test_dry_run` (`test_never_touches_real_state_and_is_idempotent`, a `gate: ? modules, 0s` vs `1s` timing-string race). Neither test file is touched by this diff (`asf/watch.py`, `asf/dry_run` machinery vs. this diff's `asf/doctor.py`, `asf/views/status.py`, `tools/install.sh`). Isolated in a scratch worktree of plain `origin/main` (`5651905d9`, one commit past this branch's `55c0250` merge-base, untouched by either): `python3 -m unittest tests.test_watch tests.test_dry_run -v` there reproduces the identical `test_a_tick_appended_while_watching_is_printed` failure (`AssertionError: Lists differ: [] != ['2026-09-25T09:00:00Z TICK — record ok, harvest FAILED', ...]`) with no code from this branch present — so this red is pre-existing on trunk, not a defect this diff introduces. `bash tools/check_generic.sh` → `check_generic: clean`. `python3 -m asf.redact --unpublished` → `redact: clean`. `bash tools/check_conventions.sh` was refused by this session's sandbox ("this command requires approval"), same refusal noted in round 1 of `B-0129`; `python3 -m unittest tests.test_conventions -v` (which exercises the same script) → `Ran 30 tests in 148.474s` / `OK`. Recorded `fail` because the gate's own last line is red, per the letter of the check. |
| no secret value printed, no background process, no skipped check | pass | No secret in the diff: it changes scheduler read-back logic, a doctor row, a status cell, and their tests built from the module's own vocabulary — no token, credential, or path outside the repo; `asf.redact --unpublished` over the branch's one commit is clean. No background process left running: the full gate (`python3 tools/run_tests.py`) exceeded this tool's foreground timeout and was moved to a tracked background task by the harness itself (not started deliberately in the background); this session attached a monitor and waited on it to completion rather than proceeding without it. A scratch worktree (`.scratch-main`, `origin/main` at `5651905d9`) used to isolate the Gate failures from this diff was removed in the same turn — `git worktree list` and `git status --porcelain` are both clean of it now, apart from this review file. The 14 skips are the suite's standing count, unchanged by this diff. One check did not run literally — `bash tools/check_conventions.sh` — covered by the green unittest that exercises it, same gap as `B-0129`'s round 1. |

verdict: approved

## C

none.

## I

**I1 — the card declares no `writes:` footprint, so the scope row has nothing to judge the diff
against.** Recorded `fail` for that reason alone; the six files touched are exactly what the
card's Description calls for and nothing more. Same gap `B-0129`'s round 1 (`I1`) records.

**I2 — the Gate is red on `test_watch::test_a_tick_appended_while_watching_is_printed` and
`test_dry_run::test_never_touches_real_state_and_is_idempotent`, both pre-existing on
`origin/main` and unrelated to this diff** (verified by running both against a clean
`origin/main` scratch worktree, where `test_watch`'s failure reproduces identically with none of
this branch's code present; `test_dry_run`'s failure is a timing-string race — `0s` vs `1s` in a
duration format — that appeared once in the combined run and did not reproduce on the isolated
rerun, consistent with a flaky formatted-duration boundary rather than a defect). Flagging so the
next session to touch either module does not re-diagnose the same drift.

**I3 — `asf/doctor.py`'s and `asf/views/status.py`'s `missing` computation is duplicated
almost verbatim between the two modules** (`scheduler.label_for(product.name, c.name, cfg) for c
in declared if ... not in loaded_labels`). Fine as delivered — the two call sites differ in what
they do with `missing` (a doctor row vs. a joined cell fragment) — but a third caller would be the
signal to factor it into one `asf/scheduler.py` helper (e.g. `missing_clocks(product, loaded, cfg)`)
rather than a third copy.
