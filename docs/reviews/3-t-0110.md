# Review — T-0110, round 3

Diff reviewed: `e2900310a` (`origin/worker/T-0110`'s current head) against `904ffbf9f` — the
branch's merge-base with `origin/main` at the time this round started (confirmed by
`git merge-base origin/main HEAD` before `origin/main` advanced further, mid-review, with large
unrelated changes of its own; the branch has not moved past that base, so `git diff 904ffbf9f
HEAD` is still the branch's own work, nothing else).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff --stat 904ffbf9f HEAD` touches `asf/workers/report.py`, `tests/test_report.py`, `asf/briefs/facts.py`, `tests/test_brief_facts.py`, `tests/test_workers.py`, `tests/test_lifecycle.py`, `tests/test_tick_steps.py`, `asf/workers/runtime.py`, `tests/test_widen.py` (Task 1's `writes:` plus round 1's two required additions, both already accepted by round 2) plus `asf/workers/health.py` and `asf/workers/lifecycle.py` (source) — the two files round 2's own C list required (`docs/reviews/2-t-0110.md`: "retype off `report_mod.parse(...)`" for `health.py:229-230` and `lifecycle.py:598`), the same reasoning round 2 itself used to accept `runtime.py`/`test_widen.py` against round 1's C list. `docs/reviews/1-t-0110.md` and `docs/reviews/2-t-0110.md` are this item's own review files, committed onto the same lane branch per this repo's convention |
| every Step of the Task is implemented | pass | Steps 1–10 of Task 1 (`docs/plans/f-0025.md:95-115`) all land, as rounds 1 and 2 already found and this round reconfirms: `COMMON`/`_CHECK`/`_check` (`asf/workers/report.py:18-139`), `KIND_FIELDS` for all fourteen current kinds (`:47-115`), `schema`/`fence`/`typed` (`:142-208`), the five readers `check`/`failure`/`ruling`/`ruling_fields`/`summary` (`:211-275`), `render`/`contract` (`:302-352`), `facts.last_report` re-based on `typed(text, None)` (`asf/briefs/facts.py:134-149`) |
| acceptance tests byte-identical to the plan's | pass | `tests/test_report.py` is unchanged since round 1 (this round's only commit, `e2900310a`, touches `asf/workers/health.py` and `asf/workers/lifecycle.py` alone). Every class/case Task 1's Acceptance names (`docs/plans/f-0025.md:133-185`) is present verbatim: `SchemaTest` (`:23-46`), `ParseTest` (`:48-126`), `RenderTest` (`:128-148`), `ContractTest` (`:150-175`), `ReadersTest` (`:177-222`) |
| those tests were run and are green | pass | `python3 -m unittest tests.test_report -v`: `Ran 37 tests in 0.107s` / `OK` |
| the Gate commands are green | fail | `python3 -m unittest tests.test_brief_facts tests.test_workers tests.test_lifecycle tests.test_tick_steps -v`: `Ran 307 tests in 299.507s` / `OK`. But `python3 -m unittest discover -s tests -t .` (Task 1's Gate line 3, run in full — not truncated through `tail -3`, which is exactly what let this past rounds 1 and 2): `Ran 3478 tests in 1885.438s` / `FAILED (failures=7, errors=1, skipped=14)`. All 8 failures trace to the two files round 2 required this round to fix: `tests.test_lane.FeederAndOutcomes.test_b0097_a_network_blip_is_retried_and_a_hook_refusal_carries_its_output` errors `TypeError: cannot unpack non-iterable NoneType object` at `health.push_retry`; `tests.test_e2e_lane.RefusedPushFF.test_fault8_refused_push_carries_the_hooks_output` fails the same way (detail text now empty); and six `…overrules…` tests across `tests.test_lane.LaneRepo`/`Orphans` fail `'BACK' != 'GATE'` — all from `lifecycle.overruling`. `bash tools/check_generic.sh`: `check_generic: clean` |
| no secret value printed, no background process, no skipped check | pass | No secret literals in the diff; every long-running run (`discover`, the four-module run) was started and waited on to completion in this session, none abandoned in the background. `bash tools/check_conventions.sh` itself was refused by this session's sandbox on every attempt (`This command requires approval`, reproducible, no operator present to grant it); its exact logic — the same patterns file, the same `python3 -m asf.conventions --forbidden` generation, the same `git ls-files asf/*.py asf/**/*.py` scan — was run directly in Python instead and returned clean. Disclosed here and in this report's `left out:` line rather than silently marked done |

verdict: changes requested

## C — must fix before landing

- `asf/workers/health.py:229-236` (`push_retry`) and `asf/workers/lifecycle.py:595-598`
  (`overruling`) — both now call `report_mod.typed(text, None)` and, on `ReportError` (no
  ```json asf-report``` fence), fall back to an **empty** `{}` with no prose reading at all. Task
  1's own module docstring is explicit that "nothing yet *demands* a report" at this point in the
  plan (`docs/plans/f-0025.md:62`) — every result today, and every fixture in `tests/test_lane.py`
  and `tests/test_e2e_lane.py` that exercises these two functions (`tests/test_lane.py:502-527`'s
  `_adjudicated` helper, `tests/test_lane.py:1868-1881`'s `test_b0097…`, `tests/test_e2e_lane.py`'s
  fault-8 fixture), is **prose-only**. Silently treating an unfenced prose report as `{}` instead
  of falling back to it — the way `report.py`'s own `footprint_claim`/`_prose` still do, on
  purpose, for exactly this reason (`asf/workers/report.py:355`'s own comment: "still read off the
  prose block; it is not the typed report's business") — makes `push_retry` return `None` where it
  used to classify a hook refusal or network blip (confirmed: `test_b0097…`'s
  `cls, detail = health.push_retry(...)` now raises `TypeError: cannot unpack non-iterable
  NoneType object`), and makes `overruling` return `None` for every adjudicate ruling on a
  prose-only report — which is every ruling today, on the exact B-1377 loop `overruling`'s own
  docstring says it exists to stop (confirmed: six `…overrules…` tests in `tests.test_lane` now
  see `'BACK' != 'GATE'`, i.e. the review hold is never lifted). This is a live regression on
  `asf/harvest/lane.py:1160`'s production call to `overruling`, not a test-only one. Fix: give
  both functions the same prose fallback `footprint_claim` already keeps — read the typed report
  when a fence is present, and fall back to the existing prose read (`report_mod._prose`, or an
  equivalent) when it is not — rather than treating "no fence" as "no data".
- Once fixed, run `python3 -m unittest discover -s tests -t .` **in full** (not through `tail -3`
  or `tail -N`, which is what let this regression through both the round-2 fix's own check and, on
  this branch, would have let it through again) and paste its real last line in the report.

## I — worth doing, not blocking

- `asf/briefs/facts.py:141-149` (`last_report`) — unchanged since round 1's I: `sha` is still not
  in `REPORT_FIELDS`, so a brief no longer carries which sha last landed.

### Missed in round 1

(none — this round's finding is in code that did not exist until the round-2 fix commit
`e2900310a`, landed after round 2's own review; round 1's diff never touched `health.py` or
`lifecycle.py`)
