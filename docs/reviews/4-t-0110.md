# Review — T-0110, round 4

No writer's report was found for this round: the branch's head, `85c822ec`, is a `wip(worker/T-0110):
the session ended with this uncommitted — committed by the factory (B-0094)` commit, i.e. the
writer's session ended without printing a REPORT and the factory committed what was on disk. The
diff it carries is exactly round 3's required fix (`asf/workers/health.py`, `asf/workers/lifecycle.py`,
`asf/workers/report.py`), so this review reads that diff and the plan in its place.

Diff reviewed: `git diff 954ee3e5f HEAD` (`954ee3e5f` is `origin/main` at the moment this branch's
merge-base was cut, confirmed by `git merge-base origin/main HEAD`; `origin/main` has since
advanced to `fe58c197` with unrelated work — a `git diff origin/main HEAD` taken after that
advance shows files this branch never touched, `CHANGELOG.md`, `asf/evidence/evidence.py`,
`docs/reviews/1-t-0198.md`, `tests/test_evidence.py` — the same situation round 3 hit and the same
fix: diff against the merge-base, not the now-moved tip).

| check | result | evidence |
| --- | --- | --- |
| scope: the diff stays inside `writes:` | pass | `git diff --stat 954ee3e5f HEAD` touches exactly the 14 files round 3 already found in scope: `asf/workers/report.py`, `tests/test_report.py`, `asf/briefs/facts.py`, `tests/test_brief_facts.py`, `tests/test_workers.py`, `tests/test_lifecycle.py`, `tests/test_tick_steps.py`, `asf/workers/runtime.py`, `tests/test_widen.py` (Task 1's `writes:` plus round 1's two accepted additions), `asf/workers/health.py`, `asf/workers/lifecycle.py` (round 2's two accepted additions), and `docs/reviews/{1,2,3}-t-0110.md` (this item's own review files, committed on the lane branch per this repo's convention). No new file entered scope this round |
| every Step of the Task is implemented | pass | Unchanged from round 3: `COMMON`/`_CHECK`/`_check` (`asf/workers/report.py:18-139`), `KIND_FIELDS` for all fourteen kinds (`:47-115`), `schema`/`fence`/`typed` (`:142-208`), the five readers `check`/`failure`/`ruling`/`ruling_fields`/`summary` (`:211-275`), `render`/`contract` (`:302-352`), `facts.last_report` re-based on `typed(text, None)` (`asf/briefs/facts.py:202-228`). This round's only change to `report.py` is outside the ten Steps: the prose-fallback section (`:355-417`) `push_retry`/`overruling` now share |
| acceptance tests byte-identical to the plan's | pass | `tests/test_report.py` is byte-for-byte unchanged since round 1: `git diff 536a93f9d..HEAD -- tests/test_report.py` is empty. `python3 -m unittest tests.test_report -v`: `Ran 37 tests in 0.028s` / `OK` |
| those tests were run and are green | pass (module-level) / fail (whole suite) | `python3 -m unittest tests.test_report -v`: `OK` (37 tests). `python3 -m unittest tests.test_brief_facts tests.test_workers tests.test_lifecycle tests.test_tick_steps -v`: `Ran 321 tests in 395.916s` / `OK`. But see the Gate row: the named tests pass, the suite they sit inside does not |
| the Gate commands are green | fail | `python3 -m unittest tests.test_report -v`: `OK`. `python3 -m unittest tests.test_brief_facts tests.test_workers tests.test_lifecycle tests.test_tick_steps -v`: `OK`. `python3 -m unittest discover -s tests -t .` **run in full**: `Ran 3595 tests in 2026.488s` / `FAILED (failures=6, skipped=14)` — six `tests.test_lane.LaneRepo`/`Orphans` `…overrules…` tests, the exact function round 3's fix targeted, regressed again: `test_an_adjudicate_ruling_on_the_unchanged_head_overrules_the_review`, `test_a_ruling_naming_the_code_commit_under_the_review_commit_overrules_it`, `test_a_ruling_launched_on_the_head_it_left_alone_overrules_whatever_sha_it_names`, each `'BACK' != 'GATE'`. `bash tools/check_generic.sh`: `check_generic: clean`. `bash tools/check_conventions.sh` was refused by this session's sandbox (`This command requires approval`, same as round 3); its exact logic was run directly in Python instead (patterns file + `python3 -m asf.conventions --forbidden` + the same `git ls-files` scan) and printed `clean` |
| no secret value printed, no background process, no skipped check | pass | No secret literals in the diff (`git diff 954ee3e5f HEAD \| grep -i` for key/secret/password/token-as-credential/private-key markers matches only `FENCE_TOKEN` and doc prose). `discover` exceeded this session's 30-minute foreground limit and was moved to background by the harness itself, not started there by choice — it was watched to its own completion and its real last line is quoted above, not truncated. `check_conventions.sh`'s sandbox refusal and the Python stand-in for it are disclosed here, not silently marked done |

verdict: changes requested

## C — must fix before landing

- `asf/workers/report.py:361-363` (`FIELD_RE`) — the prose fallback's field regex recognizes
  `needs writes` but not the bare `writes` field the adjudicate prose TAIL also emits
  (`asf/briefs/build.py:96-98`, the same TAIL `ruling_fields`'s docstring and F-0090 stand on).
  `_prose`'s line loop keys a line to the *last matched* field and appends any line that matches no
  key onto that field's value (`asf/workers/report.py:383-388`); with `writes` unrecognized, a
  prose report's `writes: a.txt` line after `blocked_on: none` is swallowed into `blocked_on`,
  turning it into `'none\nwrites: a.txt'` — a value `_claim` no longer recognizes as "no claim"
  (confirmed: `report_mod._claim(report_mod._prose(text)['blocked_on'])` returns
  `'none\nwrites: a.txt'`, not `None`, for exactly the fixture `tests/test_lane.py`'s
  `_adjudicated` helper builds). `asf/workers/lifecycle.py:608-618`'s prose branch of `overruling`
  reads that corrupted `blocked_on` as a live claim and refuses to overrule
  (`if status != 'done' or not ruling_text or fields['blocked_on'] or fields['superseded_by']:
  return None`) — the review hold is never lifted on a finished, on-head, unblocked ruling. This is
  the live regression `python3 -m unittest discover` caught: six `tests.test_lane` `…overrules…`
  tests now see `'BACK' != 'GATE'`, the same B-1377 loop this round's own fix was written to close.
  Fix: add `writes` to `FIELD_RE`'s alternation (ordered after `needs writes` so the longer phrase
  still matches first), so the prose fallback reads `writes:` as its own field the same way the
  typed branch already does via `ruling_fields`.
- Once fixed, run `python3 -m unittest discover -s tests -t .` **in full** and paste its real last
  line in the report — round 3 already named this the exact truncation that let a regression here
  through twice.

## I — worth doing, not blocking

- `asf/briefs/facts.py:141-149`-equivalent `last_report` — unchanged since round 1's I: `sha` is
  still not in `REPORT_FIELDS`, so a brief no longer carries which sha last landed.
- No writer's report accompanied this round's diff (see the note above the table). The fix itself
  matches round 3's C exactly, so this review could proceed without it, but a session that ends on
  an uncommitted wip commit with no REPORT is itself the failure mode `pushed: no` exists to
  surface — worth asking why this session did not reach its own end.

### Missed in round 1

(none — this round's finding is in code that did not exist until the round-3-fix commit
`4a816528b`, landed after round 3's own review; round 1's diff never touched the prose-fallback
section of `report.py`)
