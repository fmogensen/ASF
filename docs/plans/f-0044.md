# Plan F-0044 — the scorecard says what shipped before it says what it cost

Epic: E-0001 — ASF 0.1 — a working factory. Spec: `docs/specs/f-0044.md` (approved). Branch: `plan/F-0044`.

Five Tasks, cut along the spec's own §2 sections and its six fenced acceptance blocks. Task 1 is
the durable event: `ON_PROD_EVENT`, `write_on_prod_event` and the one line in `ingest`'s write loop
that fires on the transition into `on-prod`. Task 2 is the two pure readers in `metrics` — `shipped`
(the event is the record, `stage_since` is the fallback) and `throughput` (the count, the priced
count and the whole-life mean), plus `record_throughput`, which is the pair of windows over one
record. Task 3 is `throughput_records`: which records are read, and which are named as unread.
Task 4 is the section itself — `throughput_block`, `render_daily`'s new first section and
`cmd_rollup`'s resolved list and `features:` line. Task 5 is the edge cases of §3.6: a quiet week,
a single record and no product config.

Tasks 2, 3, 4 and 5 all write `asf/metrics/metrics.py` and `tests/test_metrics.py`, so the feeder
runs them one after the other whatever this plan says; the `after:` lines below fix that order to
the one their dependencies need. Task 2 genuinely needs Task 1 landed — `shipped` reads
`ingest.ON_PROD_EVENT`, and a Task 2 that landed first would import a name that does not exist.
So the chain is total and is honest about it.

Wave order: **1 → 2 → 3 → 4 → 5**.

## Decisions

| # | Question | Decided |
|---|---|---|
| P1 | How the work is cut | By the spec's own sections and its six §3 fences: §2.1/§3.1 the event (Task 1), §2.2/§3.2–§3.3 the two readers (Task 2), §2.3/§3.4 the record list (Task 3), §2.4/§3.5 the section and the two callers (Task 4), §3.6 the edges (Task 5). Every Task's Acceptance is exactly one of those fences, copied byte for byte, and every Task's Gate additionally runs the whole suite. |
| P2 | Task card ids | Not minted here. `asf.record.plan_tasks.mint_plan_tasks` turns each `### Task N:` heading of the landed plan into a card with an id from `asf.record.ids.mint_id` (`asf/record/plan_tasks.py:48,81`), so this plan's headings are the parser's ordinals `1…5`. The session's `BACKLOG_ID_RANGE` (`S:19850-19899,T:19850-19899,B:19850-19899`) is therefore unused by this plan; no id is invented. |
| P3 | Which Story is which | The session's brief said the Feature's Stories are "(none yet)" — the record does not hold them under this Feature yet. The spec's frontmatter does: `stories: [S-18900 … S-18905]`, six ids for the six bare `- S:` bullets of its `## Stories` section. The mapping is positional, first bullet to first id: S-18900 the `ingest` event, S-18901 `shipped`, S-18902 `throughput`, S-18903 `throughput_records`, S-18904 the headline and the per-product table, S-18905 the quiet week. `plan_tasks.stories_of` keeps only the ids the record holds, so a Task card minted before these Stories are in the record simply carries none — the `stories:` lines below are still the plan's coverage statement, and are what a groom reconciles against. See P18: as the parser stands it would carry none even after they are in the record. |
| P4 | **`EXPECTED_DAILY` and §2.4's printed block are two different fixtures, and §3.5 asks for both** | `Rollup.test_daily_tables_match_the_expected_md` calls `metrics.render_daily(self.root, DAY, self.items)` (`tests/test_metrics.py:370`) — no `records` argument. By §2.4 that is the `records=None` branch: one row labelled `this record`, a `Total` row equal to it. So `EXPECTED_DAILY` gains *that* section, not the `sample`/`demo`/`extra` table §2.4 prints. §2.4's block is the fixture of the new `ScorecardFirstLineTests`, which passes `records` explicitly. §3.5's two clauses — "renders the section exactly as printed there" and "the updated `EXPECTED_DAILY` keeps both tables unchanged below the new section" — are therefore about two different tests, and Task 4 writes both. |
| P5 | **How §2.4's numbers are constructible while `## Waste` and `## Cost per Feature (7 days)` stay byte-identical** | §3.5 requires `sample` to ship 3 Features costing $37.20 together, while the fixture's cost table stays `F-0001 … 3.75`, `F-0002 … —`, `**All** … 3.75` and `generated:` stays `metrics/ci (3), metrics/sessions (4), metrics/ticks (2)`. The two are compatible because the section's cost is **whole-life** (`read_stream(root, 'ci')`, no `days`) and the table's is the 7-day slice (`read_stream(root, 'ci', week)`): the extra spend goes on stream days **before `2026-09-15`**, outside `days_back(DAY, 7)`. The third shipped Feature's spend is entirely pre-window, so `cost_table` gives it no row at all (a row needs `sessions or runner_min or usd is not None` in the window, `asf/metrics/metrics.py:531`), and `generated:` counts only `DAY`'s three streams. This is the same mechanism §3.3's last clause tests; Task 4 builds the fixture that way, and Task 2 proves the mechanism first. |
| P6 | **`render_daily` gains a `rows` keyword the spec did not name, so the rollup reads every product's streams once, not twice** | §4 puts `features: 3 on prod this week, $12.40 each (last week 3, $19.55)` on `cmd_rollup`'s stdout **before** `cost:`, and `render_daily` returns text — so `cmd_rollup` cannot read the Total out of it without parsing markdown. Computing the per-record loop a second time means re-reading every product's whole `metrics/ci` and `metrics/sessions` (`record_throughput` reads them undated, by §2.2). Instead the loop lives in one function, `throughput_rows(root, day, items, records)` → `[(label, week, prev)]`, and `render_daily(root, day, items, conv=None, records=None, skipped=(), rows=None)` computes it only when `rows` is None. `cmd_rollup` computes it once, prints its line from `throughput_total(rows)`, and hands the same list on. Every fenced call still works unchanged: §2.4's `render_daily(root, day, items, conv, records, skipped)` is positional through `skipped`, §3.6's `render_daily(root, day, items)` is the `records=None` branch, and `tests/test_metrics.py:355,370,373` are untouched. |
| P7 | **`append_event` cannot write the `events` stream, which is why §2.1 writes the line by hand** | `append_event` calls `natural_key(stream, ev)` (`asf/metrics/metrics.py:292,261-267`), whose fallthrough is `return (ev['tick'],)` — an `events` line has no `tick`, so it raises `KeyError`. `write_on_prod_event` therefore opens the day's file itself, exactly as `tick.Context.event` does (`asf/tick/tick.py:162-171`): `os.makedirs(..., exist_ok=True)`, append mode, `json.dumps(..., sort_keys=True, ensure_ascii=False)`, one `\n`. Its own idempotence check (does this day's file already hold this kind for this item) replaces `append_event`'s. |
| P8 | **`metrics/events` needs no folder, no schema and no `STREAMS` entry** | It is already created by `asf init` (`asf/init.py:28-29`, `STREAM_FOLDERS` names `metrics/events`), `STREAMS` is `('ci', 'sessions', 'ticks')` (`:39`) and `SCHEMAS` (`:114`) has no `events` key — so `validate` is never reached for it and a new `kind` needs no schema change. `read_stream(root, 'events')` over a record with no such directory globs to `[]` and returns `[]` (`:280-290`), which is also why §3.6's empty record raises nothing. §1.1 confirmed against the tree. |
| P9 | **`intake_latency_rows` ignores the new kind by construction, and §3.1's last clause is a structural claim** | The dispatch is `if kind == 'intake' / elif 'groom_answer' / elif 'launch'` (`asf/metrics/metrics.py:425-440`) and `intake_n = sum(1 for ev in events if ev.get('kind') == 'intake')` (`:441`). A `feature-on-prod` line has an `item` and a `ts` but matches no branch and is not counted, so both rows and both notes are identical with and without it. The test §3.1 asks for asserts exactly that equality over the same event list with and without the new lines. |
| P10 | **`env.ASF_HOME` is bound at import, so §3.4's "`ASF_HOME` pointed at a path that does not exist" is an attribute patch** | `asf/env.py:18` reads `os.environ` once at import time; `tests/__init__.py` sets the variable before `asf` is imported (B-0043). Re-setting `os.environ['ASF_HOME']` inside a test would change nothing. `schema._all_products` reads `env.ASF_HOME` at call time (`asf/schema.py:230-234`), so `mock.patch.object(env, 'ASF_HOME', <temp dir>)` is what §3.4's fixture uses — for the three product files it writes and for the missing-home case alike. No test unpins the suite's home. |
| P11 | **`throughput_records` enumerates with `schema._all_products()` and adds no new public surface** | It is the one enumerator of `~/.ASF/products/*.yaml` in the tree (`asf/schema.py:230`), and `asf/metrics/metrics.py` already imports `asf.record.index`, which imports `asf.schema` — so `from asf import schema` at the top of `metrics` adds no module and no cycle (`asf/schema.py` imports only `env` at module level; `frontmatter`, `load_items` and `shadow` are function-local, `:85,86,209`). Promoting it to a public `schema.all_products()` would put `asf/schema.py` in a Task's `writes:` for a rename the spec did not ask for. The private call is made in one place, with a comment naming it as the product list. |
| P12 | **Reading another product's `backlog_dir` is a read of the operator's checkout, and is not a B-0013 breach** | B-0013 is about the tick never *touching* the operator's backlog; `throughput_records` returns a path and `record_throughput` opens `index.json` and `metrics/*/**.jsonl` under it for reading only. Nothing is written, no lock is taken, and a missing or unreadable one is a skipped name (§1.4), never an exception. That another product's checkout may sit behind its own record clone is the spec's stated choice ("Other products are read from `backlog_dir`, this one from `root`") and is not reopened here. |
| P13 | **Every live rollup enumerates the products; the bare-checkout rollup reads only `root`** | `cmd_rollup` is called from the tick with `product=product.name` (`asf/tick/tick.py:109`, `asf/tick/step_daily.py:172`), so `throughput_records` gets a name and walks `~/.ASF/products`. Run by hand over a checkout with no product config, `_resolve_product` already raises `env.ConfigError` and falls back to `DEFAULTS` (`asf/metrics/metrics.py:1211-1213`); the same `getattr(args, 'product', None) or None` yields None and the list is this record alone. `tests/test_tick.py:716` patches `cmd_rollup` out entirely, so no tick test sees either path. |
| P14 | Why the chain is total | Tasks 2–5 all write `asf/metrics/metrics.py` and `tests/test_metrics.py`; two Tasks whose `writes:` intersect never run together, so the feeder serialises them regardless of `after:`. Task 2 additionally *needs* Task 1: `shipped` reads `ingest.ON_PROD_EVENT` (§2.2), and landing Task 2 first would land an `ImportError`. Splitting the constant out to break the chain would move it out of the module §1.2 puts it in, for no wall-clock gain. |
| P15 | The spec's own rendering quirks are copied, not improved | `_delta_int` renders `+0` for no change (§2.4 says so in words: "never `0`"), and `_delta_money` as fenced renders `+$0.00` when the two means are equal — `'+' if now_ >= then else '-'`. §3.6's quiet-week headline reads `0 features on production this week, — per feature — last week 0, — · Δ features +0 · Δ cost —`, em dash against em dash. Every one of these is fenced in the approved spec and is reproduced byte for byte; no Task tidies the wording. |
| P16 | `python3` everywhere | Every fence in the spec — §3's blocks and the `## Stories` bullets alike — is written `python3`, and §3's preamble is `python3 tools/run_tests.py`. Every **Acceptance** block below copies the spec's fence exactly; every **Gate** uses `python3` and the repo's four checks. |
| P17 | What no Task touches | `asf/evidence/evidence.py` (`feature_stage`, `feature_state`, `on_prod_for_stage` are closed, §1.3), `asf/init.py`, `asf/record/core.py`, `asf/record/index.py`, `STREAMS`, `SCHEMAS`, `KINDS`, `scorecard_rows`, `cost_table`, `write_costs`, `write_release` and every release path. No waste row is added or moved — F-0101 is editing that table (§1.3) and this plan's diff must not meet it. No item field is added: `stage`, `stage_since`, `cost` and `spend_usd` keep their writers. |
| P18 | **`plan_tasks.STORY_ID_RE` cannot see a five-digit Story id, so no Task card minted from this plan will carry its Stories — and this plan does not fix it** | `STORY_ID_RE = re.compile(r'\bS-\d{4}\b')` (`asf/record/plan_tasks.py:26`) matches exactly four digits: against `S-18900` the `\d{4}` consumes `1890` and the trailing `\b` fails between `0` and `0`, so `stories_of` returns `[]`. Every id this Feature uses is five digits (the spec's `S-18900 … S-18905`, and this session's `BACKLOG_ID_RANGE` mints `S:19850-19899`), and it is not local: 208 of the 313 distinct Story ids under `docs/specs/` are five digits. The consequence here is that each `### Task N:` heading below becomes a Task card with an empty `stories:` list — the card still builds, still carries `writes:` and `after:`, and the feeder still launches it, but the Story link is lost. Fixing the regex means editing `asf/record/plan_tasks.py`, which is neither in the spec's §1.2 nor in any Story of this Feature; widening a plan to carry an unrelated parser fix is exactly what the brief forbids. It is raised as `NEEDS OPERATOR` in this session's report instead, and the `stories:` lines below stay the plan's own coverage statement. |

P4, P5, P6, P7, P10 and P11 are the places the spec's design meets the tree as it stands; each is
named and each is closed inside the Task that causes it. P6 is the only addition to a signature the
spec fenced, and it is additive and defaulted — every call the spec and the suite already write
keeps its meaning. P18 is the one defect this plan finds and declines to fix, because fixing it
would put a file no Story of this Feature owns into a Task's `writes:`; it is the session's
`NEEDS OPERATOR` instead.

---

### Task 1: the moment a Feature reaches production is written down
stories: S-18900
writes: asf/record/ingest.py, tests/test_ingest.py
after: none

§2.1. `ingest` already has the old stage, the new stage and `now` in one loop; it writes none of it
anywhere a report can read. This Task adds the kind, the writer and the one condition that fires on
the transition into `on-prod`. Nothing reads the stream yet — Task 2 does.

**Files**

- `asf/record/ingest.py` — `ON_PROD_EVENT = 'feature-on-prod'` and `write_on_prod_event(root, iid,
  from_stage, now)` beside `MACHINE_KEY_ORDER` (`:19-21`); the transition test inside the write
  loop (`:440-462`), after `frontmatter.write_machine`; `import json` and `import os` at the top
  (the module has neither today, `:8-16`).
- `tests/test_ingest.py` — `FeatureOnProdEventTests` (new), built on the fixture shape of
  `ProductionIsTheProductsOwn` (`:63-125`), which already drives `ingest` with a mocked
  `evidence.load` carrying `prod_sha` and merged shas.

**Steps**

1. `ON_PROD_EVENT = 'feature-on-prod'` beside `MACHINE_KEY_ORDER`, with §2.1's comment: the
   `metrics/events` kind written when a Feature's stage enters `on-prod`, the durable record read
   by the rollup's first line (F-0044).
2. `write_on_prod_event(root, iid, from_stage, now)`: the path is
   `<root>/metrics/events/<now[:10]>.jsonl`. Read the file if it exists and return False when any
   line already has `kind == ON_PROD_EVENT` and `item == iid` — that is the idempotence the record
   clone needs, since it is reset and re-derived every cycle. Otherwise `os.makedirs(dirname,
   exist_ok=True)`, append `json.dumps({'from': from_stage or '', 'item': iid, 'kind':
   ON_PROD_EVENT, 'ts': now}, sort_keys=True, ensure_ascii=False) + '\n'`, return True. A line that
   will not parse as JSON is skipped, not raised on — the stream is unvalidated (§1.1) and a
   rollup must not be the thing that fails.
3. In the write loop, `old = machine.get('stage')` is read from the `machine` dict
   `frontmatter.split_machine` already returns (`:446`), **before** `_ingest_fields` is called, and
   after the `write_machine` / History block:

   ```python
   if type_ == 'feature' and stage_val.get(iid) == 'on-prod' and old != 'on-prod':
       write_on_prod_event(root, iid, old, now)
   ```

   It sits inside the `if ordered is None: continue` guard, which is correct: a Feature already at
   `on-prod` changes nothing and `_ingest_fields` returns `None` for it anyway. `'on-prod'` is
   written as the literal the ladder returns (`asf/evidence/evidence.py`'s `feature_stage`); the
   card's prose says "on-production" and the record says `on-prod` (§1.1).
4. The docstring of `write_on_prod_event` says why it does not go through `append_event`: P7 —
   `natural_key` has no `events` branch and raises `KeyError` on any line without a `tick`.
5. Nothing else in the loop moves. `_ingest_fields`, `append_history_lines`, the `## History` line
   and `stage_since` are untouched, so the event is purely additive.
6. `FeatureOnProdEventTests`: a record whose `F-0001` has every Task Closed and an `ev` carrying
   `prod_sha` plus the merged shas — one `asf ingest` writes exactly one line to
   `metrics/events/<today>.jsonl`, `{"from": "landed", "item": "F-0001", "kind":
   "feature-on-prod", "ts": …}` with `list(obj) == sorted(obj)` and `ts` matching `TS_RE`. A second
   `ingest` the same day adds none. A Feature reaching only `landed` (`prod_sha` absent) writes
   none. A Feature whose machine block already reads `stage: on-prod` writes none. A Task and a Bug
   that reach prod write none. The Feature's `## History` lines and its `stage_since` equal what
   the same fixture produces on `main` today.
7. The last clause of §3.1 in the same class (P9): build a list of `intake` / `groom_answer` /
   `launch` events, assert `metrics.intake_latency_rows(evs)`, then append the
   `feature-on-prod` lines and assert the two rows — values and notes — are equal.

**Gate**

```sh
python3 -m unittest tests.test_ingest -v
python3 -m unittest tests.test_metrics tests.test_record -v
python3 tools/run_tests.py
bash tools/check_generic.sh && bash tools/check_conventions.sh && python3 -m asf.cli check
```

**Acceptance**

```bash
python3 -m unittest -v tests.test_ingest.FeatureOnProdEventTests
```

Covers §3.1 whole: one line per transition and only on the transition, keys sorted and the `ts`
well-formed, `landed` without a deployment writing nothing, a non-Feature writing nothing, the
History line and `stage_since` unchanged, and `intake_latency_rows` unmoved by the new kind.

```bash
cd "$(git rev-parse --show-toplevel)"
python3 tools/run_tests.py
```

---

### Task 2: what shipped, and what it cost over its whole life
stories: S-18901, S-18902
writes: asf/metrics/metrics.py, tests/test_metrics.py
after: Task 1

§2.2. Three functions and one constant, all pure or nearly: `shipped` answers which Features reached
production in a window, `throughput` answers how many and at what mean cost, and
`record_throughput` is the pair of windows over one record. Nothing renders yet — Task 4 does.

**Files**

- `asf/metrics/metrics.py` — `ON_PROD_STAGE = 'on-prod'` beside `STREAMS`/`KINDS` (`:39-40`);
  `shipped`, `throughput`, `record_throughput` in the rollup section, after `subtree_cost`
  (`:375-387`) and before `esc` (`:389`); `from asf.record.ingest import ON_PROD_EVENT` beside the
  existing `asf.record` imports (`:34-36`).
- `tests/test_metrics.py` — `ShippedTests` and `ThroughputTests` (new).

**Steps**

1. `ON_PROD_STAGE = 'on-prod'` with the comment that it is the token `evidence.feature_stage`
   returns, not the card's prose "on-production" (§1.1).
2. `from asf.record.ingest import ON_PROD_EVENT` at module level — one owner for the kind string
   (verified acyclic: `asf.record.ingest` pulls no `asf.metrics` module).
3. `shipped(events, items, days)` exactly as §2.2 specifies, in three passes and with no clock:
   `seen = {}` over **every** event given, not the window's — `ev.get('kind') == ON_PROD_EVENT`,
   keep the newest `ts` per `item`, value `ts[:10]`; then for each Feature in `items` with
   `stage == ON_PROD_STAGE` and a truthy `stage_since`, `seen.setdefault(fid, stage_since[:10])`;
   then return `{fid: d for fid, d in seen.items() if d in set(days) and items.get(fid, {}).get('type') == 'feature'}`.
   The docstring says why the first pass is over the whole stream: an item an older event names
   must never fall back to `stage_since`, or it lands in both windows.
4. `throughput(items, costs, ids)` → `{'features', 'priced', 'usd', 'mean'}`. For each id,
   `subtree_cost(items, costs, iid)['usd']`; `features` counts every id, `priced` and `usd` only
   those whose `usd is not None`, `mean = usd / priced` when `priced` else None, and `usd` is None
   when `priced` is zero — never `0.0`, which would render `$0.00` and read as free work (§1.4).
   `max(1, …)` is not used; the `priced` guard is the one that keeps the division safe.
5. `record_throughput(root, items, day)` → `(week, prev, this_wk, prev_wk)` where
   `this_wk = days_back(day, 7)` and `prev_wk = days_back(day, 14)[:7]`. It reads
   `read_stream(root, 'events')`, `read_stream(root, 'ci')` and `read_stream(root, 'sessions')`
   **undated** — the cost is whole-life (§1.4) — builds `costs = compute_costs(ci, sessions)` once,
   calls `shipped` twice over the one event list, and `throughput` twice. The docstring names the
   whole-stream read as deliberate and points at P5's consequence for the fixture.
6. `ShippedTests`: the nine clauses of §3.2, each its own assertion — `F-0001`'s event in this
   window and not the previous; `F-0002`'s in the previous only; `F-0003` at `on-prod` with a
   `stage_since` and no event falling back into this window; `F-0004` at `on-prod` with a
   `stage_since` in this window **and** an older event landing in the previous window only and
   never both; two events for one id resolving to the newer day; a `T-0001` with the kind excluded;
   an id `items` does not name excluded; `stage: landed` with a windowed `stage_since` excluded;
   `stage: on-prod` with no `stage_since` excluded.
7. `ThroughputTests`: §3.3's five clauses — three priced ids → `{'features': 3, 'priced': 3,
   'usd': 37.20, 'mean': 12.40}`; a fourth unpriced → `features` 4 and the rest unchanged; all four
   unpriced → `mean` None, `usd` None, `priced` 0; no ids → zeros, `mean` None, no
   `ZeroDivisionError`; a Feature's cost summing its Stories, Tasks and Bugs. The last clause is
   the whole-life one and is P5's mechanism proved in isolation: the same record read with 7 days
   of streams and with 30 gives the same `features` and a larger `usd`.

**Gate**

```sh
python3 -m unittest tests.test_metrics -v
python3 tools/run_tests.py
bash tools/check_generic.sh && bash tools/check_conventions.sh && python3 -m asf.cli check
```

**Acceptance**

```bash
python3 -m unittest -v tests.test_metrics.ShippedTests
```

```bash
python3 -m unittest -v tests.test_metrics.ThroughputTests
```

Covers §3.2 and §3.3: the event is the record and `stage_since` only fills for an item no event
names; the event wins even when it is older and even when it falls outside the window asked for;
only Features count; and the mean names what it could not price instead of pricing it at zero.

```bash
cd "$(git rev-parse --show-toplevel)"
python3 tools/run_tests.py
```

---

### Task 3: which records are read, and which are named as unread
stories: S-18903
writes: asf/metrics/metrics.py, tests/test_metrics.py
after: Task 2

§2.3. One function that turns "this record, plus every other product" into a list of
`(label, root)` pairs and a list of names that could not be read. It opens nothing under
`~/.ASF` at all when no product name is given, which is what keeps every existing caller and the
pinned test home honest (B-0043).

**Files**

- `asf/metrics/metrics.py` — `throughput_records(root, product_name=None)` after
  `record_throughput`; `from asf import schema` beside `from asf import env` (`:34`).
- `tests/test_metrics.py` — `ThroughputRecordsTests` (new).

**Steps**

1. `from asf import schema` at module level (P11: no new module, no cycle — `asf.record.index`
   already pulls it).
2. `throughput_records(root, product_name=None)`: when `product_name` is falsy, return
   `([('this record', root)], [])` immediately — **before** any `env.ASF_HOME` access, so a caller
   that did not ask reads nothing outside `root`. Otherwise start the list with
   `(product_name, root)` — this record is always read from `root`, never from its own
   `backlog_dir` (§1.4), because `cmd_rollup` is writing the file into it and the two must agree.
3. Then, for each `name` in `schema._all_products()` sorted and skipping `product_name`: load it
   with `env.load_product(name)` inside a `try`, catching `env.ConfigError` and `OSError` →
   `skipped.append(f'{name} (config unreadable)')`. A product with no `backlog_dir` or one that is
   not a directory → `skipped.append(f'{name} (no backlog_dir)')`. A `backlog_dir` with no
   `index.json` → `skipped.append(f'{name} (no index.json)')` — the string §3.4 and §2.4 both
   spell for `extra`. A `backlog_dir` whose `os.path.realpath` equals `root`'s is dropped as a
   duplicate and is not skipped either (the rollup run over a checkout rather than a clone).
4. The docstring says what §1.4 decided: a product whose record cannot be read is *named* and left
   out of the Total, because zero is a number an operator will act on and a named omission is not;
   and that a rollup must never fail because another product's checkout is missing. It also carries
   P12: every path here is opened read-only and nothing is written under another product's
   `backlog_dir`.
5. `ThroughputRecordsTests`, all of it under `mock.patch.object(env, 'ASF_HOME', <temp dir>)`
   (P10 — the module attribute, not `os.environ`): write `products/sample.yaml`,
   `products/demo.yaml` and `products/extra.yaml` into that home, give `demo` a `backlog_dir` with
   an `index.json` and `extra` one without. Assert `throughput_records(root, 'sample')` is
   `[('sample', root), ('demo', <demo backlog_dir>)]` with `['extra (no index.json)']` skipped, and
   that `root` is used for `sample` even though `sample.yaml` names a different `backlog_dir`.
   Then: a yaml that will not parse is skipped with its reason and raises nothing; a
   `backlog_dir` that does not exist is skipped; a product whose `backlog_dir` *is* `root` appears
   once. Finally `throughput_records(root, None)` is `([('this record', root)], [])` with
   `env.ASF_HOME` patched to a path that does not exist — nothing outside `root` is opened.

**Gate**

```sh
python3 -m unittest tests.test_metrics -v
python3 -m unittest tests.test_00_home tests.test_env -v
python3 tools/run_tests.py
bash tools/check_generic.sh && bash tools/check_conventions.sh && python3 -m asf.cli check
```

**Acceptance**

```bash
python3 -m unittest -v tests.test_metrics.ThroughputRecordsTests
```

Covers §3.4: this record from `root` and never from its own `backlog_dir`, every other readable
product from its `backlog_dir`, the three unreadable shapes each skipped with a reason and raising
nothing, the self-duplicate dropped, and the no-product-name call touching no `~/.ASF` at all.

```bash
cd "$(git rev-parse --show-toplevel)"
python3 tools/run_tests.py
```

---

### Task 4: the scorecard's first line
stories: S-18904
writes: asf/metrics/metrics.py, tests/test_metrics.py
after: Task 3

§2.4. The section itself: the headline sentence, the note line, the per-product table and the
weighted Total, put above `## Waste`, with `cmd_rollup` resolving the record list once and printing
the same numbers to stdout. Everything below the new section is byte-identical to what the same
fixture renders today.

**Files**

- `asf/metrics/metrics.py` — `_delta_int`, `_money`, `_delta_money`, `throughput_total(rows)`,
  `throughput_rows(root, day, items, records)` and `throughput_block(day, rows, skipped)` after
  `throughput_records`; `render_daily` (`:552-566`) gains `records=None, skipped=(), rows=None`
  and the new first section; `cmd_rollup` (`:1203-1231`) resolves the list and prints its line.
- `tests/test_metrics.py` — `EXPECTED_DAILY` (`:289`) gains the `records=None` section (P4);
  `ScorecardFirstLineTests` (new) with the two-record fixture of §2.4.

**Steps**

1. The three formatters exactly as §2.4 fences them: `_delta_int(now_, then)` →
   `f"{now_ - then:+d}"` (so no change is `+0`, never `0`); `_money(v)` → `'—'` when None else
   `f"${v:,.2f}"`; `_delta_money(now_, then)` → `'—'` when either is None, else
   `f"{'+' if now_ >= then else '-'}${abs(now_ - then):,.2f}"`. Copied as written (P15).
2. `throughput_total(rows)` → the Total's `(week, prev)` pair: `features` and `priced` summed
   across the rows, `usd` summed over the priced (None when nothing was priced), `mean` the
   **weighted** mean — total priced USD over total priced count, never the mean of the per-product
   means (§1.4). Used by `throughput_block` for its Total row and its headline, and by `cmd_rollup`
   for its stdout line, so the three can never disagree.
3. `throughput_rows(root, day, items, records)` → `[(label, week, prev)]`: for each
   `(label, r)` in `records`, `record_throughput(r, items if os.path.realpath(r) ==
   os.path.realpath(root) else match.load_index(r), day)[:2]`. This record reuses the `items` map
   `cmd_rollup` already loaded; every other record's comes from `match.load_index` on its own path
   (§2.4). A record whose index will not load returns an empty map rather than raising — it was
   already checked for `index.json` in Task 3, and a rollup does not fail on another product.
4. `throughput_block(day, rows, skipped)` → the list of markdown lines, pure over its arguments:
   `## Features on production`, the headline sentence restating the Total row (count, mean, last
   week's pair, `Δ features`, `Δ cost`, then "Every section below is diagnostic."), the note line
   naming both windows, the `by the day each Feature's stage reached \`on-prod\`` clause and the
   whole-life-cost sentence, then `Not read: <names joined by ', '>.` **only when `skipped` is
   non-empty**, then the seven-column table and the `**Total**` row. The windows in the note are
   `days_back(day, 7)[0] … [-1]` and `days_back(day, 14)[:7][0] … [-1]`, so the printed
   `2026-09-15 … 2026-09-21 against 2026-09-08 … 2026-09-14` falls out of the same call
   `record_throughput` makes.
5. `render_daily(root, day, items, conv=None, records=None, skipped=(), rows=None)`: the two
   existing lines of `out` stay exactly as they are, then
   `out += throughput_block(day, rows if rows is not None else throughput_rows(root, day, items, records or [('this record', root)]), skipped)`
   and `out += ['## Waste', …]`. With `records` and `rows` both None the section is one row
   labelled `this record` whose Total equals it — printed all the same, so the section is never
   absent (§2.4). The `rows` keyword is P6; the docstring says it is the rollup's already-computed
   list and that None means "compute it here".
6. `cmd_rollup`, after `conv` and before `render_daily`:
   `records, skipped = throughput_records(root, getattr(args, 'product', None) or None)`,
   then `rows = throughput_rows(root, day, items, records)` and
   `text = render_daily(root, day, items, conv, records, skipped, rows)`. After the
   `wrote/unchanged` print and **before** `cost:` (§4), from `throughput_total(rows)`:
   `print(f"features: {w['features']} on prod this week, {_money(w['mean'])} each (last week {p['features']}, {_money(p['mean'])})")`.
7. `EXPECTED_DAILY` gains the `records=None` section between the `generated:` line and `## Waste`:
   one row labelled `this record`, a `**Total**` equal to it, no `Not read:` line. Both tables
   below it keep every byte they have today — that is the regression net for "everything else on
   the card is diagnostic".
8. `ScorecardFirstLineTests`: build §2.4's fixture on the existing `fixture_streams` base so the
   `generated:` counts stay `(3), (4), (2)` and `## Cost per Feature (7 days)` stays unchanged, and
   put every extra dollar on stream days **before `2026-09-15`** (P5) — `sample` shipping 3 priced
   Features totalling $37.20 this week and 1 at $31.00 last, `demo` shipping 0 this week and 2 last
   (one priced $8.10, one not), `extra` skipped. Assert the rendered section equals §2.4's block
   byte for byte; that it appears **before** `## Waste`; that the Total's `$/feature` is `$12.40`
   and not the mean of `$12.40` and `$8.10`; that `demo`'s `$/feature` and its Δ are `—` and not
   `$0.00`; that `Priced` reads `3/3` and `0/0`; that `Not read: extra (no index.json)` is present
   and absent when nothing was skipped; and that `## Waste` and `## Cost per Feature (7 days)` are
   identical to `EXPECTED_DAILY`'s below the section.

**Gate**

```sh
python3 -m unittest tests.test_metrics -v
python3 -m unittest tests.test_tick tests.test_tick_steps -v
python3 tools/run_tests.py
bash tools/check_generic.sh && bash tools/check_conventions.sh && python3 -m asf.cli check
```

**Acceptance**

```bash
python3 -m unittest -v tests.test_metrics.ScorecardFirstLineTests tests.test_metrics.Rollup
```

Covers §3.5: the section rendered exactly as §2.4 prints it and above `## Waste`; the headline
carrying both numbers, last week's pair and both deltas; the weighted Total; `—` where nothing was
priced; the `Priced` column; the `Not read:` line present and absent; and `EXPECTED_DAILY`'s two
tables unchanged below the new section.

```bash
cd "$(git rev-parse --show-toplevel)"
python3 tools/run_tests.py
```

---

### Task 5: a quiet week, one product, and no product config
stories: S-18905
writes: tests/test_metrics.py
after: Task 4

§3.6. The first line is the first line whether or not anything shipped, whether or not there is a
second product, and whether or not `~/.ASF` can be read at all. This Task writes no production
code: if a clause fails, the fix belongs in Task 4's code and this Task's gate is what finds it.

**Files**

- `tests/test_metrics.py` — `ThroughputEdgeTests` (new). No `asf/` file is touched.

**Steps**

1. `render_daily(root, day, items)` with no `records`: the section is present, one row labelled
   `this record`, the `Total` row equal to it, the headline exactly
   `0 features on production this week, — per feature — last week 0, — · Δ features +0 · Δ cost —`,
   and no `Not read:` text. The em dashes are §2.4's and are asserted as written (P15).
2. The same over an empty record — no `index.json`, no `metrics/` directory at all — renders the
   same section and raises nothing (P8: `read_stream` globs an absent directory to `[]`).
3. `asf metrics rollup <day>` over that record exits 0 and prints a `features: …` line, and that
   line is before the `cost:` line in its stdout (§4). Driven through the module's own CLI the way
   `run_cli` already does in this file.
4. A record whose only Feature shipped in both windows' worth of history — an event in each week —
   is counted in exactly one: the newer event wins (`shipped`'s "newest wins"), so this week has it
   and the previous does not.
5. `render_daily` with `records` given but `env.ASF_HOME` patched to an unreadable path raises
   nothing, because the enumeration already happened in `cmd_rollup` — the proof that
   `throughput_records` is the only function that touches the operator home.
6. If any clause needs a change under `asf/`, it is a defect in Task 4's code: fix it there, in
   this Task's commit, and say so in the commit message. Nothing in `asf/` is edited to make a test
   pass in a way that changes what §2.4 renders.

**Gate**

```sh
python3 -m unittest tests.test_metrics -v
python3 tools/run_tests.py
bash tools/check_generic.sh && bash tools/check_conventions.sh && python3 -m asf.cli check
```

**Acceptance**

```bash
python3 -m unittest -v tests.test_metrics.ThroughputEdgeTests
```

```bash
python3 -m unittest -v tests.test_metrics
```

Covers §3.6: the section printed for a quiet week, for a single record and for an empty one; the
rollup exiting 0 with its `features:` line; a Feature counted in exactly one window; and the
operator home untouched by `render_daily` itself.

```bash
cd "$(git rev-parse --show-toplevel)"
python3 tools/run_tests.py
```

---

coverage: 6/6 stories; uncovered: none
