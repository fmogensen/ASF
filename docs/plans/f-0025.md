# Plan F-0025 — typed envelopes: every job ends with a machine-readable report

Epic: E-0001 — ASF 0.1 — a working factory. Spec: `docs/specs/f-0025.md` (approved). Branch: `plan/F-0025`.

Six Tasks, cut along the spec's own design sections by *who owns the report at that moment*: the
schema module and every prose reader it replaces (Task 1), the contract a brief prints (Task 2),
the fixtures that must emit one before anything demands it (Task 3), the verdict (Task 4), the
correction (Task 5), and the three readers that file what the report says (Task 6).

The order is not a preference. This repository *is* the factory, and landed code judges the
sessions that land the next Task. A `judge` clause that refuses a report before the briefs ask for
one (Task 2) and before the fixtures emit one (Task 3) would fail every live session and most of
the suite on the tick it lands. Task 4 is therefore downstream of both, and Tasks 5 and 6 are
downstream of the verdict they act on.

Wave order: **1 → 2 ‖ 3 → 4 → 5 → 6**.

The spec was written against `origin/main` at `dfe10fe` and is right about the twelve kinds, the
registry, `judge`, `hold` and the goldens. It is wrong or silent in four places where D11's
deletion of `report.parse` reaches a reader the design never names: `asf.briefs.facts.last_report`
(PD4), `report.ruling_fields` and the adjudicate schema (PD5), the adjudicate template's own word
`REPORT` (PD6), and the printed line a multi-line correction would produce (PD7). Each is recorded
below and closed inside the Task that causes it. No scope is added: every one of them is a reader
the spec already decided to retype, met where it actually lives.

§"Whole suite and the gates" binds every Task, so every Gate below runs the whole suite and both
`tools/` checks beside its own fence.

## Decisions

| # | Question | Decided |
|---|---|---|
| PD1 | How the work is cut | By the spec's `## The design` sections, grouped by the module that owns the report at that step, with each Task's Acceptance being the spec's own fenced test command for the file it writes. Every Gate is that fence plus `python3 -m unittest discover -s tests -t .`, `bash tools/check_generic.sh` and `bash tools/check_conventions.sh`. |
| PD2 | Task card ids | Not minted here. `asf.record.plan_tasks.mint_plan_tasks` (`asf/record/plan_tasks.py:48`) turns each `### Task N:` heading of the landed plan into a card with an id from the record's own minter, so this plan's headings are the parser's ordinals `1…6`. This session's `BACKLOG_ID_RANGE` (`T:17700-17749`) is therefore unused; no id is invented. |
| PD3 | Which Story is which | The spec's `## Stories` bullets carry their ids outright (S-14850 … S-14857), one per acceptance command, so the `stories:` lines below quote them directly. The brief's "The Stories: (none yet)" means the record has not minted the cards yet, not that the spec has none. |
| PD4 | **`asf.briefs.facts.last_report` is a live prose reader the spec says does not exist** | The spec's Out says "`repo_facts['last_report']` is filled by no caller today (`asf/briefs/preamble.py:247` — only tests pass it)". It is filled: `facts.repo_facts` sets it from `facts.last_report` (`asf/briefs/facts.py:154`), which calls `report.parse` (`:134`) over `REPORT_FIELDS = ('status', 'pushed', 'tests', 'left out', 'ruling')` (`:31`); and `repo_facts` is called in production from `asf/briefs/build.py:332` and `asf/tick/step_wave.py:194`. D11 deletes `parse` from under it. Task 1 re-bases `last_report` on `typed(text, None)` — the kind-less shape the spec designed for exactly this caller — keeping the same five capped fields, with `'left out'` becoming `left_out`. This is not the spec's Out: no new fact is carried into a brief, the one that was already carried is read off the object instead of the prose. |
| PD5 | **`report.ruling_fields` and the three adjudicate fields the schema omits** | `ruling_fields` (`asf/workers/report.py:72`, F-0090 D10) reads `blocked_on`, `writes` and `superseded_by` off the prose block, and the prose TAIL asks every kind for them (`asf/briefs/build.py:96-98`, asserted by `tests/test_briefs.py:234`). The spec's `adjudicate` schema names only `ruling`, `upheld`, `overruled`, and D11 deletes the `parse` that `ruling_fields` stands on. Left as written, an adjudicate session would be asked in prose for three fields the typed schema would refuse as unknown keys, and F-0090's mechanism would go dark on the tick this lands. Task 1 adds `blocked_on` (str\|null), `writes` (list) and `superseded_by` (str\|null) to `KIND_FIELDS['adjudicate']` and reads `ruling_fields` off the typed object, `None` for an absent or empty field as today. The spec's D5 rules and its Out ("validating a field's *content*") are untouched: no id is checked against the record. |
| PD6 | **The adjudicate template says `REPORT`, and the new test refuses that word** | §"tests/test_briefs.py" adds `test_no_template_carries_a_contract_of_its_own`: no `templates/*.md` may match `REPORT`, `Final message` or an `asf-report` fence. Two lines in one template carry it — `adjudicate.md:35` (the `Final message:` line the spec deletes) and `adjudicate.md:19`, "line of the REPORT below", which the spec does not mention. Task 2 rewords `:19` to name the `ruling` field in lower case; `tests/test_briefs.py:251`'s existing assertions on that template (the three field names, "A paragraph with no field behind it changes nothing", the order of `THE RULING GOES TO THE RECORD` before `` `blocked_on: T-0025` ``) carry no `REPORT` and stay as they are. |
| PD7 | **A multi-line correction would print a page** | `lifecycle.hold` builds its printed line as `held {branch}: {text} — back to its session (round N)` (`asf/workers/lifecycle.py:628-631`) and health records `line.split(': ', 1)[1]` in `found`. `unpushed_text` and `empty_branch_text` are one line; `report_text` is a sentence, a contract and twenty lines of tail. Task 5 passes the full text to `hold` — the correction the session gets is the whole of D9 — and builds the *printed* line itself from the `why` and `hold`'s `rounds`: `held <branch>: bad report: <why> — back to its session (round 1)`, which is the spec's own Records line, and `— adjudicate pending` when `hold` returned no `rounds`. `hold` is not touched, so D6's "nothing new is invented for it" holds. |
| PD8 | The correction session is judged on `correct`, not on the kind it re-prints | The FIX → CORRECT row gives the held branch `brief_kind='correct'` (`asf/feeder/rows.py:271`), so the correction run's own report is a `correct` report, while the text it was handed asks it to re-print the original kind's. That is D9 as written and it does not loop: `report_spent` makes the branch immune to a second report hold (D8), so a correction that again reports badly is judged on its work. Task 5's `test_the_second_time_it_is_not_held_again` is that guarantee. No row kind changes. |
| PD9 | `rows.correction_rows` needs no new kind | It reads a correction's `kind` only for the at-cap reason string (`asf/feeder/rows.py:268`) and hands `c['text']` to the brief regardless. A `report` hold therefore rides the existing FIX → CORRECT row with no edit, which is what the spec's D7 assumes; Task 5's `test_the_feeder_sends_it_back_to_the_same_branch` asserts it rather than a change causing it. |
| PD10 | `{contract}` is safe in `TAIL` | `build.render` is `str.format` over the template (`asf/briefs/build.py:153-160`), and the contract's JSON braces arrive as a *substituted value*, not as template text, so they are never re-parsed. The one new key is `context()['contract']`, and `placeholders(TAIL)` gaining `contract` is what the existing placeholder test already covers. |
| PD11 | The brief no longer ends with a fence | `tests/test_briefs.py:231` asserts `text.rstrip().endswith('```')`. The spec's contract block ends with the paragraph "It must parse and it must match this shape exactly …", so that assertion is false the moment the tail changes. Task 2 re-points it at the contract's closing sentence — the same guarantee (the tail is the last thing in the brief), measured against what the tail now ends with. |
| PD12 | Three existing fixtures assert prose behaviour and go red at Task 1 | `tests/test_workers.py:869` (a prose `pushed: no` is held), `tests/test_lifecycle.py:204` (a groom's prose `pushed: no` is not unpushed work) and `tests/test_tick_steps.py:255` (a prose `ruling:` is filed). Each is a reader the spec retypes, so each fixture becomes `report.render(kind, …)` in Task 1 — the file is in Task 1's `writes:` for that reason alone, and Task 3 rewrites the rest of those same files behind it. No assertion is weakened: what was proved of the prose is proved of the object. |
| PD13 | `report.UNPUSHED` keeps its string | `health.UNPUSHED_REASON_PREFIXES` is built from it (`asf/workers/health.py:61`) and `lifecycle.judge` compares against `runtime_mod.report.UNPUSHED` (`:481`). The spec keeps the constant; the plan keeps its value `'unpushed work'` byte for byte, so the one `end_reason` prefix the factory has matched on since B-0051 does not move. |

PD4, PD5, PD6 and PD7 are the four places the spec's design meets the tree as it stands. None of
them widens the card: each is a reader D11 deletes the ground from under, met in the Task that
deletes it.

---

### Task 1: the schema module, and every prose reader it replaces
stories: S-14850, S-14851, S-14852, S-14854
writes: asf/workers/report.py, tests/test_report.py, asf/briefs/facts.py, tests/test_brief_facts.py, tests/test_workers.py, tests/test_lifecycle.py, tests/test_tick_steps.py
after: none

The spec's `asf/workers/report.py — rewritten` section, whole. The module stops being a regex and
becomes the owner of the report: the common fields, the twelve field tables, the checker, the
reader, the renderer and the contract text. `parse`, `unpushed` and `NO_RE` are deleted, not
deprecated (D11), so the three readers outside the module that stood on them are retyped in the
same diff (PD4, PD5, PD12). Nothing yet *demands* a report: `check` exists and nobody calls it.

**Files**

- `asf/workers/report.py` — **rewritten**. `ReportError`; `REQ`, `STATUSES`, `PUSHED`,
  `FENCE_TOKEN`, `UNPUSHED` (unchanged value, PD13), `BAD_REPORT`; `COMMON` and `KIND_FIELDS` as
  the spec tables them, with `adjudicate` carrying `blocked_on`, `writes`, `superseded_by` besides
  `ruling`, `upheld`, `overruled` (PD5); `_CHECK`/`_check`; `schema`, `fence`, `typed`, `check`,
  `failure`, `ruling`, `ruling_fields`, `summary`, `render`, `contract`. Imports `json` and `re`
  and nothing of ASF's — `build.KINDS` is not imported, the table is the spec's twelve words
  written out, and `tests.test_report` is what holds the two equal.
- `asf/briefs/facts.py` — `last_report` reads `report.typed(text, None)` inside a
  `report.ReportError` guard that returns the ledger line alone; `REPORT_FIELDS` becomes
  `('status', 'pushed', 'tests', 'left_out', 'ruling')`; a list-valued field is capped through
  `_cap` on its joined items (PD4). The docstring keeps saying what it says — never the
  transcript above the report.
- `tests/test_report.py` — **rewritten**: `SchemaTest`, `ParseTest`, `RenderTest`, `ContractTest`,
  `ReadersTest`, exactly the spec's cases, plus one case for `ruling_fields` over a typed
  adjudicate report and one asserting an absent field is `None` (PD5, the F-0090 assertions at
  `:105-122` restated against the object).
- `tests/test_brief_facts.py` — the four `last_report` cases (`:189-210`) script typed reports
  through `report.render`; `:245`'s `repo_facts` shape is unchanged.
- `tests/test_workers.py:869` — the held self-reported-unpushed fixture becomes
  `report.render('coder', status='partial', pushed='no', why='the suite is still running in the background')`
  appended to the same prose. The assertions are untouched (PD12).
- `tests/test_lifecycle.py:204` — the groom fixture becomes `report.render('groom', pushed='no',
  why='a ruling is not a commit')`; still `FINISHED`.
- `tests/test_tick_steps.py:255` — the adjudicate log's result becomes
  `report.render('adjudicate', ruling='no open finding; the fix stands')`; `file_rulings` files
  the same line.

**Steps**

1. `COMMON`, the enumerations and `_CHECK`/`_check`, mirroring `asf.metrics.metrics`'s
   `_CHECK`/`_apply` idiom (D2) without importing it: a tuple of `_CHECK` names is a type list, a
   tuple of plain strings is an enumeration of values.
2. `KIND_FIELDS`: one entry per kind of the spec's table, each field
   `(types, default, instruction)` and the instruction being the `Final message:` line it
   replaces. `adjudicate` gains PD5's three, whose instructions are the prose TAIL's own words
   (`the id this item must wait for, or none`, and so on).
3. `schema(kind)` — `COMMON` merged with the kind's, `ReportError` on an unknown kind.
4. `fence(text)` — the last fenced block whose info line carries `asf-report`; a fence is three
   backticks or more and closes on the first line of at least that run.
5. `typed(text, kind=None)` — parse, then check: unknown keys (`kind` given only), required keys,
   types, enumerations, then D5's three cross-field rules. One sentence per failure, each naming
   the key, in the spec's words.
6. `check`, `failure`, `ruling`, `ruling_fields`, `summary` — the five readers, none of which
   raises; `failure` returns `UNPUSHED` for `pushed: no` and `None` for an unreadable report (D6).
7. `render(kind, **fields)` — documented placeholders for every required field the caller omits,
   `ReportError` on a field the kind has not got, output a fenced `json asf-report` block.
8. `contract(kind)` — the spec's text, `COMMON` first then the kind's own, so the twelve share
   their first eleven lines.
9. `facts.last_report` per PD4, then its four tests.
10. `tests/test_report.py` rewritten; the three fixtures of PD12 converted.

**Gate**

```sh
python3 -m unittest tests.test_report -v
python3 -m unittest tests.test_brief_facts tests.test_workers tests.test_lifecycle tests.test_tick_steps -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
python3 -m unittest tests.test_report -v
```

- `SchemaTest`
  - `test_every_brief_kind_has_a_schema`: `sorted(report.KIND_FIELDS) == sorted(build.KINDS)`,
    and `report.schema(k)` returns for each without raising.
  - `test_every_field_declares_types_a_default_and_a_line`: for every kind, every field's tuple
    is `(types, default, instruction)` with a non-empty instruction and types that are either all
    `_CHECK` names or all plain values.
  - `test_a_kind_field_never_shadows_a_common_one`:
    `set(KIND_FIELDS[k]) & set(COMMON) == set()` for every kind.
  - `test_an_unknown_kind_refuses`: `report.schema('preflight')` raises `ReportError`.
- `ParseTest`
  - `test_the_block_is_read_out_of_the_final_message`: prose, then a fenced `json asf-report`
    object → `typed(text, 'spec')` is that object.
  - `test_the_last_block_wins`: two blocks → the second.
  - `test_no_block_is_a_named_failure`: `check('spec', 'I did the thing.')` names
    `asf-report`.
  - `test_broken_json_is_a_named_failure`: a block holding `{"item":` → the message carries the
    JSON error.
  - `test_an_unknown_key_is_refused`: a `spec` report with `"plan": "…"` → `unknown key(s) plan`.
  - `test_a_missing_required_key_is_refused`: no `pushed` → names `pushed`.
  - `test_a_bad_enum_is_refused`: `"status": "finished"` → names `done, partial, blocked`.
  - `test_the_three_cross_field_rules`: `pushed: yes` with no `sha`; `pushed: no` with no `why`;
    `status: blocked` with empty `needs_operator` and `left_out` → each refused, each named; and
    each satisfied → accepted (D5).
  - `test_a_good_report_of_every_kind_parses`: `typed(render(k), k)` for every
    `build.KINDS` → no raise, and `check` is `None`.
- `RenderTest`
  - `test_render_round_trips_through_typed`: for every kind, `typed(render(k, item='F-0025'),
    k)['item'] == 'F-0025'`.
  - `test_render_refuses_a_field_the_kind_does_not_have`: `render('close', stories=[])` raises.
  - `test_render_fills_every_required_field`: for every kind, the rendered object's keys are a
    superset of the required ones.
- `ContractTest`
  - `test_the_contract_names_every_field_of_the_kind`: for every kind, `contract(k)` contains
    each field name of `schema(k)` and each field's instruction text.
  - `test_the_contract_is_the_shape_it_asks_for`: the JSON in `contract(k)`, with its
    `<…>` placeholders replaced by `render(k)`'s values, passes `typed(…, k)`.
  - `test_the_common_fields_come_first_in_every_kind`: the first eleven lines of the object in
    `contract(k)` are the same for all twelve kinds.
- `ReadersTest` — the card's second acceptance, stated as a contradiction
  - `test_the_json_wins_over_the_prose`: a text whose prose REPORT says `pushed: no — waiting for
    the suite` and whose typed report says `"pushed": "yes", "sha": "abc1234"` →
    `report.failure(text)` is `None` and `runtime.result_ok(rec)` is True.
  - `test_and_the_other_way_round`: prose `pushed: yes abc1234`, typed `"pushed": "no", "why":
    "the suite is still running"` → `report.failure(text)` is `report.UNPUSHED` and
    `runtime.failure_reason(rec)` is the same.
  - `test_the_ruling_is_read_off_the_json`: a text whose prose `ruling:` says one thing and whose
    typed `ruling` says another → `report.ruling(text)` is the typed one.
  - `test_the_prose_reader_is_gone`: `report` has no attribute `parse`, `unpushed` or `NO_RE`.
  - `test_a_malformed_report_is_not_a_failure_reason`: a result with no block →
    `report.failure(text)` is `None` and `runtime.result_ok(rec)` is True (D6: `judge` catches
    it, not this).
  - `test_summary_is_one_line`: `summary` of a done report, a `pushed: no` report and a blocked
    report → three one-line strings naming the status and, where there is one, the reason.

---

### Task 2: the contract every brief prints, and no template of its own
stories: S-14853
writes: asf/briefs/build.py, asf/briefs/templates/adjudicate.md, asf/briefs/templates/close.md, asf/briefs/templates/coder.md, asf/briefs/templates/correct.md, asf/briefs/templates/fix-bug.md, asf/briefs/templates/fixer.md, asf/briefs/templates/groom.md, asf/briefs/templates/plan.md, asf/briefs/templates/rebase.md, asf/briefs/templates/reshape.md, asf/briefs/templates/review.md, asf/briefs/templates/spec.md, tests/fixtures/briefs/golden/adjudicate.md, tests/fixtures/briefs/golden/close.md, tests/fixtures/briefs/golden/coder.md, tests/fixtures/briefs/golden/correct.md, tests/fixtures/briefs/golden/fix-bug.md, tests/fixtures/briefs/golden/fixer.md, tests/fixtures/briefs/golden/groom.md, tests/fixtures/briefs/golden/plan.md, tests/fixtures/briefs/golden/rebase.md, tests/fixtures/briefs/golden/reshape.md, tests/fixtures/briefs/golden/review.md, tests/fixtures/briefs/golden/spec.md, tests/test_briefs.py
after: Task 1

The spec's `asf/briefs/build.py` and `asf/briefs/templates/*.md` sections. The generated block is
appended to `TAIL` after the prose one (D10), eleven templates lose the `Final message:` line
whose words are now field instructions, and a test refuses a template that invents a contract.
Nothing reads the block yet — this Task makes the factory *ask*, and Task 4 makes it read.

**Files**

- `asf/briefs/build.py` — `from asf.workers import report as report_mod` beside the `stall`
  import (`:32`); `TAIL` gains `{contract}` after its fenced prose REPORT block (PD10);
  `context()` gains `'contract': report_mod.contract(kind)` — one key, nothing else.
- `asf/briefs/templates/*.md` — eleven `Final message:` lines deleted (`adjudicate.md:35`,
  `close.md:13`, `coder.md:18`, `fix-bug.md:30`, `fixer.md:20`, `groom.md:30`, `plan.md:29`,
  `rebase.md:18`, `reshape.md:16`, `review.md:26`, `spec.md:27`); `adjudicate.md:19` reworded to
  name the `ruling` field in lower case (PD6). `correct.md` is unchanged; the twelve files are
  listed in `writes:` because the test that refuses a contract reads all of them.
- `tests/fixtures/briefs/golden/*.md` — all twelve regenerated with `ASF_UPDATE_GOLDEN=1`, and
  the diff read before it is committed: each must differ only by the deleted line and the added
  contract block.
- `tests/test_briefs.py` — `ContractInBriefTest` (new, the spec's four cases); `:231`'s
  "ends with a fence" assertion re-pointed at the contract's closing sentence (PD11); `:234` and
  `:251` unchanged (the prose TAIL keeps its ruling lines, D10).

**Steps**

1. The import and `context()`'s one key; run `python3 -m unittest tests.test_briefs.PlaceholderTest`
   to prove no template can reach for `contract` and change it.
2. `TAIL`: the prose block stays byte for byte, `{contract}` on its own after it.
3. The eleven deletions, one file at a time, checking that the words each line named are the
   instruction text of a field in that kind's schema — where they are not, the schema is wrong,
   not the template, and it is Task 1's table that is corrected on this branch.
4. `adjudicate.md:19` per PD6, keeping `THE RULING GOES TO THE RECORD` before
   `` `blocked_on: T-0025` `` so `:251` still passes.
5. `ContractInBriefTest`, then `ASF_UPDATE_GOLDEN=1`, then read the twelve diffs, then the suite.

**Gate**

```sh
ASF_UPDATE_GOLDEN=1 python3 -m unittest tests.test_briefs
git diff --stat tests/fixtures/briefs/golden
python3 -m unittest tests.test_briefs -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
ASF_UPDATE_GOLDEN=1 python3 -m unittest tests.test_briefs   # regenerate, then read the diff
python3 -m unittest tests.test_briefs -v
```

- `ContractInBriefTest`
  - `test_every_brief_carries_its_kind_s_contract`: for each of `build.KINDS`, the built brief
    contains `report.contract(kind)` verbatim.
  - `test_no_template_carries_a_contract_of_its_own`: no `templates/*.md` matches
    `REPORT`, `Final message`, or a fence carrying `asf-report` — the card's
    "a template cannot invent its own".
  - `test_the_contract_a_brief_prints_is_the_one_the_reader_checks`: for each kind, the JSON in
    the built brief's contract block parses and, with its placeholders filled, passes
    `report.typed(…, kind)`.
  - `test_the_goldens_match`: the existing per-kind byte comparison, over the twelve regenerated
    goldens.

---

### Task 3: the fake runtime emits a report, and so does every fixture
stories: S-14852
writes: asf/workers/runtime.py, tests/test_workers.py, tests/test_lifecycle.py, tests/test_tick_steps.py, tests/test_session_identity.py, tests/test_sample_product.py
after: Task 1

The spec's `asf/workers/runtime.py — FakeRuntime` section and D12. Every scripted result gains a
schema-valid report before anything demands one, so that Task 4's clause lands on a suite that is
already telling the truth. `failure_reason` keeps its shape — the typed body arrived with Task 1.

**Files**

- `asf/workers/runtime.py` — `FakeRuntime.run` writes
  `step.get('result', '') + '\n\n' + report.render(kind, …)` into the log's result line, with
  `kind = step.get('kind') or 'coder'`; `step.get('report')` false writes no report at all.
  `Result(text=…)` stays the step's own string, so every existing assertion on `Result.text`
  holds. The class docstring gains the two new step keys. `failure_reason` is unchanged.
- `tests/test_workers.py`, `tests/test_lifecycle.py`, `tests/test_tick_steps.py`,
  `tests/test_session_identity.py`, `tests/test_sample_product.py` — the scripted results that
  assert on the *log's* result text (rather than `Result.text`) updated to expect the appended
  block; any fixture that needs a particular kind gains `'kind': …` on its step.
- `tests/test_workers.py` — `FakeRuntimeReportTest` (new, the spec's two cases).

**Steps**

1. `FakeRuntime.run` per the spec, `report` imported at module scope (it already is, `:21`).
2. Run the five files and fix what the appended block moved, one file at a time; a test that
   asserted on the log's result string asserts on its first line or on `report.check` being
   `None`, never on the absence of a report.
3. `FakeRuntimeReportTest`.
4. The whole suite, because `FakeRuntime` is what most of it runs on.

**Gate**

```sh
python3 -m unittest tests.test_workers tests.test_lifecycle tests.test_tick_steps tests.test_session_identity tests.test_sample_product -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
python3 -m unittest tests.test_workers -v
```

- `FakeRuntimeReportTest`
  - `test_the_fake_appends_a_valid_report`: `FakeRuntime([{'ok': True, 'result': 'done',
    'kind': 'coder'}])` → the log's result text starts `done` and
    `report.check('coder', text)` is `None`; `Result.text` is still `'done'`.
  - `test_a_step_can_refuse_to_report`: `{'report': False}` → `check` names the missing block.

---

### Task 4: the bad-report verdict, judged last
stories: S-14855
writes: asf/workers/lifecycle.py, tests/test_lifecycle.py
after: Task 2, Task 3

The spec's `asf/workers/lifecycle.py` section. `judge` gains one clause, after the result's own
failure and after the push checks (D6), so a run that failed for a real reason is judged on that
reason. `report_spent` is the registry read the caller resolves, exactly as `landing` is, and
`report_text` is the correction D9 describes. Nothing holds yet — Task 5 does the holding.

**Files**

- `asf/workers/lifecycle.py` — `RUN_FIELDS` gains `'report'` (B-0041); `REPORT = 'report'` beside
  `UNPUSHED`/`EMPTY`; `BAD_REPORT = report.BAD_REPORT` re-exported for health's prefix test;
  `report_spent(path, run)`; `judge(run, ev, landing=None, report_done=False)` with the clause
  last, before `FINISHED`; `derive` passes `report_done=report_spent(path, run)` beside its
  `landing=lands(run, path)`; `report_text(kind, why, tail)`.
- `tests/test_lifecycle.py` — `BadReportJudgeTest` and `ReportTextTest` (new, the spec's cases).

**Steps**

1. `RUN_FIELDS`, `REPORT`, `BAD_REPORT`.
2. `report_spent`: True when any run on this run's branch carries a `correction` of kind `report`
   or a `report` field of its own; the branch, not the job (D8). `False` for a run with no branch.
3. `judge`'s clause, verbatim from the spec, reading `ev.result and ev.result.get('result')`;
   `report.check(None, …)` is `None`, so a run whose kind names no schema is not judged on its
   report.
4. `derive` passes `report_done`; every other caller of `judge` keeps its two arguments and so
   keeps today's behaviour until Task 5 gives it the third.
5. `report_text`: the sentence, "your work is pushed and is not being re-done — print the report
   again, in this shape", `contract(kind)`, then the last 20 lines of the result under "what you
   said".
6. The two test classes, then the suite.

**Gate**

```sh
python3 -m unittest tests.test_lifecycle -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
python3 -m unittest tests.test_lifecycle -v
```

- `BadReportJudgeTest`
  - `test_a_pushed_run_with_no_typed_report_fails`: an ok result with prose only, a pushed branch
    with commits → `judge(run, ev)` is `failed: bad report: …` naming `asf-report`.
  - `test_a_pushed_run_with_a_good_report_finishes`: the same run with
    `report.render('coder')` appended → `FINISHED`.
  - `test_the_report_is_judged_last`: an unpushed branch *and* no report → the verdict is
    `failed: not pushed: …`, not the report's (D6).
  - `test_a_run_whose_kind_has_no_schema_is_not_judged_on_its_report`: `kind: None` and
    `kind: 'preflight'` with no report → `FINISHED`.
  - `test_report_done_skips_the_clause`: no report, `report_done=True` → `FINISHED`.
  - `test_report_spent_reads_the_branch_not_the_job`: a registry with a `report` hold on run A of
    branch `worker/T-1` → `report_spent(path, {'branch': 'worker/T-1'})` is True for a *different*
    job on that branch, and False for another branch (D8).
  - `test_report_is_a_run_field`: `'report' in lifecycle.RUN_FIELDS`, and a fold over
    `launch, report=unreadable, launch` leaves the second run without it (B-0041).
- `ReportTextTest`
  - `test_the_correction_carries_the_why_the_contract_and_what_was_said`:
    `report_text('spec', 'no ```json asf-report block…', tail)` contains the sentence, every
    field name of the `spec` contract, the words `not being re-done`, and the tail's last line.

---

### Task 5: the correction on its own branch, once
stories: S-14856
writes: asf/workers/health.py, tests/test_workers.py
after: Task 4

The spec's `asf/workers/health.py` section, D7 and D8. A bad report ends the run and hands the
same branch in the same worktree back to a `correct` session whose whole job is to re-print the
report; the second time on that branch, nothing is held — `report: 'unreadable'` is written, the
run is re-judged on its work, and a `bad-report` entry joins `found` for Task 6 to emit.

**Files**

- `asf/workers/health.py` — one `elif reason.startswith(f'failed: {lifecycle.BAD_REPORT}')` after
  the empty-branch arm (`:315-322`): when `lifecycle.report_spent(registry, s)` is False, hold
  with `lifecycle.REPORT` and `lifecycle.report_text(s.get('kind'), why, tail)` and record PD7's
  printed line; when it is True, `pool_mod.update_session(product, job, report='unreadable')`,
  re-judge with `report_done=True`, write the re-judged `end_reason`, and
  `found.append((job, 'bad-report', why))`. `UNPUSHED_REASON_PREFIXES` is untouched: a bad report
  is not unpushed work.
- `tests/test_workers.py` — `BadReportHoldTest` (new, the spec's six cases).

**Steps**

1. The `why` is the reason's own tail (`reason.split(f'{lifecycle.BAD_REPORT}: ', 1)[-1]`), and
   the `tail` handed to `report_text` is the last 20 lines of the result's text.
2. The hold arm, with PD7's printed line built from `why` and `hold`'s `rounds` — `— adjudicate
   pending` when `hold` returned none.
3. The spent arm: `report='unreadable'`, the re-judged reason on the registry and on `s`, the
   `bad-report` entry. No `correction`, no round spent.
4. `BadReportHoldTest`, then the suite.

**Gate**

```sh
python3 -m unittest tests.test_workers -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
python3 -m unittest tests.test_workers -v
```

- `BadReportHoldTest`
  - `test_a_bad_report_ends_the_run_and_holds_its_branch`: a live session whose log's result is
    prose-only on a pushed branch with commits; one `health(product, fix=True)` → the run's
    `end_reason` is `failed: bad report: …`, its registry line carries a `correction` of kind
    `report`, `rounds` is 1, and the printed line says `held <branch>`.
  - `test_the_correction_is_the_contract_for_that_kind`: the held run's `correction['text']`
    contains the `spec` contract's field names and the tail of what the session said.
  - `test_the_feeder_sends_it_back_to_the_same_branch`: `lifecycle.corrections(path)` has the
    item with `kind: 'report'` and the run's own branch, and `rows.correction_rows` turns it into
    a FIX → CORRECT row whose `branch` is that branch (not a new one).
  - `test_the_second_time_it_is_not_held_again`: the same branch, a second run, again no report →
    no new `correction`, the run carries `report: 'unreadable'`, its `end_reason` is `finished`,
    and `found` has `(job, 'bad-report', why)` (D8).
  - `test_a_good_report_is_untouched`: the same fixture with `report.render('spec', …)` appended
    → `end_reason: finished`, no `correction`, no `report` field.
  - `test_an_unpushed_run_is_still_held_for_the_push`: prose-only *and* unpushed → one hold, of
    kind `unpushed`, and the correction text is the push one.

---

### Task 6: the ruling, the session's reason and the bad-report event
stories: S-14857
writes: asf/metrics/metrics.py, asf/tick/step_health.py, tests/test_metrics.py, tests/test_tick_steps.py
after: Task 5

The spec's `asf/tick/step_health.py` and `asf/metrics/metrics.py` sections — the three readers
that file what a report says. `file_rulings` keeps its shape (`report_mod.ruling` was retyped in
Task 1) and gains nothing; the step emits one event per `bad-report` entry Task 5 puts in `found`;
the session's `reason` becomes the typed one-line summary where there is one, and the result's
text where there is not, so an ended run from before this change still reads.

**Files**

- `asf/tick/step_health.py` — `run(ctx, …)` emits `ctx.event('bad-report', job=…, item=…,
  kind=…, why=…)` for each `(job, 'bad-report', why)` in `found`; `file_rulings`'s docstring
  updated to say the ruling is the typed `ruling` field, its body unchanged (`:117`).
- `asf/metrics/metrics.py` — `session_event`'s `reason` (`:927-929`) becomes `report.summary(obj)`
  when the result carries a readable typed report, else today's first-line-of-the-text. The
  `sessions` schema (`:123`) is unchanged: no new key, no migration.
- `tests/test_tick_steps.py` — the two ruling cases and `test_the_bad_report_event_is_written`.
- `tests/test_metrics.py` — `test_the_session_reason_is_the_typed_summary`.

**Steps**

1. `session_event`: read the typed object through `report.typed(text)` (kind-less) inside a
   `ReportError` guard; on success `report.summary(obj)`, on failure the text as today. The import
   is local to the function if a module-level one would cycle.
2. `step_health.run`'s event, keyed off `found` so no second read of the registry is needed.
3. The four tests, then the suite and both checks.

**Gate**

```sh
python3 -m unittest tests.test_tick_steps tests.test_metrics -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
python3 -m unittest tests.test_tick_steps tests.test_metrics -v
```

- `test_a_ruling_is_filed_from_the_typed_report`: an ended `adjudicate` run whose result carries
  `report.render('adjudicate', ruling='the hold was an add/add conflict; the fix stands')` → the
  item's card gains that `## History` line, the run is marked `adjudicated`, and the `ruling`
  event carries the same text.
- `test_a_run_whose_typed_report_has_no_ruling_is_marked_not_filed`: `ruling: ''` →
  `adjudicated: 'none'`, no history line.
- `test_the_bad_report_event_is_written`: one tick over the twice-bad run →
  `metrics/events/<day>.jsonl` in the record clone has one `kind: bad-report` line naming the
  job, the item, the kind and the why.
- `test_the_session_reason_is_the_typed_summary`: a session whose result carries a
  `status: partial`, `pushed: no` report → the `sessions` event's `reason` is
  `report.summary(obj)`, one line; and a result with no typed report still records its text.

---

coverage: 8/8 stories; uncovered: none
