# F-0073 — Plan: everything enters through the inbox; the type is derived from the card's shape

Epic: E-0001 — ASF 0.1 — a working factory
Spec: `docs/specs/f-0073.md` (binding — this plan adds no scope it does not carry)

---

## Decisions

Checked against the checkout at `8fde7e5`. Where the spec is wrong or says nothing, the ruling is
made here and not added to the code without a note.

| # | question | ruling in this plan |
| --- | --- | --- |
| PD1 | **The spec's History line has an extra `)`.** *The design* has callers pass `why="inbox) — shape: {rule} → {type}"`, but `write_new_item` writes `- {date}: created ({why})` (`asf/record/ids.py:79`). That gives `created (inbox) — shape: signature → bug)`, with a trailing `)`, and the spec's own tests (`…History line ending created (inbox) — shape: signature → bug`) would fail on it. | `write_new_item` gains a keyword argument `shape=None`, a `(rule, type)` pair. When it is given, the History line is `- {date}: created ({why}) — shape: {rule} → {type}`. `why` stays the bare origin (`inbox`, `new`, `file-bugs`). The line reads exactly as the spec's *Records* section says. Task 2. |
| PD2 | Where `Card` and `infer_parent_epic` live. `shape.derive` needs `infer_parent_epic` (D9), and `inbox.py` needs `derive`. If `shape.py` imported `inbox.py`, the two modules would import each other. | `infer_parent_epic` moves to `asf/groom/shape.py`, and `asf/groom/inbox.py` imports it back under the same name, so `tests/test_groom.py`'s `inbox_mod.infer_parent_epic` test does not change. `Card = namedtuple('Card', 'title headers description features acceptance')` is defined in `shape.py`, and `inbox.parse_inbox_file` returns one. `shape.py` imports only stdlib and `asf.record.core`, so `asf/record/check.py` can import `shape.SIZE`. Task 1. |
| PD3 | The order of the `parent:` check. The spec says "a `parent:` that doesn't exist gets the same question as today, whichever rule is being tried". Today there are two texts: *"name an existing Epic"* for a Feature and *"name an existing item"* for a Bug. | `derive` checks, in this order: (a) both `signature:` and `writes:` → the "one thing" Question; (b) a `parent:` not in `canonical` → `` `parent: X` does not exist — name an existing item or remove the line. ``; (c) the rule table in order; (d) the `type:` line (D4). `test_missing_parent` asserts only `does not exist`. |
| PD4 | A `signature:` card whose `parent:` is a type outside `PARENT_TYPES['bug']` (a Task, a Bug). The spec gives no text for this case. | Question: `` `parent: X` is a <type> — a Bug hangs under an Epic, a Feature or a Story. `` This mirrors the `default` rule's wrong-type text. |
| PD5 | **Blank lines in the header block.** `asf inbox` writes `# T`, then optionally `parent: ID`, **a blank line**, then the body file. The spec says the body file "can carry any header line". But `parse_inbox_file` stops reading header lines at the first blank line: `body_lines` is no longer empty after it (`asf/groom/inbox.py:46-54`). So a `signature:` line at the top of a body file would be read as description, and the card would never become a Bug. | The header block becomes: every `INBOX_KV_RE` line after the title up to the first line that is neither blank nor a header line. Blank lines inside that block are skipped. `asf inbox` writes the layout the spec gives, and the groom reads the lines the body file adds. Task 3 covers this with one parsing test in `tests/test_groom.py`'s `InboxParsingTests`. |
| PD6 | `parse_inbox_file` no longer returns a 4-tuple. Besides the three `test_infer_*` tests the spec deletes, `test_title_strips_leading_hash` and `test_explicit_type_and_parent_lines` (`tests/test_groom.py:65-78`) unpack that tuple. | The two tests read `Card` attributes (`card.title`, `card.headers.get('type')`, `card.headers.get('parent')`, `card.description`) and keep their assertions. Task 3. |
| PD7 | `asf new story|task --body-file F`. Today `--body-file` replaces the whole body verbatim (`asf/record/new.py:137-139`). A verbatim body would have no `## Acceptance` from `--acceptance` and no `— shape:` History line, so the card would escape D6. | For `story` and `task`, `cmd_new` writes the card with `write_new_item(..., why='new', shape=(rule, type), acceptance=args.acceptance)`, and the `--body-file` text becomes the `## Description`. `decision` and `rule` keep today's path unchanged (their skeleton, or `--body-file` verbatim). The typed fields `cmd_new` sets today (`title`, `parent`, `priority`, `area`, `legacy_id`, `--set` pairs, and `writes` for a Task) go in as `typed_fields`. Task 5. |
| PD8 | Where `--acceptance` and `--writes` are declared. | In `asf.record.new.add_arguments`, next to `--set`. This is the function `tests/test_backlog.py`'s parser-building helper already calls. Its docstring changes from "Bug-only flags" to "the shape flags". `--severity`, `--signature` and `--found-in` go, and so does `SEVERITIES`. `SETTABLE['bug']` is deleted. Task 5. |
| PD9 | `asf/record/plan_tasks.py:80` also mints Tasks, with `why='plan F-nnnn'`. The spec does not mention it. | Unchanged. These Tasks carry `writes:` from the plan, and they have no `— shape:` line, so D6 leaves them unsized. Adding one would widen the spec. |
| PD10 | One intake directory, one lookup. `cmd_groom` needs `intake_dir` from the product (spec), and `cmd_inbox` needs the same value. | `asf.groom.inbox.intake_dir(args)` returns `env.load_product(getattr(args, 'product', None)).conventions.intake_dir`, or `DEFAULT_INTAKE_DIR` on `env.ConfigError`. This is the same pattern `cmd_groom` already uses for `default_bug_epic` (`asf/groom/groom.py:294-299`). `cmd_groom` (Task 3) and `cmd_inbox` (Task 4) both call it. |
| PD11 | Test isolation. `hermetic.build()` inherits `ASF_HOME` (`asf/hermetic.py:62`), so an operator's default product could change `intake_dir` or `default_bug_epic` inside a test. `asf check` also reports the stream folders `make_repo` does not create. | `tests/test_inbox_shape.py`'s `run` wraps `tests.test_groom.run` and sets `ASF_HOME=<root>/no-such-asf-home`, as `tests/test_file_bugs.py:94` does. The one exception is `test_intake_dir_is_the_convention`, which points `ASF_HOME` at a temp home holding `products/p.yaml` and passes `--product p`. The module's `make_record()` calls `make_repo()` and also creates `asf.init.STREAM_FOLDERS`, then runs `asf index` so the cwd looks like a record to `resolve_record`. |
| PD12 | D10 says `asf inbox` resolves the record the way `asf new` does, and that `tests/test_path_resolution.py` tests this. Only if `inbox` is in that file's `COMMANDS` audit table does the audit cover it. | Task 4 adds the row `(['inbox', '--title', 'a card from nowhere'], 'record')` after the `new` row. It asserts that `record:` comes first and that the cwd stays empty. |
| PD13 | **One test module, so the Tasks run in order.** The spec names every acceptance class as `tests.test_inbox_shape.<Class>`, so all five classes live in one file. Every Task writes that file, so the five Tasks run one after another. The code has the same order anyway: sizing needs `shape.SIZE`, intake needs `derive` and `write_new_item`'s new arguments, `asf inbox`'s last test needs intake, and `asf new` needs `write_new_item`'s `acceptance=`. | Five Tasks in five waves, each small enough for one session. Each Task **adds** its class and never edits a class an earlier Task landed. Shared helpers (`make_record`, `seed`, `run`, `card`) go at the top of the module, and the first Task that needs a helper adds it. |
| PD14 | The Story ids. | The spec carries S-7950 … S-7956. The brief says the record holds no Story for F-0073 yet. `asf.record.plan_tasks.stories_of` keeps only ids the record holds, so the Task cards minted from this plan carry `stories:` only once those Stories exist. The record mints the Task card ids when the plan lands, and this plan names none. |

### Shared contracts (the Tasks below code against these)

- `asf.groom.shape`: `Shape(type, rule, parent)`, `Question(text)`,
  `Card(title, headers, description, features, acceptance)` (`headers` is a dict with lower-case
  keys; `features` and `acceptance` are lists of item texts without the `- ` / `- [ ] `
  prefix), `SIZE`, `DEFECT_WORDS_RE`, `RULES = ('signature', 'writes', 'features-list',
  'parent-feature', 'default')`, `infer_parent_epic(canonical, tokens)`,
  `derive(card, canonical, default_bug_parent=None)`.
- `asf.record.ids.write_new_item(root, canonical, type_, new_id, typed_fields, body, date, why,
  acceptance=(), sections=None, shape=None)` (PD1).
- The History line: `- <date>: created (<origin>) — shape: <rule> → <type>`. It is read with
  `SHAPE_LINE_RE = re.compile(r'created \(([^)]*)\) — shape: (\S+) → (\w+)')`, defined once in
  `shape.py`. `asf check` and the groom file both use it.
- `asf.groom.inbox.intake_dir(args)` (PD10).

## Waves

| wave | Task | why this order |
| --- | --- | --- |
| 1 | Task 1: the shape rules | pure; everything else imports `asf.groom.shape` |
| 2 | Task 2: the History line and `asf check`'s size rules | needs `SIZE` and `SHAPE_LINE_RE`; shares `tests/test_inbox_shape.py` |
| 3 | Task 3: intake | needs `derive` and the `write_new_item` arguments |
| 4 | Task 4: `asf inbox` | `test_inbox_then_groom` needs intake; shares `asf/groom/inbox.py` with Task 3 |
| 5 | Task 5: `asf new` | needs `write_new_item(acceptance=, shape=)`; shares `asf/cli.py` and `tests/test_path_resolution.py` with Task 4 |

After every Task, the spec's changed suites and the whole suite are green. No Task is allowed to
leave another suite red for a later Task to fix.

---

### Task 1: The shape rules — `asf/groom/shape.py`
stories: S-7950, S-7951
writes: asf/groom/shape.py, asf/groom/inbox.py, tests/test_inbox_shape.py

**Files**: `asf/groom/shape.py` (new), `asf/groom/inbox.py` (only the move of
`infer_parent_epic`, PD2), `tests/test_inbox_shape.py` (new: `ShapeRulesTest`,
`ShapeQuestionTest`).

**Steps**
1. `shape.py`: `Shape`, `Question`, `Card`, `SIZE` (the spec's five texts, verbatim),
   `RULES`, `SHAPE_LINE_RE`, and
   `DEFECT_WORDS_RE = re.compile(r'\b(broken|red|fails?|failing)\b', re.IGNORECASE)`.
2. Move `infer_parent_epic` into `shape.py` unchanged. `inbox.py` then does
   `from asf.groom.shape import infer_parent_epic`. The rest of `inbox.py` does not change in
   this Task.
3. `derive(card, canonical, default_bug_parent=None)`, in the order PD3 sets:
   - Both `signature` and `writes` in `card.headers` → `Question('A card is one thing: signature: makes it a Bug, writes: makes it a Task — drop one.')`.
   - `parent` given and not in `canonical` → the PD3 text.
   - `signature` → bug. The parent is `parent:` if its type is in `PARENT_TYPES['bug']` (otherwise the PD4 text), else `default_bug_parent`, else `Question('Which item is this Bug under? Add a `parent: <id>` line.')`.
   - `writes` → task. The parent must be a Feature or a Story, otherwise `Question('A Task hangs under a Feature or a Story — add parent: <id>.')`.
   - `features-list`: `len(card.features) >= 2` → epic with parent `None`. If a `parent:` is present → `Question('An Epic has no parent — drop the parent: line, or it is a Feature under that Epic.')`.
   - `parent-feature`: the parent's type is `feature` → story if `card.acceptance` has items, otherwise `Question('A Story is one PR with one acceptance list — add an ## Acceptance checklist.')`.
   - The parent's type is `story` → `Question('Under a Story only a Task hangs — add a writes: line, or parent the card to the Feature.')`.
   - `DEFECT_WORDS_RE` matches title + description and `card.acceptance` is empty → the D5 text, verbatim from the spec.
   - `default` → feature. The parent is a `parent:` naming an Epic. A `parent:` of any other type → `` `parent: X` is a <type> — a Feature hangs under an Epic. ``. With no `parent:`, the parent is `infer_parent_epic(canonical, tokenize(card.title))`, and if that finds nothing → `Question('Which Epic is this under? No open Epic shares a title word with it.')`.
   - Then D4: if `card.headers.get('type')` is present and is not `shape.type` (compared lower-case) → `Question(f"`type: {t}` is not read — the card's shape reads as {type} ({rule}: {SIZE[type]}). Remove the line, or change the shape.")`.
4. `tests/test_inbox_shape.py`: a module docstring, the `card(title, headers=None, description='',
   features=(), acceptance=())` helper, and `seeded_canonical()`. `seeded_canonical()` builds the
   spec's record (E-0001 `Factory billing`, decided; F-0001 `Billing plans` under E-0001; S-0001
   under F-0001) in memory from `frontmatter.parse` of the card texts, with no temp dir. Then
   every `ShapeRulesTest` and `ShapeQuestionTest` case the spec lists, with the names and asserted
   substrings exactly as there.

**Gate**
```bash
python3 -m unittest -v tests.test_inbox_shape.ShapeRulesTest tests.test_inbox_shape.ShapeQuestionTest tests.test_groom
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

**Acceptance**
```sh
python3 -m unittest tests.test_inbox_shape -v
```
- S-7950 — `python3 -m unittest tests.test_inbox_shape.ShapeRulesTest -v`
- S-7951 — `python3 -m unittest tests.test_inbox_shape.ShapeQuestionTest -v`

---

### Task 2: The record keeps the reading — `write_new_item`, file-bugs' shape line, `asf check`'s size rules
stories: S-7955
writes: asf/record/ids.py, asf/record/check.py, asf/tick/file_bugs.py, tests/test_inbox_shape.py

**Files**: `asf/record/ids.py`, `asf/record/check.py`, `asf/tick/file_bugs.py`,
`tests/test_inbox_shape.py` (adds `CheckShapeTest` and the helpers `make_record`, `seed`, `run`
from PD11).

**Steps**
1. `write_new_item(..., acceptance=(), sections=None, shape=None)`:
   - The body is `## Description`, then each `sections` entry as `## <heading>\n- <item>…\n\n`,
     in the map's order, then `## Acceptance` with one `- [ ] <item>` per item (or today's single
     `- [ ] ` when there are none), then `## Non-goals`, `## History`, `## Children` and
     `## Backlinks` as today.
   - An empty `sections` value (for example `{'Features': []}`) writes no heading.
   - The History line gets `f" — shape: {rule} → {type}"` appended when `shape` is given (PD1).
   - With the defaults, the output is byte-identical to today's.
2. `file_bugs.py:199` passes `shape=('signature', 'bug')`, and `why` stays `'file-bugs'`.
3. `check.py`: after the per-item checks, add a size pass. It covers every open item (`is_open`)
   whose `## History` section (`parse_sections`) has a line matching `shape.SHAPE_LINE_RE`, and
   checks the item against the spec's table:
   - bug: `signature` is non-empty.
   - task: `writes` is a non-empty list.
   - story: `## Acceptance` has at least one `- [ ]`/`- [x]` line with text after the box.
   - epic: `## Features` has at least 2 `- ` bullets, or `derived[iid]['children']` holds at least 2 items of type `feature`.
   - feature: neither `writes` nor `signature`.

   Each finding is `add(rec, find_line(rec, 'id'), …)`, with the spec's text and the parenthesis
   taken from `shape.SIZE`. Items with no shape line are skipped (D6), and so are items that are
   not open.
4. `CheckShapeTest`: every case the spec lists. The items are written with `write_item` and a
   body whose History carries the shape line. `test_file_bugs_writes_the_shape_line` reuses
   `tests/test_file_bugs.py`'s CI-line fixture (`write_ci_line`, two failures on `main` within
   24h), runs `asf file-bugs`, and checks the History line and that `asf check` reports no
   shape finding.

**Gate**
```bash
python3 -m unittest -v tests.test_inbox_shape.CheckShapeTest tests.test_file_bugs tests.test_backlog tests.test_t10_gate
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

**Acceptance**
```sh
python3 -m unittest tests.test_inbox_shape -v
```
- S-7955 — `python3 -m unittest tests.test_inbox_shape.CheckShapeTest -v`

---

### Task 3: Intake reads the shape — `process_inbox`, the groom line, the intake directory
stories: S-7952, S-7956
writes: asf/groom/inbox.py, asf/groom/groom.py, tests/test_inbox_shape.py, tests/test_groom.py, tests/test_t10_gate.py

**Files**: `asf/groom/inbox.py`, `asf/groom/groom.py`, `tests/test_inbox_shape.py` (adds
`IntakeTest`), `tests/test_groom.py`, `tests/test_t10_gate.py`.

**Steps**
1. `INBOX_KV_RE` widens to `type|parent|signature|severity|writes|stories`.
   `parse_inbox_file(text) -> shape.Card`:
   - The header block follows PD5.
   - `## Features` and `## Acceptance` are lifted out of the rest of the text. A Features item is
     the text after `- `. An Acceptance item is the text after `- [ ] ` or `- [x] `, and empty
     boxes are dropped.
   - Everything else, stripped, is `description`.
   - `writes` and `stories` values stay raw strings in `headers`. Step 3 splits them.
2. `intake_dir(args)` (PD10). Remove `INBOX_TYPES`, `infer_inbox_type`, `BROKEN_WORDS_RE` and
   `GOAL_WORD_RE`.
3. `process_inbox(root, canonical, date, default_bug_parent=None, intake_dir='inbox')`:
   - Skip a file that already has a `## Question`, as today.
   - Call `derive`. A `Question` is appended as today.
   - A `Shape` is minted with `mint_id` and `write_new_item(..., why='inbox', shape=(rule, type), acceptance=card.acceptance, sections={'Features': card.features})`.
   - Typed fields: `title`, `parent`, `decided: False`. A Bug also gets `severity` (the header, if it is `S1`/`S2`/`S3`, else `S3`), `found_in: dev` and `signature`. A Task also gets `writes` and `stories` as lists, split on `,` and stripped.
   - The move to `<intake_dir>/done/<slug>.md` stays as today.
4. `groom.py`:
   - `groom_inbox_section` reads `SHAPE_LINE_RE` from the card's body. When it matches, the why
     is `from inbox as <type> (<rule>), awaiting a decision`. Otherwise it stays
     `from inbox, awaiting a decision`.
   - `cmd_groom` passes `intake_dir=inbox.intake_dir(args)`.
5. `tests/test_groom.py`:
   - Delete `test_infer_bug_from_broken_language`, `test_infer_epic_from_goal_language` and
     `test_infer_feature_default`.
   - Move the two parsing tests to `Card` attributes (PD6), and add
     `test_header_lines_after_a_blank_line` (PD5).
   - The three `Checkout is broken` intake tests add `signature: checkout-pay` in the header
     block. `test_inbox_bug_with_no_default_configured_asks_a_question` still expects its
     Question.
   - The `ambiguous feature` test is unchanged.
6. `tests/test_t10_gate.py`: the `report.md` card becomes
   `# Checkout is broken\nparent: E-0009\nsignature: checkout-pay\nCustomers cannot pay right now.\n`.
   The counts do not change.
7. `IntakeTest`: every case the spec lists. `test_epic_card_keeps_its_features` runs `asf index`
   and then `asf check` on the `make_record` fixture, which has the stream folders (PD11).
   `test_intake_dir_is_the_convention` writes a product file with `conventions:\n  intake_dir: intake`
   under a temp `ASF_HOME` and runs `asf groom --product p`.

**Gate**
```bash
python3 -m unittest -v tests.test_inbox_shape.IntakeTest tests.test_groom tests.test_t10_gate
python3 -m unittest tests.test_groom tests.test_backlog tests.test_t10_gate tests.test_schema_version tests.test_path_resolution tests.test_file_bugs -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

**Acceptance**
```sh
python3 -m unittest tests.test_inbox_shape -v
```
```sh
python3 -m unittest tests.test_groom tests.test_backlog tests.test_t10_gate tests.test_schema_version tests.test_path_resolution tests.test_file_bugs -v
```
- S-7952 — `python3 -m unittest tests.test_inbox_shape.IntakeTest -v`
- S-7956 (the `test_groom` and `test_t10_gate` half) — `python3 -m unittest tests.test_groom tests.test_backlog tests.test_t10_gate tests.test_schema_version tests.test_path_resolution tests.test_file_bugs -v`

---

### Task 4: `asf inbox` — one untyped card into the record's intake directory
stories: S-7953
writes: asf/groom/inbox.py, asf/cli.py, tests/test_inbox_shape.py, tests/test_path_resolution.py

**Files**: `asf/groom/inbox.py` (adds `cmd_inbox`), `asf/cli.py`, `tests/test_inbox_shape.py`
(adds `InboxCommandTest`), `tests/test_path_resolution.py` (the PD12 row).

**Steps**
1. `cli.py`:
   - Add the subparser `inbox` with
     `help="file an untyped card into the record's intake dir (the groom types it)"` and the
     arguments `--title` (required), `--body-file`, `--parent` and `--product`. There is no
     `--type`.
   - Dispatch with
     `cmd_inbox(args, resolve_record(args, announce=_announce_stderr))`.
2. `cmd_inbox(args, root)`:
   - `d = os.path.join(root, intake_dir(args))`, created if it is missing.
   - The slug is `re.sub(r'[^a-z0-9]+', '-', title.lower()).strip('-')`, or `card` if that is
     empty. If the name is taken, append `-2`, `-3`, and so on.
   - The text is `# {title}\n`, then `parent: {id}\n` if given, then `\n`, then the body file
     verbatim.
   - Print the path relative to `root` and return 0. It mints nothing and runs no groom.
3. `InboxCommandTest`: every case the spec lists. `test_writes_an_untyped_card` compares a
   before/after listing of every item folder. `test_inbox_then_groom` runs `asf inbox --title
   "Billing invoices" --parent E-0001` and then `asf groom`, and expects exactly one new file
   under `features/` whose `parent` is `E-0001`.
4. `tests/test_path_resolution.py`: add the PD12 row.
5. Nothing is added to `asf.plugin_build`, because `inbox` is neither a view nor a dialogue.
   `python3 -m asf.plugin_build check` must stay clean.

**Gate**
```bash
python3 -m unittest -v tests.test_inbox_shape.InboxCommandTest tests.test_path_resolution tests.test_plugin
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

**Acceptance**
```sh
python3 -m unittest tests.test_inbox_shape -v
```
- S-7953 — `python3 -m unittest tests.test_inbox_shape.InboxCommandTest -v`

---

### Task 5: `asf new` mints only what a spec or plan cuts — Stories with acceptance, Tasks with writes
stories: S-7954, S-7956
writes: asf/record/new.py, asf/cli.py, asf/init.py, tests/test_inbox_shape.py, tests/test_backlog.py, tests/test_schema_version.py, tests/test_path_resolution.py

**Files**: `asf/record/new.py`, `asf/cli.py` (the `add_arguments` import name only),
`asf/init.py` (the cheat sheet), `tests/test_inbox_shape.py` (adds `NewShapeTest`),
`tests/test_backlog.py`, `tests/test_schema_version.py`, `tests/test_path_resolution.py`.

**Steps**
1. `cmd_new`: the first statement refuses `epic`, `feature` and `bug`. It prints
   `error: <type> is new work — it enters through the inbox: asf inbox --title "…" [--body-file F]; the groom derives its type from the card's shape`
   to stderr, returns 2 and writes nothing.
2. `add_arguments` (PD8):
   - Add `--acceptance TEXT` and `--writes GLOB`, both `action='append'`.
   - Remove `--severity`, `--signature` and `--found-in`, and remove `SEVERITIES` from
     `new.py`.
   - Delete `SETTABLE['bug']`.
   - `cli.py` imports the function by its new alias `add_new_shape_arguments`.
3. `story` with no `--acceptance` → `error: a Story is one PR with one acceptance list — give --acceptance`,
   exit 2. `task` with no `--writes` → `error: a Task is one session with a writes: footprint — give --writes`,
   exit 2. Both checks run after the parent checks that exist today.
4. `story` and `task` are written through `write_new_item` (PD7):
   - A Story gets `shape=('parent-feature', 'story')` and `acceptance=args.acceptance`.
   - A Task gets `shape=('writes', 'task')` and the typed field `writes=args.writes`.
   - Both use `why='new'`.
   - The title overlap refusal (exit 3) and `--force` stay as they are, and the id is still
     printed on stdout.
5. `asf/init.py:82` becomes the two lines the spec gives: `asf inbox --title "…"      file new work (the groom types it)`
   and `asf new story|task …      cut a Story or Task from a spec or plan`.
6. `tests/test_backlog.py`. Seed `E-0001` and `F-0001` with `write_item` wherever a test minted
   them before.
   - `NewCommandTests`: sequential ids, overlap and `--force`, and the skeleton move to
     `new story --parent F-0001 --acceptance x`.
     - `test_missing_parent_refused` becomes `new story --title T --acceptance x` → 2.
     - `test_nonexistent_parent_refused` becomes `new story … --parent F-0009 --acceptance x` → 2.
     - `test_epic_rejects_parent` becomes `new decision --title T --parent E-0001` → 2 (a
       Decision has no parent).
     - `test_wrong_parent_type_refused` adds `--acceptance x` so that it still fails on the
       parent type.
   - `NewSetFieldsTests` uses `new story` and drops `source=review` (a Bug-only field).
     `severity=S1` is still refused.
   - `BugSeverityTests` keeps its two `asf check` tests unchanged. Its three `new bug` tests are
     replaced by `test_new_bug_refused`: exit 2, `enters through the inbox`, and an empty
     `bugs/`.
7. `tests/test_schema_version.py`:
   - Seed `E-0001` and `F-0001` with `write_item`, with `schema_version:` in their machine
     lines.
   - Run `new story --title 'Ship it' --parent F-0001 --acceptance x`.
   - Assert the stamp on `stories/S-0001`. The rest of the test is unchanged.
8. `tests/test_path_resolution.py`:
   - Add a `features/F-0001.md` card under `E-0001` to the fixture.
   - Line 28 and line 141 become
     `new story --title … --parent F-0001 --acceptance x`, and line 141 keeps
     `--product sample`.
9. `NewShapeTest`: every case the spec lists.

**Gate**
```bash
python3 -m unittest -v tests.test_inbox_shape.NewShapeTest tests.test_backlog tests.test_schema_version tests.test_path_resolution
python3 -m unittest tests.test_groom tests.test_backlog tests.test_t10_gate tests.test_schema_version tests.test_path_resolution tests.test_file_bugs -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

**Acceptance**
```sh
python3 -m unittest tests.test_inbox_shape -v
```
```sh
python3 -m unittest tests.test_groom tests.test_backlog tests.test_t10_gate tests.test_schema_version tests.test_path_resolution tests.test_file_bugs -v
```
```sh
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```
- S-7954 — `python3 -m unittest tests.test_inbox_shape.NewShapeTest -v`
- S-7956 (the `test_backlog`, `test_schema_version` and `test_path_resolution` half) — `python3 -m unittest tests.test_groom tests.test_backlog tests.test_t10_gate tests.test_schema_version tests.test_path_resolution tests.test_file_bugs -v`

---

## The whole gate (the spec's *Whole suite and the genericity gate*) — green after Task 5

```sh
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

## Story coverage

| Story | Task(s) |
| --- | --- |
| S-7950 | Task 1 |
| S-7951 | Task 1 |
| S-7952 | Task 3 |
| S-7953 | Task 4 |
| S-7954 | Task 5 |
| S-7955 | Task 2 |
| S-7956 | Task 3, Task 5 |

coverage: 7/7 stories; uncovered: none
